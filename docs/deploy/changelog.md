# 部署文档变更记录

| 日期 | 文档 | 版本 | 变更内容 | 关联 Commit |
|------|------|------|---------|-------------|
| 2026-04-15 | [railway.md](railway.md) | v1.0 | Railway PaaS 部署指南：环境变量、Volume 配置、数据源诊断、任务系统验证、FAQ | `fa3c653` |
| 2026-05-02 | [railway.md](railway.md) | v1.1 | **Vite base 修复**：`vite.config.ts` 设置 `base = "/static/dist/"`，否则 Rollup 写入 manifest 的 modulepreload `<link rel="modulepreload">` URL 是 `/assets/...` 不带 `/static/dist/` 前缀，Flask 路由 404，浏览器分两次报 chunk load 错误。`tests/web/test_vite_asset_base.py` (8 case) 锁 manifest 路径契约 + react-vendor chunk 必须独立存在 + card chunk ≤250kB + socket/ChartPanel 仅在 `dynamicImports` 不在静态 `imports`。**Smoke**: `curl -I https://<railway-host>/static/dist/assets/index-*.js` 应 200 + `cache-control: public, max-age=31536000, immutable` | `2396ab1`（fix(frontend): set vite base） |
| 2026-05-02 | [railway.md](railway.md) | v1.1 | **静态资源 cache header**：`web/app.py::add_static_asset_cache_headers` 注册 after_request hook：`/static/dist/assets/*` → `public, max-age=31536000, immutable`（content-hash 命名安全长缓存）、`/static/dist/.vite/manifest.json` → `no-store`（部署后立即生效）、其它 `/static/dist/*` → `public, max-age=3600`。`tests/web/test_static_cache_headers.py` (2 case) 锁两条契约。**Smoke**: 部署后清浏览器缓存重新加载，DevTools Network 验证三类路径的 cache-control 头 | 同上 |
| 2026-05-02 | [railway.md](railway.md) | v1.1 | **modulepreload + lazy bundle 修复**：`tests/web/test_vite_modulepreload.py` (3 case) 锁 manifest 中所有页面 entry 的 `imports`/`dynamicImports` 边界 — analysis 页 `lazy-bundle` 不再静态 import 回 `src/islands/analysis/main.tsx`（v1.5 chunk 循环修复）；socket / ChartPanel 仅在 dynamicImports（避免首屏阻塞）。**Smoke**: 部署后 `https://<host>/analysis/<id>` 第一屏 OverviewCard 正常显示而非 fallback；DevTools Network 看到 `socket-*.js` 仅在订阅时按需加载 | `5e918ef`（feat: analysis rendering） + `2396ab1` |
| 2026-05-03 | [railway.md](railway.md) | v1.2 | **gunicorn / eventlet 工作进程**：生产服务通过 `gunicorn --worker-class eventlet -w 1 stock_trading_system.web.app:app` 启动以承载 SocketIO；`-w 1` 单进程是必要约束（多进程会让 per-user room 广播分裂、TaskManager 全局 singleton 失效）；横向扩容用 SocketIO sticky session + Redis adapter 而非 worker。**Smoke**: `ps -ef \| grep gunicorn` 应见 1 个 worker；提交分析任务后 `task_progress` 实时事件能到达浏览器 socket（DevTools WS 帧抓包） | — |
| 2026-05-03 | [railway.md](railway.md) | v1.2 | **前端 dist asset 路径变更**：`/static/dist/assets/*-<hash>.js` 路径每次 `npm run build` 都因 content hash 变化；CI 必须在 `npm run build` 后才能 `git commit` 推送，否则 `manifest.json` 引用的 chunk 名跟实际 dist 文件名对不上 → 浏览器拉空 404。Railway nixpacks 在 build 阶段已自动跑 `npm run build`，但本地 dev → main 直推需手动 build。**Smoke**: 部署后 `curl https://<host>/static/dist/.vite/manifest.json \| jq '.["src/islands/analysis/main.tsx"].file'` 应返回真实存在的文件名 | — |
| 2026-05-03 | [railway.md](railway.md) | v1.3 | **生产环境 LLM fallback 配置**：`config/default_config.yaml` 新增 `llm.fallback_enabled = true`（默认开启），缺第二 provider api_key 时自动降级为 bare primary（无 fallback）不破坏部署；监控 `result.fallback_counts.{gemini→qwen, qwen→gemini}` 可观测限流频次。**Smoke**: 故意 unset `GEMINI_API_KEY`，提交一个 quick AI 分析，应自动用 Qwen 完成而非 500 | — |
| 2026-05-03 | [railway.md](railway.md) | v1.3 | **任务事件持久化**：`task_events` 表（envelope `{task_id, user_id, seq, event, payload, emitted_at}`）已迁移到生产 SQLite/Postgres；`/api/tasks/events?task_id=...&since=<seq>` 是断线重连后的 catch-up 入口，前端 `subscribeTaskStream` on connect 自动调用。**Smoke**: 部署后提交一个 deep 分析、断网重连，列表行进度从 task_events 回放恢复到正确阶段 | — |
