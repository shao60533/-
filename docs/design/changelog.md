# 设计方案变更记录

| 日期 | 文档 | 版本 | 变更内容 | 关联 Commit |
|------|------|------|---------|-------------|
| 2026-05-17 | [screener-v3.md](screener-v3.md) | v1.3 | **选股结果页头部紧凑化 + 圆桌/大师中文化 + 股票名称暴露**（用户 2026-05-17 提，~1.5h，纯前端 [`ScreenerV3Page.tsx`](../../stock_trading_system/web/frontend/src/islands/screener-v3/ScreenerV3Page.tsx)）：**零后端契约改动**。**症状**：(a) v1.2 用 6 个 `<Stat>` 排顶部 KPI，[stat.tsx:17](../../stock_trading_system/web/frontend/src/components/ui/stat.tsx#L17) `<Stat>` 内部就是 `<Card>` → 结果页头部=7 张独立卡视觉占用过高；(b) 圆桌辩论 verdict badge `CONSENSUS`/`CONTESTED` 是英文；(c) 14 大师 signal `BULLISH`/`BEARISH`/`NEUTRAL`（`(s.signal).toUpperCase()` 透传后端枚举）；(d) 全页只显股票代码，A 股六位数字+美股缩写混合识别成本高。**4 项变更**：(1) "标题行 + 6 `<Stat>`" → 单 `<Card>`（CardHeader 装标题/CardContent 装 KPI grid），新建 `SummaryKpi` 内联组件替代 `<Stat>`，`tone="bull"|"bear"|"neutral"` 决定数值颜色；移动端栅格 `grid-cols-2` → `grid-cols-3`（两行 3 列代替 3 行 2 列）。(2) verdict badge 中文化：`rt.split` 仍为唯一真值源，`true`→`不共识`(sell variant) / `false`→`共识`(buy variant)，**不重新推**避免与后端 `consensus_gurus` 不一致；CardContent 新增"共识方:"/"异议方:"两行明确列出 `consensus[]`/`dissent[]` 大师名单（v1.0 已有的字段，v1.2 只显示 `共识 N 人 · 异议 N 人` 计数）。(3) 新加表示层 `signalLabelZh()` helper（`includes("bull"\|\|"buy")`→`看多` / `includes("bear"\|\|"sell")`→`看空` / else→`中性`），替换桌面表 + 移动卡 + 展开卡 3 处英文 badge，**底层 GuruSignal.signal 英文枚举不动**。(4) `stockName()` 多 key fallback helper（`short_name`/`shortName`/`long_name`/`longName`/`name`）4 处暴露：圆桌标题 `{ticker} · {name}` / 桌面表 ticker 列下小字（`max-w-[140px] truncate` 钉死宽度防挤压它列） / 移动卡 ticker 同行 truncate / 展开卡标题 `{ticker} · {name} — 大师评分详情`，**100% 复用** v1.2 已有的 `funds` state（`/api/fundamentals/<ticker>` 30s LocalCache），无新端点无新 fetch。**显式覆盖** v1.2 §14.4.1（6 卡→1 卡）/§14.4.3（英文 verdict→中文）/§14.4.4（英文 signal→中文）。**强约束**：不动后端 schema / DTO / `_aggregate` / `RoundtableResult` / v1.2 既有的 `signalBadge()` 颜色映射 / `consensusBadge()` / `VotesBar` / `CandidateExpanded` 结构 / 桌面表 8 列布局；helper 全部对 null/undefined/空串/未知 signal 安全降级。**验证**：`npx tsc -b` exit 0 / `npm run build` 通过（`screener-v3-DJZcHAAk.js` 46.19KB gzip 14.21KB）/ `npm run test:unit` 37/37 pass。**流程教训**：本期编码先动手再补 doc，违反 [CLAUDE.md](../../CLAUDE.md) "先搞清楚问题本质再动手 → 写方案 → 更新 changelog → 实施"流程，事后补；下次必须方案在前。 | `c76867b` |
| 2026-05-13 | [hardening-iteration-v1.md](hardening-iteration-v1.md) | v1.0 | **硬化迭代 v1（安全 / 多租户 / 数据可信度 / 业务版本收敛 / Web 拆分）施工图**：基于 2026-05-13 全仓 4 路并行审查（Web 安全+架构 / 数据层+业务逻辑 / 多租户+持久化 / 全仓 grep）综述报告的 **9 Critical + 16 High** 起 5 Phase 6 周迭代。**核心定性**：约 70% 内容是对 [multi-tenant.md](multi-tenant.md) §5.1 已声明 `Flask-WTF` 依赖但代码层 grep `CSRFProtect\|flask_wtf` **0 命中** / [auth/decorators.py:22 admin_required](../../stock_trading_system/auth/decorators.py#L22) 已实现但漏装在 `/api/settings`（[web/app.py:2654](../../stock_trading_system/web/app.py#L2654)）与 `/api/scheduler/start`（[web/app.py:2630](../../stock_trading_system/web/app.py#L2630)）/ [p0a_data_partition.py](../../stock_trading_system/migrations/p0a_data_partition.py) 给 `alert_history` 加了 `user_id` 列但 [save_alert_trigger](../../stock_trading_system/portfolio/database.py#L624) 不传值列恒为 NULL 等的**回归补齐**；约 30% 是新发现的版本演进遗留：(1) screener v1/v2/v3 三套并存且都被引用（v1 → web/main/telegram，v2 → workers，v3 → workers），(2) [strategy/backtest.py](../../stock_trading_system/strategy/backtest.py)（web 入口）vs [strategy/backtester.py](../../stock_trading_system/strategy/backtester.py)（worker 入口）字段名 `total_return_pct vs total_return` + 年化口径 252 vs 365 + 滑点 1% vs 0 + RSI Wilder vs SMA 全部不一致，(3) [v3 cache.py:51 `local_cache.set(..., ttl=...)`](../../stock_trading_system/screener/v3/cache.py#L51) 调用 [LocalCache.set](../../stock_trading_system/data/local_cache.py#L123) 不存在的形参 → 14 guru × N ticker 缓存全部静默失效（异常被 `except Exception: logger.debug(...)` 吞），(4) [LocalCache:92-95](../../stock_trading_system/data/local_cache.py#L92) 未知 category 退化"无 TTL"=永不过期，`regime` / `guru_signal_v3` / `screen_results` 多 category 未注册。**5 Phase**：P0 安全底座 W1（CSRF 接入 + 漏装 admin_required + alerts/portfolio owner 强制 + 限流 + 统一 error handler）/ P1 多租户契约强制化 W2（Telegram bot 白名单 + 删 legacy TaskScheduler._post_market_close 跨租户路径 + `_user_id() raise` + cross_user_access 矩阵从 3 → 12+ + invariants 补 5 条）/ P2 数据可信度 W3（LocalCache.set 加 ttl 形参 + 未知 category 拒绝 + Pickle → JSON + Qwen prompt 主题外置 + datetime 时区统一 53 处 + Paper Trade Decimal 化 + Polygon 限速锁 + Quote Pydantic 模型）/ P3 业务版本收敛 W4-W5（screener v1/v2 → v3 收口 + backtest.py 退役 + DataManager delegate DataRouter + yfinance 直调收口 + 迁移 runner + analysis_history schema 统一 + task_events.seq DB 化）/ P4 Web 拆分 W6（4517 行 app.py 按域拆 11 个 Blueprint + 闭包提到 services + request_id + audit 中间件）。**§ 复用 / Reuse**：L0×8（auth/decorators / cross_user_access / invariants / task_store PRAGMA / screener.v3.pipeline / strategy.backtester.BacktestEngine / daily_snapshot_scheduler / templates/layout.html 既有 meta） / L1×7（Flask-WTF 已依赖未接入 + Flask-Limiter + pydantic 已用 + tenacity 已用 + zoneinfo stdlib + Decimal stdlib + email-validator） / L2×1（yoyo-migrations 思路 clean-room 80 LOC） / L4×8（统一 error_handler / Telegram authz / `_user_id raise` / themes.yaml loader / utils/timez / Paper Decimal / 迁移 runner / Blueprint + audit middleware）。**估算净 LOC**：+520 自写 −1390 删除 ≈ **−870**（v1/v2 screener + backtest.py + Pickle 路径 + fallback 兜底 + duplicate analysis_history schema）。**测试**：新增 12 测试文件约 82 case + 扩 cross_user_access 12+ 条 + invariants 加 5 条。**不动清单**：auth/ 全 11 模块（已通过审查）/ OAuth provider 主体 / Paper Trade 业务逻辑 / UI React Island / TradingAgents monkey-patch / Telegram bot 命令语义（仅加白名单不动 /buy /sell /alert）/ analyzer.py 967 行 god object（独立迭代）。**Rollout**：P0/P1 直接合主线（修漏洞优先级最高）/ P2.6 Decimal feature flag `paper_trade_decimal=true` dogfood 1 周 / P3.1 P3.2 deprecated shim 保留 2 周 / P4 单独长分支 dogfood 后合。**配套文档**：[test-cases/hardening-iteration-v1.md](../test-cases/hardening-iteration-v1.md) 待 P0 前新建 + multi-tenant.md v1.1 addendum 注"CSRF/admin_required 已在 hardening-iteration-v1 落实"+ engineering-principles.md v1.1 加本期复用比例审计条目 + README.md 更新功能演进时间线。**触发**：[CLAUDE.md](../../CLAUDE.md) 第一性原理"先搞清楚问题本质再动手"，本期不是新设计是把已有约束执行到位。 | — |
| 2026-05-15 | [batch-analyze-holdings.md](batch-analyze-holdings.md) | v1.2 | **batch_analysis 子记录拆分修复**：v1.0/v1.1 把整批结果当 JSON blob 塞进 `task_results_generic`，每只持仓没有独立 `analysis_history` 行 → `/api/history` 与 `/api/history/<id>` 详情页都看不到拆分结果，`user_analysis_advice` / agent score / paper trade side effects 也只跑父任务一次。**修复**：[`TaskManager.record_child_analysis`](../../stock_trading_system/tasks/task_manager.py) 新方法，每只成功 ticker 用合成 `task_id = batch:{parent_task_id}:{ticker}:{index}` 调 `task_store.save_result("analysis", ...)` 写 `analysis_history`，复用同一份 `_post_analysis_save` hook（user_advice + agent_score + paper_trade）。[`WorkerDeps.task_manager`](../../stock_trading_system/tasks/workers.py) 字段由 `register_default_workers` 回注，batch worker 拿到后逐只调用；失败/skipped 路径不变（失败只在 items 留 error，skipped 仍只保留 `last_analysis_id`）。父任务仍承载批量进度 + `batch_analysis_item` 事件 + 汇总 `succeeded/failed/skipped` —— 任务中心仍是一个 `batch_analysis` 父行。**回归保护**：单只 `/api/analyze` 路径不变（pytest case 4 钉住"恰好 1 行 analysis_history"）。**新测试**：[`tests/tasks/test_batch_analysis_history_split.py`](../../tests/tasks/test_batch_analysis_history_split.py) 4 case（2 成功 / 1 成功 1 失败 / skipped 不重复 / 单只回归）+ [`tests/web/test_api_history_batch_visibility.py`](../../tests/web/test_api_history_batch_visibility.py) 1 case（live Flask: batch 提交后 `/api/history?include_running=true` 返回 2 条 `task_id` 以 `batch:` 前缀的子行，每行可通过 `/api/history/<id>` 打开完整结构化详情）。 | local |
| 2026-05-15 | [onboarding.md](onboarding.md) | v1.0 | **新手引导施工图**（基于 [`demo_onboarding_v1.html`](../../demo_onboarding_v1.html) 高保真原型）：**Schema** 新表 `user_onboarding`（`user_id PK + welcome_pending/welcomed/tour_completed/checklist_dismissed bool + steps_completed JSON + tour_skipped_at_step int + updated_at`），idempotent migration 接 [`auth/bootstrap.py`](../../stock_trading_system/auth/bootstrap.py) 启动链不动 `users` schema。**Backend** [`auth/onboarding_repository.py`](../../stock_trading_system/auth/onboarding_repository.py) 新建（`get_or_init / init_for_new_user / mark_step（KNOWN_STEPS frozenset 校验 + 幂等）/ mark_welcomed / dismiss_checklist / reset`，fail-soft 所有写操作 try/except + log warn 不抛——onboarding 写失败不阻塞业务）+ [`web/app.py`](../../stock_trading_system/web/app.py) 4 API 端点 `GET /api/onboarding/state` / `POST /api/onboarding/mark-welcomed` / `POST /api/onboarding/dismiss-checklist` / `POST /api/onboarding/reset`，**无** complete-step 公开端点（防前端伪造，任务完成只能由后端 hook 触发）；注册时 `_onboarding_repo.init_for_new_user(new_user.id)` 写 `welcome_pending=1`（同步改 `/api/auth/register` 与 `/api/auth/oauth/register` 两条注册路径）。**4 任务完成钩子**复用现有 API：`add-holding`（portfolio.buy 成功）/ `first-analysis`（analysis worker save_analysis）/ `first-screen`（screen_v3 worker success）/ `first-paper-plan`（paper_trade save_plan），统一调 `_mark_onboarding_step(user_id, step_id)` fail-soft helper，worker 内从 `params["__user_id__"]` 取 user。**Frontend** 新建 [`components/shared/onboarding/`](../../stock_trading_system/web/frontend/src/components/shared/onboarding/) 模块：`useOnboardingState.ts` hook（fetch state + markWelcomed/dismissChecklist/reset + auto refresh）+ `useOnboardingTour.ts`（Driver.js 1.3 wrapper，6 步 verbatim 配置）+ `tour-steps.ts` 6 步常量 + `WelcomeModal.tsx`（移动端 z-100 modal，蓝色 badge / 3 大能力卡 / 双 CTA `稍后再说`+`开始 60 秒导览` / 黄底风险提示底部）+ `OnboardingChecklist.tsx`（z-6 浮于 tabbar 上方，4 任务列表，progress bar，100% 自动隐藏 + 庆祝 toast，右上 × 永久关闭）+ `EmptyStateCTA.tsx`（共享组件，4 处空状态复用）。**Driver.js 集成**：`npm install driver.js@^1.3.1` 本地依赖不走 CDN（保国内访问 + 离线开发）+ [`styles/onboarding.css`](../../stock_trading_system/web/frontend/src/styles/onboarding.css) dark theme 覆写（popover/footer/btn 全部贴合 shadcn dark）。**z-index 分层**：主内容 0 / tabbar 5 / Checklist 6 / topbar 40 / WelcomeModal 100 / Driver.js overlay 999。**6 锚点 ID 注入**：MobileTopbar `#topbar` / DashboardPage AccountOverviewCard `#account-hero` / HoldingsSection `#holdings-section` + `#batch-analyze-card` / Sidebar MobileTabbar `[data-mobile-tabbar]` / OnboardingChecklist `#onboarding-checklist`（每个宿主组件仅加 1 行 id/data-* 属性，**不改内部实现**）。**AppShell 集成**：mount `<WelcomeModal>` 与 `<OnboardingChecklist>`，条件渲染——`welcome_pending && !welcomed` 时弹 modal、`welcomed && !checklist_dismissed && steps_completed<4` 时显 Checklist，state===null 时不渲染任何引导组件（防 race）。**LLMSwitcher inline hint**：localStorage `onboarding_hint_llm_dismissed` 单点控制，首次点 trigger 弹 tooltip 蓝边解释，关闭后永久静默；不依赖后端 state（移动端 hint 不跨设备同步）。**4 处空状态 CTA**：持仓为空 `+ 添加第一只持仓` → 弹买入 dialog / Analysis Inbox 为空 `↓ 发起第一次分析` → 锚定 form / 选股最近为空 `↓ 开始第一次选股` / 纸面列表为空 `→ 看 AI 分析` 跳 `/analysis`。**设置页**加 `<OnboardingSection>` "重新观看引导"按钮 → 调 reset → toast。**严格不动**：[`<LLMSwitcher>`](../../stock_trading_system/web/frontend/src/components/shared/LLMSwitcher.tsx) 内部逻辑（仅加 hint wrapper）/ 桌面 `<Sidebar>` 视觉 / OAuth 路由与流程 / 邀请码 / 多租户隔离 / `users` schema / TradingAgents / LLM / screener / paper-trade 业务逻辑 / 6 个锚点宿主组件内部实现 / shadcn primitives。**测试**：后端 3 文件 ~21 单测（onboarding_repository 7 含跨用户隔离 / API 10 含**无** complete-step 端点 + 注册 hook + OAuth 注册 hook / hooks 4 含 4 个任务完成路径）+ 前端 vitest 3 文件 12 case（WelcomeModal 3 / OnboardingChecklist 5 / useOnboardingState 4）+ Playwright 5 新 case（注册→modal 自动弹 / 跳过→Checklist 显 / 开始导览→Tour 6 步 / 买入→任务划掉 / 4 全完成→Checklist 消失+toast；桌面 ≥md viewport 引导组件 0 渲染断言）+ 手动 5 机型回归 iPhone SE2/14/14 Pro Max/Pixel 7/iPad 10 项。**约 9h 实装 ~1020 LOC** 纯前端 + 后端薄表 + 4 端点。**性能**：移动端首屏增量 ≤8KB gzipped JS（driver.js ~5KB + 组件 ~3KB），Driver.js Tour 启动 ≤200ms，Checklist 渲染 CLS=0。**用户 2026-05-15 决策**：按 demo 收敛 4 项任务（删 `first-alert`，与底 4 tab 1-1）+ 仅移动端不做桌面变体 + 风险提示保持 modal 底黄底 + 100% 完成 600ms 自动隐藏+toast + 跳过策略任意时刻可跳 + 设置页 reset 重开 + Driver.js 库（5KB,Tailwind 友好,React 19 兼容）。 | — |
| 2026-05-13 | [batch-analyze-holdings.md](batch-analyze-holdings.md) | v1.1 — frontend entry patch | **批量分析持仓前端入口接通施工图**（用户 2026-05-13 反馈"这个功能补一下"，关闭 [mobile-ui-v1.3](mobile-ui-v1.3.md) R-MUI-02 标注的"产品缺口"占位）：现状审计 — backend `batch_analysis` worker 已实装（[`tasks/workers.py:782 make_batch_analysis_worker`](../../stock_trading_system/tasks/workers.py)，含跳过近期分析 / 逐只推送 `batch_analysis_item` 事件 / 顺序执行 / 失败容错），[`TasksPage.tsx`](../../stock_trading_system/web/frontend/src/islands/tasks/TasksPage.tsx) 已识别 `batch_analysis` 任务类型，但 2 项前端入口仍未接通：(1) `POST /api/batch/analyze` 路由从未实装（grep `web/app.py` 无此路由，前端无法触发 worker） (2) [`HoldingsSection.tsx:111-137`](../../stock_trading_system/web/frontend/src/islands/dashboard/HoldingsSection.tsx) 当前是 gap-note 占位卡，点击只 `alert("产品缺口：批量分析持仓尚未接入前端入口")`。**修法**：(A) 后端新增 `POST /api/batch/analyze` 仿 [`/api/analyze:1603`](../../stock_trading_system/web/app.py#L1603) pattern——必须登录 + 多租户 `_get_portfolio_for_user(g.user.id)` 拿持仓 + 持仓空返 400 reason=`no_holdings` 避免任务中心被无效任务污染 + 调 `tm.submit("batch_analysis", {skip_recent_hours, date, __user_id__}, title=f"批量分析持仓 · {N} 只", created_by=g.user.id)` + 返 `{task_id, status:"queued", total_holdings}`；(B) 前端新建 `<BatchAnalyzeHoldingsCard holdingsCount={N}>` 替换 HoldingsSection gap-note 卡——保留黄边 hero accent 视觉但**移除"产品缺口"badge**（不再是占位）+ 头部加 `<Sparkles>` icon 强化 AI 操作语义 + 文案改"一键复核所有持仓的最新 AI 观点"+ 提交前 `confirm()` 二次确认（成本+时长提示防误点）+ `holdingsCount===0` 时按钮 disabled 文案变 "暂无持仓" + 提交成功 `toast.success` 带 `action: 查看任务` 一键跳 `/tasks?focus=<task_id>` + 400 `no_holdings` 友好提示 + `busy + disabled` 双重防点重复 + `data-batch-analyze-trigger` 锚点供 Playwright + 第二个 "查看历史批次"按钮跳 `/tasks?type=batch_analysis`。**严格不动**：`make_batch_analysis_worker` 内部 / `_emit_batch_item` 事件 envelope / `tm.register("batch_analysis",...)` 注册链 / TasksPage 任务渲染 / `/api/analyze` 单只路径 / 多租户边界 / HoldingsSection 其余部分（搜索 / 5↔ 全部 / 持仓卡）。**测试**：后端 `tests/web/test_api_batch_analyze.py` 4 case（未登录→401 / 持仓空→400 no_holdings / 3 只持仓→200+task_id+total_holdings=3 / 跨用户隔离 alice bob 互不可见）+ 前端 `tests/frontend/dashboard/batch-analyze-card.test.tsx` 5 case（disabled 文案 / 可点文案 / confirm 取消 / 成功 toast+action / no_holdings 友好提示）。**约 1.5h 实装 ~150 LOC**（后端 50 / 前端 80 / 测试 40 LOC，纯增量，0 schema / 0 worker / 0 task type 改动）。**用户 2026-05-13 决策**：补 v1.0 设计稿（2026-04-18 立项后端实装但前端入口长期欠账）→ v1.1 frontend entry patch；保持 v1.0 设计稿其它部分不变（worker 内部 / WebSocket envelope / 跳过策略 / 任务中心展示） | — |
| 2026-05-11 | [oauth-quick-signin.md](oauth-quick-signin.md) | v1.0 — addendum (login UX 微调) | **登录页 OAuth 按钮位置 + 品牌 logo 双补丁施工图**（用户 2026-05-11 反馈）：(1) `/login` OAuth 按钮区原设计在邮箱密码 form **上方** — 实际邮箱密码是当前用户主登录路径（已注册用户高频复用），OAuth 应是次选；改为 form **下方**，DOM 顺序「邮箱密码 form → divider 或使用 → OAuth 按钮区」。divider 文案从「或使用邮箱密码」改为「或使用」（form 已在上方，不再需要"邮箱密码"提示）。(2) 按钮 logo 升级 — 之前文档只说"加 logo"未指定资产，本期落地两个官方品牌合规 SVG：[`stock_trading_system/web/static/icons/google.svg`](../../stock_trading_system/web/static/icons/google.svg) Google Identity 多色 G mark（4 色规范 #4285F4 蓝 / #34A853 绿 / #FBBC05 黄 / #EA4335 红，viewBox 0 0 48 48）+ [`stock_trading_system/web/static/icons/github.svg`](../../stock_trading_system/web/static/icons/github.svg) octocat 单色 #24292f 深灰（viewBox 0 0 24 24）。按钮容器视觉规范：白底 `#fff` + 灰边 `#d0d7de` + 圆角 6px + flex 居中（`justify-content:center`）+ 18×18 logo + 14px 字号 500 weight + gap 10px label，与 GitHub Sign In / Google Sign In 主流视觉规范对齐。资产引用通过既有 `/api/auth/providers` 返回的 `icon` 字段（`f"/static/icons/{p.name}.svg"`，无需改路由）。**严格不动**：6 个 OAuth 路由 / `OAuthProvider` Protocol / Google + GitHub provider 实现 / OAuth callback 5 路径分支 / 邀请码门 / 同邮箱合并策略 / Schema / `oauth_accounts` 表 / token 加密 / Settings LoginMethodsSection / `<LLMSwitcher>` / 后端任何代码。**测试**：vitest 已有 oauth-buttons.test.tsx 加 2 case（DOM 顺序断言：邮箱密码 form 在 oauth 按钮之前 / divider 文案 "或使用"），login.html 内嵌 JS 改动属模板层不需新单测。**约 +30min 实装 ~30 LOC**：login.html template HTML 顺序对调 ~15 / 2 个 SVG 文件 (google ~6 行 path / github ~4 行 path) ~10 / button inline style 升级 ~5。**用户 2026-05-11 决策**：保持 OAuth v1.0 整体方案不变（Google + GitHub 双 provider / 邀请码门保留 / 同邮箱合并策略）只调 login UX 两处（位置 + logo）；本期与 OAuth v1.0 主体 Code instruction 在不同 commit 推送（v1.0 主体已在 5h 实施估算内）。 | — |
| 2026-05-11 | [mobile-ui-v1.3.1.md](mobile-ui-v1.3.1.md) | v1.3.1 — fixup #2 (视觉验收) | **5 项移动端视觉级验收回退闭环**（用户 2026-05-11 部署后实测；纯前端、不改后端 / API / DB / 任务 / LLM）：(1) **首页账户 hero 数字截断** — `<AccountOverviewCard>` 原 `flex items-start justify-between gap-3` + 双 child `min-w-0 truncate` 在 390px 下让 `$XXX,XXX.XX` 与 `+$X,XXX · ±X.XX%` 互相挤占，触发 ellipsis。修改 [`DashboardPage.tsx`](../../stock_trading_system/web/frontend/src/islands/dashboard/DashboardPage.tsx#L524) 改为 `flex flex-wrap items-baseline gap-x-3 gap-y-1`，左侧 `flex-shrink-0` 保账户总值始终完整，右侧 `sm:ml-auto sm:text-right` 桌面同行 / 移动自然换行；删 `truncate` + 加 `whitespace-nowrap` + `data-account-value` / `data-account-today-pnl` 锚点供测试断言不被 `text-overflow:ellipsis` 切。(2) **顶栏模型切换视觉** — [`<MobileTopbar>`](../../stock_trading_system/web/frontend/src/components/shared/MobileTopbar.tsx) 之前嵌的是 ghost 文本 trigger `模型: Gemini` 不贴 demo `.provider` 蓝色 pill。给 [`<LLMSwitcher>`](../../stock_trading_system/web/frontend/src/components/shared/LLMSwitcher.tsx) 加 `variant?: "full" \| "pill"` prop（默认 `full` 保桌面行为完全不变），`pill` 渲染圆形蓝点 + 蓝字 + `border-radius: 9999px` + `bg-[color-mix(blue 10%, transparent)]` 三态 hover/active 与 demo 像素对齐；MobileTopbar 改用 `<LLMSwitcher variant="pill" />`，DropdownMenu 展开后的 Provider/Preset radio 内容两 variant 共用零分叉。`data-llm-pill=""` 锚点。(3) **底部 tab active 不贴 demo** — 之前 active 仅 `text-primary` 文字变蓝。Sidebar.tsx `<TabItem>` + `<MobileTabbar>` More 按钮统一加：(a) 顶部 22×3px 蓝色 accent bar（`<span aria-hidden h-[3px] w-[22px] rounded-b-full>`，inactive 透明 / active `bg-[var(--color-accent-blue)]`）+ (b) 整个 tab 容器 active 时加 `bg-[color-mix(blue 10%, transparent)]` + `rounded-lg` 与 demo `.tabbar button.active` 同等视觉权重；inactive 仍 `text-muted-foreground` 不抢眼。`data-active="true"` + `data-tab-accent="active"` 锚点。(4) **首页内容区重复 `<h1>首页</h1>`** — 移动端 MobileTopbar 已展示 `pageTitle="首页 · 资产与持仓"`，DashboardPage 内容首行又渲染 `<h1>首页</h1>`，移动端首屏被双标题挤掉账户卡。改为 `<h1 className="hidden md:block text-xl font-bold">首页</h1>`——桌面无 MobileTopbar，h1 保留作页面 anchor；移动端 `display: none` 让账户 hero 直接进入首屏。(5) **补 v1.3.1 单测** — 项目已在 v1.2 滚回 vitest 基础设施（package.json 无 `vitest` devDep + 无 `__tests__/`），两条路径补：(5a) 新建 [`scripts/test/check-v131-components.mjs`](../../stock_trading_system/web/frontend/scripts/test/check-v131-components.mjs) 13 项源级 contract guard（Sparkline `length<2 return null` + svg `data-sparkline` 标记 / MobileTopbar `pageTitle &&` 守卫 + `<LLMSwitcher variant="pill" />` 调用 / LLMSwitcher pill `data-llm-pill` + `rounded-full` / AccountOverviewCard `length>=5 && <Sparkline>` + 4 个 `data-account-*` 锚点 + 不允许在 hero 数字上加 `truncate` / DashboardPage `<h1 className="hidden md:block">首页` 守卫），接入 `npm run lint:ux` 链路 → `npm run build` 失败而非 ship 回退；(5b) 新建 [`e2e/specs/v131-visual.spec.ts`](../../stock_trading_system/web/frontend/e2e/specs/v131-visual.spec.ts) 5 个 Playwright runtime case @ 390px 覆盖：hero 数字 `scrollWidth-clientWidth<=1` 防 ellipsis / MobileTopbar 顶栏 + pageTitle subtitle + 蓝色 pill border-radius 非 0 / 底 tab `[data-mobile-tab="/"][data-active="true"]` 且 accent bar `data-tab-accent="active"` + 其他 tab 不 active / 移动端 `<main>` 内 `<h1>首页</h1>` 计算样式 `display: none` / sparkline 条件契约（≥5 历史点出 svg / <5 不渲染不报错）。Test 用 `page.route()` stub `/api/settings/llm-provider` 返 fixture 让 LLMSwitcher 离开 `if (!state) return null` 早返；用 `addInitScript` `history.replaceState(null, "", "/")` 让 `isActive("/")` 在 dev harness 路径下也成立。**严格不动** v1.3.1 R-MUI-19/20/21 已落地能力 / R-MUI-01..18 / 后端 / API / DB / 任务系统 / LLM / TradingAgents / shadcn primitives / Schwab OAuth / portfolio API / paper trade engine / Analysis rendering 8 Card 契约。**验收**：`npm run build` 3.70s 全绿（含新 contract guard）；`npx playwright test e2e/specs/scroll-width.spec.ts e2e/specs/v131-visual.spec.ts` → **53 passed (48 scroll-width + 5 v131-visual)**；`npm run lint:ux` 双 guard 通过（no-quick-mode + v1.3.1 contracts）。约 +2h 实装 ~280 LOC（DashboardPage hero 改 ~25 / LLMSwitcher pill ~40 / MobileTopbar 复 ~10 / Sidebar TabItem + MoreButton ~50 / Playwright spec 145 / Node guard 130 / 调试 stub ~10）。**根因反思**：v1.3.1 PRD 把视觉验收断言留给"用户实测对比 demo"，没把 hero 不截断、tab active 视觉、duplicate h1 等具体可机验项写进 §9 验收清单——下次 v1.x 发版前，凡是 demo 与实装并存的视觉级特征都应在用例里有 Playwright 锚点（≥1 case per 视觉特征）。 | — |
| 2026-05-11 | [mobile-ui-v1.3.1.md](mobile-ui-v1.3.1.md) | v1.3.1 — addendum | **AI 分析 / 选股页 layout 顺序对调施工图**（用户 2026-05-11 二次反馈追加）：实测 [`AnalysisPage.tsx:486+`](../../stock_trading_system/web/frontend/src/islands/analysis/AnalysisPage.tsx) `<AnalysisHomeInbox>` JSX 渲染顺序是 `分析记录 Inbox`（含 `<RunningRow>` + `<CompletedRow>`）在上 + `发起分析` 表单在下；[`ScreenerV3Page.tsx:69-73`](../../stock_trading_system/web/frontend/src/islands/screener-v3/ScreenerV3Page.tsx) `<ScreenerHomeView>` 是 `<RecentScreensCard>` 在上 + `<ScreenerForm prefillTaskId={prefillId} />` 在下——两处与"主要功能在上、历史记录在下"心智完全相反，移动端首屏被一长串历史挡住核心写动作。**修法**（仅 JSX 顺序对调，0 内部行为改动）：(A) Analysis 页在 `<AnalysisHomeInbox>` 内把 2 个 `<Card>` 顺序对调——`<Card>发起分析</Card>` 放第一个 + `<Card>分析记录 Inbox</Card>` 放第二个；section title 顺序同步对调（`发起分析` title 在前、`分析记录` title 在后）。(B) Screener-v3 页 `ScreenerHomeView` JSX return 里把 `<ScreenerForm>` 与 `<RecentScreensCard />` 顺序对调——表单在前、最近选股在后。**严格不动**：`<RunningRow>` / `<CompletedRow>` 内部渲染（运行中行的 PipelineDAG / 完成行的展开摘要 / 操作按钮 / 多选对比 / 删除收藏全部保留）/ `<RecentScreenCard>` 内部渲染（卡片视觉 / 点击跳 `?result=<task_id>`）/ 表单字段 / 提交逻辑 / prefill 流程（`?prefill=<task_id>` banner 仍在 form 内位置随之上移）/ 乐观插入运行中行行为（提交后仍插 Inbox 顶部 → 用户能看到自己的提交进入运行中态）/ Tasks 列表（不在本期范围）。**桌面端同序**：≥md 也跟着改顺序，避免桌面/移动两套布局认知负担。**测试**：vitest 4 case（`AnalysisPage.layout.test.tsx` 2 case：DOM 顺序断言 `getByText("发起分析")` 在 `getByText("分析记录")` 前 + 提交分析后乐观插入仍在 Inbox 顶部行为不变；`ScreenerV3Page.layout.test.tsx` 2 case：DOM 顺序 ScreenerForm 在 RecentScreensCard 前 + prefill 模式 banner 仍在 form 内）。**约 +1h 实装 ~55 LOC**（Analysis JSX 对调 ~10 / Screener JSX 对调 ~5 / 4 个单测 ~40），纯前端，0 后端改动，0 数据契约改动。**用户 2026-05-11 决策**：layout 顺序属于 v1.3.1 本期一并落地（不另起 v1.3.2），与 R-MUI-19/20/21 同 Code instruction 推送。 | — |
| 2026-05-10 | [mobile-ui-v1.3.1.md](mobile-ui-v1.3.1.md) | v1.3.1 | **移动端 v1.3 视觉级增量补丁施工图**（关闭 v1.3 §9.1 / §9.2 验收漏洞）：用户 2026-05-10 实测对比 demo 与实装 3 处视觉级落差——(1) 移动端 `<AppShell>` 仅 `<Sidebar>(hidden md:flex)` + `<MobileTabbar>` + `<main>`，**没有移动端顶栏** → LLMSwitcher 在移动端完全不可见，违反 v1.3 §9.1 "现有模型切换两段式 UI 可用"；(2) `<AccountOverviewCard>` 缺 hero 内嵌 mini sparkline，净值曲线被推到下方 `lg:grid-cols-3` 桌面 grid，375px 移动端首屏看不到趋势；(3) 账户卡用普通 `<Card>` 容器与持仓卡视觉权重相同，无 hero 区分。**修法**：(A) 新建 [`components/shared/MobileTopbar.tsx`](../../stock_trading_system/web/frontend/src/components/shared/MobileTopbar.tsx) sticky `md:hidden` 顶栏（左侧品牌 `⚡ StockAI Terminal` + `pageTitle` 副标 truncate 200px + 右侧复用现有 [`<LLMSwitcher>`](../../stock_trading_system/web/frontend/src/components/shared/LLMSwitcher.tsx) 两段式不动；`bg-background/95 backdrop-blur z-40 border-b`，sticky 不需 `<main>` 加 padding-top）；(B) [`<AppShell>`](../../stock_trading_system/web/frontend/src/components/shared/AppShell.tsx) 加 `pageTitle?: string` prop + 把 `<MobileTopbar pageTitle={pageTitle}>` 插在 `<main>` 上方；11 个 island main entry 显式传 `pageTitle`（dashboard=`首页 · 资产与持仓` / analysis=`分析 · Inbox 与命令` / screener-v3=`发现 · 智能选股 V3` / paper-trade-list=`纸面交易` / portfolio=`持仓 · 管理` / transactions=`持仓 · 交易记录` / reports=`报告中心` / backtest=`策略回测` / alerts=`预警中心` / tasks=`任务中心` / settings=`系统设置`，与 demo `viewNames` 完全对齐）；(C) 新建 [`components/shared/Sparkline.tsx`](../../stock_trading_system/web/frontend/src/components/shared/Sparkline.tsx) 纯 SVG 实现 ~50 LOC（不引入新图表库；`viewBox + preserveAspectRatio="none"` 自适应宽度；fill+stroke 双 path 让 sparkline 有面积感；`positive` prop 控制色码与 PnL 方向对齐；values < 2 返回 null）；(D) [`<AccountOverviewCard>`](../../stock_trading_system/web/frontend/src/islands/dashboard/DashboardPage.tsx#L494) 升级为 hero：(D1) Card className 加 `bg-card/95 ring-1 ring-primary/10 shadow-sm`（视觉差异化，与下方持仓卡分层）；(D2) CardContent 现有 2 段（账户总值+PnL / 三栏 metric）中间插 sparkline 第 2 段（`<Sparkline values={sparklineValues} height={44} positive={pnl.total_pnl >= 0} />`，`-mx-2` 让 sparkline 充满卡内宽度）；(D3) Props 加 `sparklineValues: number[]` 由 `<DashboardPage>` 从已有 `data.history.map(h => h.total_value)` 派生（不需新接口，复用现有 `/api/portfolio` 数据）；(D4) **数据不足降级**（R-MUI-20a）`sparklineValues.length >= 5` 才渲染 sparkline，<5 时账户卡退化为现状两段不破布局（阈值 5 是经验值，少于 5 个点的 sparkline 信息密度太低）。**严格不动**：[`<LLMSwitcher>`](../../stock_trading_system/web/frontend/src/components/shared/LLMSwitcher.tsx) 实现（仅消费方 +1）/ 桌面 `<Sidebar>` 视觉 / 桌面 `lg:grid-cols-3` 净值曲线独立 Card（保留作详细视图含 6 range chips + 重新计算 + 仓位分布）/ 后端 API/数据 schema/task type / v1.3 已落地 R-MUI-01..18 所有改动 / shadcn primitives / TradingAgents / 数据层 / LLM / business logic。**测试**：vitest 3 文件 12 case（MobileTopbar 4 case 含 LLMSwitcher 子组件挂载断言 / Sparkline 5 case 含 values<2 返 null + positive 色码 + 自动方向推断 / AccountOverviewCard 3 case 扩展现有含 sparkline 渲染条件 + hero className 断言）+ Playwright 12 页 × 4 viewport 回归保留（v1.3 已通过 48 case 不退化）+ 额外 3 case（移动端任意页面顶栏可见品牌 + 点 LLMSwitcher chip 弹菜单 + 375px 首屏可见 sparkline DOM 测试）+ 手动 5 机型回归（iPhone SE2/14/14 Pro Max / Pixel 7 / iPad）。**约 4h 实装 ~245 LOC** 纯前端：Sparkline 80 / MobileTopbar 80 / AppShell 集成 15 / 11 island main entry 30 / AccountOverviewCard 升级 40。**根因反思**：v1.3 PRD §3.1 仅描述功能性能力，未约束视觉层级和容器 hero 化，也未显式要求"保留顶部品牌栏 + 移动端 LLMSwitcher 入口"——下次写 PRD 时把 demo 顶栏 / hero 容器 / 视觉层级明确进 In Scope 防漏。**用户 2026-05-10 决策**：补 R-MUI-19/20/21 三条视觉级，副标用 prop 传递（不引 context，简单可追踪），数据不足时不画 sparkline 而非空状态占位。 | — |
| 2026-05-09 | [oauth-quick-signin.md](oauth-quick-signin.md) | v1.0 | **第三方快捷登录（Google + GitHub）施工图**：[`auth/`](../../stock_trading_system/auth/) 模块新增 4 文件（`oauth_repository.py` / `oauth_providers/{__init__.py,google.py,github.py}` / `oauth_crypto.py` / `oauth_session.py`）+ [`web/app.py`](../../stock_trading_system/web/app.py) 加 6 路由（`/auth/oauth/<p>/start`/`/callback` + `/api/auth/oauth/{register,linked,<p>/unlink,providers}`）+ migration `add_oauth_accounts.py`（idempotent 新表，`(provider,provider_user_id) UNIQUE`）。**复用 [Schwab OAuth 现有 pattern](../../stock_trading_system/web/app.py#L916)**（state + token 持久化 + 回调），前缀错开 `/oauth/schwab/...` vs `/auth/oauth/...`。**核心抽象 `OAuthProvider` Protocol**：`build_authorize_url(state,code_challenge,redirect_uri)` + `exchange_code(code,verifier,redirect_uri) -> (OAuthProfile, OAuthTokens)`，Google 走 OIDC discovery（`accounts.google.com/.well-known/openid-configuration`，scope `openid email profile`，`email_verified` 直读 ID Token claim），GitHub 走纯 OAuth 2.0（scope `read:user user:email`，邮箱必经 `/user/emails` 找 primary+verified）。**Callback 5 路径分支决定一切**：(1) state+PKCE 校验 (2) intent=link 已登录用户主动绑定 (3) 老 OAuth 用户直接登录 (4) 邮箱已存在+`email_verified=true` 自动合并（仅 Google）(5) 邮箱已存在+未验证 → 二次确认引导（GitHub） (6) 全新邮箱 → `_make_pending_token(itsdangerous,10min 过期)` 后跳 `/register?provider=<p>&pending=<token>` 强制邀请码门。**邀请码门 0 绕过路径**：所有新 user 创建必经 `_invite_mgr.validate()+redeem()`，OAuth 不是绕过多租户的后门。**同邮箱合并策略**：`email_verified=true` 才合并（Google 永远是 true，GitHub primary 邮箱 verified=true 才算），audit log 记 auto-link 事件。**Schema 不动 users 表**：OAuth-only 用户走 `repo.create(email, password=secrets.token_urlsafe(32), ...)` 占位 `password_hash`，未来如需真"OAuth-only 标志"加 users 布尔列（v1.0 不做）。**Token 加密**：[`auth/oauth_crypto.py`](../../stock_trading_system/auth/oauth_crypto.py) fernet `encrypt_token/decrypt_token`，`OAUTH_ENCRYPT_KEY` env 启动时 fail-fast 校验在场，v1.0 access_token 不消费仅留口子。**安全**：state 32B + PKCE S256（`_pkce_challenge=base64url(sha256(verifier))`）+ `_safe_next` 同域相对路径白名单 + `(provider,provider_user_id) UNIQUE` 防多次绑定 + session 登录后 `regenerate` 防固定 + redirect_uri 服务端二次校验。**前端 3 处改造**：(A) [`templates/login.html`](../../stock_trading_system/web/templates/login.html) JS 读 `/api/auth/providers` 动态渲染 OAuth 按钮区，邮箱密码 form 不动；(B) [`templates/register.html`](../../stock_trading_system/web/templates/register.html) 接 `?provider=&pending=&email=&name=` 预填邮箱 readonly + 强制邀请码 + 隐藏 password 字段（OAuth 模式 POST `/api/auth/oauth/register`，普通模式 POST `/api/auth/register`）；(C) [`SettingsPage.tsx`](../../stock_trading_system/web/frontend/src/islands/settings/SettingsPage.tsx) 新增 `<LoginMethodsSection>`（已绑列表 + 解绑按钮 + 关联新 provider 跳 `?intent=link&next=/settings`，解绑前置检查至少保留一种登录方式）。**`/api/diagnostics/providers` 加 OAuth 项**（client_id 在场 / redirect_uri 配置 / 最近成功登录时间）。**4 新 env**：`GOOGLE_OAUTH_CLIENT_ID`/`SECRET`+`GITHUB_OAUTH_CLIENT_ID`/`SECRET`+`OAUTH_ENCRYPT_KEY`（fernet generate_key 生成）。**新增依赖**：`authlib>=1.3`（PyJWT 已被 GitHub flow 需要可选，cryptography 已通过 schwab-py 间接引入）。**严格不动**：Schwab OAuth 路由 / `users` schema / 邀请码逻辑 / `login_required` decorator / 现有 3 个 auth 路由 / `verify_password`+`hash_password` / session 结构 / shadcn primitives / 业务逻辑。**测试**：后端 6 文件 ~50 单测（oauth_repository 6 / providers 8 / callback 12 含 5 路径分支+state mismatch+多租户隔离 / register 6 / unlink 4 / crypto 3）+ 前端 2 vitest（oauth-buttons 渲染条件 + LoginMethodsSection 解绑/关联）+ 手动 E2E 7 项（全新邮箱→Google→邀请码→注册 / 已存邮箱→Google→自动合并 / 已存邮箱→GitHub primary verified=false→二次确认 / 已登录用户 settings 关联→解绑 / 跨用户绑定别人 OAuth→error / 大陆 Google 不可达自动隐藏 P1 / Schwab OAuth 路由不受影响）。**Out of Scope（v1.1+ 排期）**：Apple Sign In（需 $99/年 dev account）/ 微信扫码（需企业认证 80 元/年）/ Microsoft/QQ/Twitter（用户覆盖低）/ 跳过邀请码（多租户红线）/ 短信登录 / users 表改 schema / OAuth token 主动消费第三方 API。**用户 2026-05-09 决策**：v1.0 仅 Google+GitHub 双 provider 免费组合 / 保留邀请码门 / 同邮箱已验证自动合并 + 未验证二次确认双策略 / Authlib 库 / demo `data-view="auth"` 同步加 OAuth 按钮。 | — |
| 2026-05-10 | [mobile-ui-v1.3.md](mobile-ui-v1.3.md) | v1.3 — fixup #1 | **首页账户总览压缩为单卡** — Railway 部署后用户实测 390px：原实装把 4 个 `Stat` 卡放在 `grid-cols-2 md:grid-cols-4 grid-collapse-mobile`，`grid-collapse-mobile` 在 ≤575.98px 强制 `grid-template-columns: 1fr`，导致 4 张账户卡每张占满整行，竖向占据近一屏，与 `demo_mobile_full_v1.html` 中"账户总值 + 今日 PnL（同行）→ 总盈亏 / 收益率 / 活跃预警（三栏）"的单卡形态不符。修复：[DashboardPage.tsx](../../stock_trading_system/web/frontend/src/islands/dashboard/DashboardPage.tsx) 新增局部组件 `AccountOverviewCard` + `Metric` —（1）顶部 flex 行：左 `账户总值` 大号 mono；右 `今日 PnL`（来源 `/api/portfolio/summary` 的 `today_pnl/today_pnl_pct`，无前日 snapshot 时降级为 `—` 不显示伪 0）；（2）下方 3 列 grid：`总盈亏 / 收益率 / 活跃预警`，沿用现有色 token；删除原 4-Stat 行 + `Stat` / `Wallet` 未用 import。`reloadHoldings` + 初始 `Promise.all` 增 `apiGet<PortfolioSummary>("/api/portfolio/summary")` 拉取 today PnL。**严格不动** 后端 API、后续 `净值与仓位` 大图（保留作详细视图）、HoldingsSection 整列、运行任务卡 / 快捷操作。验收：`npm run build` 3.67s ✓；Playwright `dashboard @ {375,390,430,768}` 4 / 4 ✓ 无横向溢出；Railway redeploy `7d491a79-63a1-4457-96b3-e0108c50828d`。 | — |
| 2026-05-10 | [mobile-ui-v1.3.md](mobile-ui-v1.3.md) | v1.3 — 实装 | **移动端 IA/UI v1.3 实装落地**（按 PRD/Design/TestCase v1.3，严格以 `demo_mobile_full_v1.html` 为契约；不改后端 / API / 任务系统 / DB / LLM 业务逻辑）。**Phase 1 — 导航与首页**：`Sidebar.tsx` MobileTabbar 5 tab 改为「首页 / 分析 / 发现 / 纸面 / 更多」，`MOBILE_PRIMARY` 用 `data-mobile-tab` 属性给 Playwright 选择器锚点；More sheet 标题改"所有页面 · 功能地图"，条目精简为 报告中心/策略回测/交易记录/预警中心/任务中心/系统设置/账号；移除「调度器」「复盘与运营」「纸面交易（已成一级）」等历史入口；`系统设置` 副标题= "模型与通知"；`MOBILE_MORE` 加 `description` 字段后改用单列布局，44px 触控区合规；More 路由 active 状态从 URL 反推（[components/shared/Sidebar.tsx](../../stock_trading_system/web/frontend/src/components/shared/Sidebar.tsx)）。**Phase 2 — 首页合并持仓**：新增 [components/shared/HoldingDialogs.tsx](../../stock_trading_system/web/frontend/src/components/shared/HoldingDialogs.tsx) 抽出 `BuyDialog/SellDialog/UpdateCostDialog`（PortfolioPage 旧内联版同步切到 import，删 ~150 行重复）；新增 [islands/dashboard/HoldingsSection.tsx](../../stock_trading_system/web/frontend/src/islands/dashboard/HoldingsSection.tsx) 渲染 持仓明细 区块：搜索 + 买入 + Chip(`显示 5` / `全部 N` / `交易记录 X` / `纸面计划`) + 批量分析持仓产品缺口卡（`alert()` 占位 + 任务中心入口，禁止伪装实装） + 持仓卡（ticker / shares / market / 成本 / 现价 / 盈亏 / 市值 / 仓位 + `看分析` → `/analysis?ticker=` 复用现有入口、卖出 / 修正成本 / 移除）；DashboardPage h1 `仪表盘` → `首页`，停止渲染独立"当前持仓"区块，运行任务卡和快捷操作保留为 2 列；增 `transactionsCount` 状态 + `/api/portfolio/transactions` count 拉取 + `reloadHoldings()` 在买/卖/改成本后并刷 dashboard + allocation + transactions（不新增端点）。**Phase 3 — Analysis Detail 收敛**：`REPORT_TABS` 标签收紧到 概览/市场/情绪/新闻/基本面/辩论/风险/决策（严格 8 个）；TabsList 加 `data-analysis-structured-tabs=""` 选择器；删除「Quick Info（最近新闻 / 基本面指标 / 多空辩论）」3 卡 + 顶部 3 卡 Stats 行 + 顶部右侧操作簇；结构化分析核心 Card 上移成首屏首个业务区，Card header 内嵌 `导出 MD / 导出 PDF / 分享链接 / 收藏`；K 线 Card 移到结构化 Card 之后；新增 `记录与操作` Card（日期 / 风险 / 深度 + 元信息 + 再次分析 / 加入观察 / 纸面交易）；新增 `RawReportFallback` Card 含 chips（Markdown / 市场原文 / 新闻原文 / 风险原文）+ 内联 `MarkdownBody` 渲染原始内容；删除"Inbox 工具"区的刷新按钮（websocket 已自动拉新）；删除现已不用的 `QuickInfoCard / KV / fundNum / fmtNum / fmtPct / NewsItem`、`Newspaper / BarChart3 / Scale` icon imports；状态 `news / fund / scrollToTab` 同步移除。（[islands/analysis/AnalysisPage.tsx](../../stock_trading_system/web/frontend/src/islands/analysis/AnalysisPage.tsx)）**Phase 4 — Screener V3 收敛**：表单底部移除 `取消` 按钮（保留主 `开始筛选`）；选股结果页删除"v1.4 透明度链路 banner"（用户输入 → 解析约束 → 候选来源 → off-theme 过滤）+ "v1.2 run-mode banner"（dynamic_llm / cache_hit_pct / llm_calls / duration）；候选移动端卡新增 `GuruScoreDetails` 组件用 `<details>` 包裹 `CandidateExpanded`，summary 文案 = `查看 14 位大师评分（共识 X% / 看多 N / 看空 N / 中性 N）`，consensus % = `Math.max(bullish, bearish, neutral) / total`；默认 closed，展开后展示 14 位大师 4-card 网格 + 基本面 + 操作；桌面 table 行内 chevron 展开保留作信息密度高的桌面替代路径。（[islands/screener-v3/ScreenerV3Page.tsx](../../stock_trading_system/web/frontend/src/islands/screener-v3/ScreenerV3Page.tsx)）**Phase 5 — Paper Trade Detail 收敛**：删除顶部 `策略 / 日度数据` 内层 ChipRow 与 `mainTab` 状态，所有内容栈式渲染；返回纸面交易按钮上提；`AI 最终决策（raw 英文）` 卡替换为 `AI 决策核心 / 执行记录` 卡 + 新组件 `StructuredDecisionPanel`（score-ring + 交易决策 Badge + 推理摘要 + 置信度 / 风险等级 / 执行方式 KV + evidence 列表，源自 `latest_advice` 解析数据，不再渲染英文原文 `FINAL TRANSACTION PROPOSAL`）；`Plan / Event` chip 双视图保留在执行记录区；`日度数据` 区只剩一个 `刷新日度数据` 按钮（既有 DailyDataTab 内 EOD 按钮无需改动）；删除 `BarChart3` 未用 import。（[islands/paper-trade/PaperTradePage.tsx](../../stock_trading_system/web/frontend/src/islands/paper-trade/PaperTradePage.tsx)）**Phase 6 — Playwright 回归**：[e2e/specs/scroll-width.spec.ts](../../stock_trading_system/web/frontend/e2e/specs/scroll-width.spec.ts) 视口由 320/375/414 改为 PRD §9.3 的 375/390/430/768，截图目录改 `docs/qa/mobile-ui-v1.3`；webServer URL + PAGES URL 加上 `/static/dist/` 前缀（适配 vite.config.ts `base="/static/dist/"`）；运行 `npx playwright test` → **48 passed (12 页面 × 4 视口) — 0 横向溢出**。**`npm run build`** 3.81s 通过；type check `npx tsc -b` exit=0。**严格不改**：Flask 路由 / Schwab OAuth / portfolio API / paper trade 后端 / SQLite schema / migrations / TaskManager / LLM provider 内部行为 / Analysis rendering 8 Card 契约 / Screener pipeline / shadcn primitives 接口；移动端不暴露独立 `/portfolio` 一级入口但生产路由保留。**已知未实装（保留为产品缺口，未伪装）**：`batch_analysis` 触发入口 / Reports & Backtest 历史列表 / Settings Diagnostics & Schwab OAuth 面板 / Analysis Inbox scope 三态 / Alerts → Analysis 反向跳转 / `/ticker/<symbol>` 聚合页（明确未新增）。 | — |
| 2026-05-09 | [mobile-ui-v1.3.md](mobile-ui-v1.3.md) | v1.3 | 移动端信息架构与 UI 实装施工图：严格按 `demo_mobile_full_v1.html`，不改产品功能/后端/架构；定义 AppShell、Dashboard、Analysis、Screener V3、Paper Trade、More 的具体改造点、共享组件建议、状态来源、回归策略和执行禁令 | — |
| 2026-05-07 | [paper-trade.md](paper-trade.md) | v1.5.2 — strategy_event analysis_id 修正 | **纸面交易 strategy_event.analysis_id 字段误用导致 ticker 串数据**（用户 2026-05-07 生产实测：`/api/paper/tickers/AAPL` 显示 SMR 的 trade_decision；本地 DB dry-run 9 条污染事件，~2h）。根因：`order_engine._execute_order:288` 和 invalid_stop guard:85 都把 `order.get("plan_id")` 当作 `analysis_id` 参数传给 `insert_strategy_event` — 但 `paper_trade_strategy_events.analysis_id` 是 `analysis_history.id` 的外键引用；plan_id 和 analysis_id 都是 INTEGER，撞到另一个 ticker 的分析行就串：本地 DB 实测 plan #276 (AAPL session 41) 写成 analysis_id=276，应该是 analysis_id=9 (AAPL)；plan #283 写成 283 实际应是 30（这就是用户截图的 AAPL→SMR）。`/api/paper/tickers/<ticker>` 把这个错误的 analysis_id 喂给 `get_analysis_by_id`，返回的 ticker 与页面 ticker 不符的分析数据，UI 拿 `latest_trade_decision` 字段直接渲染就是另一个 ticker 的策略原文。修复 v1.5.2：(A) `order_engine._resolve_analysis_id(store, order)` helper：从 `order["plan_id"]` 通过 `store.get_plan(plan_id)` 反查 `plan["analysis_id"]`，缺失/异常时返 0（哨兵值，不会撞 analysis_history.id ≥ 1 的真实行）；两处 `insert_strategy_event` 调用点 `analysis_id=order.get("plan_id") or 0` 替换为 `analysis_id=_resolve_analysis_id(store, order)`；reasoning 字段保留 `plan_id=N` 以便 audit；(B) `/api/paper/tickers/<ticker>` 增加 ticker 一致性 guard — `latest_trade_decision` 路径在 `get_analysis_by_id(analysis_id)` 后比对 `ana.ticker.upper() == page_ticker.upper()`，不匹配时 `ana = None` + `latest_trade_decision = ""` + WARN 日志；`latest_advice` 仅当 ana 通过 ticker check 才进入；plan_history 同样校验每个 plan.analysis_id 关联的 analysis 行，ticker 不符就清空该行 trade_decision；(C) 历史数据迁移 `migrations/fix_strategy_event_analysis_id.py`：`find_polluted_events(conn)` SQL 用 `JOIN paper_trade_plans ON e.analysis_id = p.id WHERE p.analysis_id IS NOT NULL AND p.analysis_id != e.analysis_id` 精确识别污染行；`apply_backfill(db_path, dry_run=False)` 用 `p.analysis_id` 回填 `e.analysis_id`；幂等（修过的行下次不再匹配 JOIN 条件）；支持 `--dry-run` 不落盘。新增测试：`tests/strategy/paper_trader/test_strategy_event_analysis_id.py` 10 case + `tests/web/test_paper_ticker_detail_ticker_guard.py` 2 case。验收：`pytest tests/strategy/paper_trader tests/web/test_paper_track.py tests/web/test_paper_eod_endpoint.py tests/web/test_paper_ticker_detail_privacy.py tests/web/test_paper_ticker_detail_ticker_guard.py tests/tasks/test_workers.py -q` → **88 passed**；`npm run build` ✓。**生产 DB dry-run** 显示 9 条历史污染事件待 backfill — `python -m stock_trading_system.migrations.fix_strategy_event_analysis_id [DB_PATH]` 需手动在 prod 跑（不替用户 apply）。**约束**：不改 schema、不改 insert_strategy_event signature、不改 get_analysis_by_id、不改 active_plan / events / dailies / trades 字段语义。**双层防御**：order engine 现在写正确值（前向防再污染）+ API 读时 guard ticker 一致性（即使老数据没迁移也不会串 UI） | — |
| 2026-05-07 | [analysis-rendering.md](analysis-rendering.md) | v1.5.1 — vite base 恢复 | **修复结构化摘要 CSS preload 路径错误 + 恢复 v1.5 回归测试**（用户 2026-05-07 验收报告，~30min）。生产 `/analysis/<id>` 概览 tab 重新出现 "结构化摘要暂不可用"。直接验收当前构建产物：HTML 资源路径正确（`/static/dist/assets/card-*.css`）但 Vite 生成的 `preload-helper-ckwbz45p.js` 内容仍是 `E=function(i){return"/"+i}` —— 默认 base 形式，没有 `/static/dist/` 前缀，所以懒加载 `AnalysisCards` 时 `__vitePreload` 拼出 `/assets/card-*.css` 404 → ErrorBoundary fallback。Git log 追溯：2026-05-03 `2396ab1 fix(frontend): set vite base to /static/dist/...` + `31675a7 perf(frontend): split react-vendor + add lazy-load regression tests` 在 `release/2026-05-02-evening` 分支落地，但**从来没合到 main**——4 天里 main 一直跑没 base 配置的旧 vite.config.ts，每次构建产物都坏的。修复：(A) `stock_trading_system/web/frontend/vite.config.ts` 在 `defineConfig({...})` 顶层加 `base: "/static/dist/"`，附长注释指明根因 + 锁条款（注释提到回归测试位置防止再被回滚）；(B) `tests/web/test_vite_asset_base.py` 从 commit `b78ad8b:tests/web/test_vite_asset_base.py` 取回原文 + trim 掉跟 v1.6/v3.0 perf split 强相关的 5 个 case（react-vendor chunk size / socket lazy / ChartPanel lazy —— main 还没合那波 perf 工作，强测会假阳性失败），保留 4 个跟 base 强相关的 regression：`test_preload_helper_chunk_exists` + `test_preload_helper_uses_static_dist_base`（断言 `"/static/dist/"` 在 helper 里 ≥1 次 + 不能含 `return"/"+` 默认形式）+ `test_no_bare_assets_url_in_built_chunks`（扫所有 dist js chunk 不得有裸 `"/assets/` URL）+ `test_modulepreload_helper_does_not_double_prefix`（manifest 路径相对，避免 `vite_helpers.py` 双前缀）；(C) `cd stock_trading_system/web/frontend && npm run build` 重建 dist —— preload-helper hash 从坏的 `ckwbz45p`（含 `return"/"+i`）变回 v1.5 已知正确的 `BzUabfra`（含 `"/static/dist/"`）。验收：`pytest tests/web/test_vite_asset_base.py -v` 4 / 4 ✓；扩展 `pytest tests/web/test_vite_asset_base.py tests/web/test_analysis_depth.py tests/web/test_analysis_detail.py tests/web/test_analysis_sndk_smoke.py -q` → **49 passed**；`grep -c '"/static/dist/"' dist/assets/preload-helper-*.js` = 1；`grep -c 'return"/"+' dist/assets/preload-helper-*.js` = 0；浏览器侧用户验证 `/assets/card-*.css` 404 消失 + 结构化摘要不再 fallback。**严格不动** v1.6 react-vendor manualChunks 扩展 / socket lazy / ChartPanel lazy（那批工作 main 还没合，本期不做范围扩张，留待 v1.6 单独合并任务）；不动 vite_helpers.py / Flask 静态挂载 / `analysis-rendering` 8 Card 数据契约。**根因不在 LLM 结构化数据**，纯是 Vite base 没配 + 修复在分叉里没回主线，被 4 天没人验出来 —— 这个 case 也证明 release 分支单独修复必须有合到 main 的兜底机制（本期手动补 = OK，长期建议 release/main 之间设 PR 自动同步） | — |
| 2026-05-07 | [paper-trade.md](paper-trade.md) | v1.5.1 — 验收 fixup | **纸面交易 v1.5 验收 3 处闭环**（用户 2026-05-07 验收报告，~1.5h）。v1.5 验收 122 passed 但点出 3 阻塞：(1) analyzer 测试 `test_gemini_missing_key_raises` / `test_init_graph_uses_per_user_provider_and_cache_scope` 在缺 `langgraph.prebuilt` 的环境下失败（`tradingagents.graph.trading_graph` import 先于 `_configure_gemini` 的 RuntimeError 触发）；(2) Finding 10 — `workers.py::make_paper_trade_worker` 仍用旧 `PaperTradeSimulator` replay simulator，产品目标 vs 当前实现关系没明文；(3) 用户审 v1.5 代码没看到 `process_analysis` 后自动调 `DailyUpdater.update_session()`（实际在 `event_executor._sync_daily_stats` line 109 已加，但调用点 `task_manager.py:_post_analysis_save` 和 `app.py:api_paper_track_create` 没显式注释，从这两处看不出 EOD 已闭环）。修复 v1.5.1：(A) `StockAnalyzer._init_graph` 在 `from tradingagents.graph.trading_graph import TradingAgentsGraph` 前调 `_assert_provider_credentials(provider)` 提前校验 qwen / gemini / openrouter api_key — 错误信息保持与既有 `_configure_*` raise 完全一致（"llm_provider=qwen but qwen.api_key is empty" / "llm_provider=gemini but gemini.api_key is empty" / "llm_provider=openrouter but openrouter.api_key is empty"），现有 `pytest.raises(..., match=...)` 断言不破坏；缺 langgraph 的环境也能跑过 missing-key 测试；(B) `make_paper_trade_worker` docstring 明确双 mode 边界：**Forward (canonical)** = `event_executor.process_analysis` + `order_engine.evaluate_day` + `DailyUpdater`，TaskManager `_post_analysis_save` / `/api/paper/track` 触发；**Replay (this worker)** = `PaperTradeSimulator` 全局 portfolio 对历史 signal stream 回放，`/api/paper/sessions/<id>/run` POST 触发；产品上是双 mode 设计而非 forward 未完成；(C) `task_manager.py:_post_analysis_save` 调 `process_analysis` 上方加注释指明 DailyUpdater 已在 `event_executor._sync_daily_stats` 内闭环（重复调会双倍迭代 EOD）；同步在 `app.py:api_paper_track_create` 加注释 + 响应追加 `new_daily_rows` 和 `dropped_orders` 计数透出给 UI；(D) 新增 `tests/strategy/paper_trader/test_process_analysis_runs_daily_updater.py` 2 case 证明：① `process_analysis(...)` 之后 `DailyUpdater.update_session(session_id)` 被调用恰好一次，session_id 与返回 `res["session_id"]` 一致 ② DailyUpdater 抛 RuntimeError 时 process_analysis swallow 不破坏主链路，`new_daily_rows=0` 但 plan/order pipeline 正常返回。验收：用户原 14 文件 122 passed + 新 `test_process_analysis_runs_daily_updater` 2 case + `test_analyzer_provider_switch` 6 case → **`pytest ... -q` 147 passed, 0 failed**；`npm run build` ✓ 3.93s。**约束保留**：不改 `_configure_qwen / _configure_gemini / _configure_openrouter` 内部实现（pre-flight 校验是新增 helper，不替换原 raise）；不改 PaperTradeSimulator 任何代码（只加 docstring 标记）；不改 TaskManager / api_paper_track 业务逻辑（只加注释 + response 透出字段）。**Finding 10 状态澄清**：当前是 forward/replay 双 mode 设计，不是未完成。若未来产品决定统一到 ticker-session 执行器，需另立 v2.0 任务（替换 simulator 内部 + /api/paper/sessions/<id>/run 切到 process_analysis 循环 + 设计 multi-ticker batch 语义）；本期不做 | — |
| 2026-05-07 | [analysis-rendering.md](analysis-rendering.md) v2.2 | — | **后端两档归一化 + analyzer 强制依赖 depth + 测试与前端 guard**（用户验收 P1，~1.5h）。v2.1 只改了前端 Switch，但后端语义还在三档：(1) workers 仅读 params.depth 不读 deep_analysis（新 canonical 字段被忽略），(2) database._normalize_depth 把 quick 当合法值（旧前端发 quick 会原样存盘，UI 又没法渲染了），(3) analyzer._iteration_for("standard") 仍 `return cfg.iteration.enabled` —— 同一 UI 按钮在两个部署上行为不同，违反 2-state 契约，(4) 旧 test_analysis_depth.py 还断言 quick 是合法值并访问已删的 _iteration_enabled 属性，1 failed/15 passed。修复：(A) database 新建 `normalize_analysis_depth(params) -> {depth, deep_analysis}` 单一归一化器：deep_analysis=True/False 优先（strict bool gate，字符串 "true" 不算数）→ depth=deep/standard；缺失时 fallback 到 _normalize_depth(params.depth)；旧 quick 经 _LEGACY_DEPTH_ALIASES = {"quick": "standard"} 始终收敛到 standard；VALID_DEPTHS = ("standard", "deep") 不再含 quick。(B) workers.py make_analysis_worker 改调 normalize_analysis_depth(params) 同时取 depth 和 deep_analysis；result 同时输出 depth (canonical) + deep_analysis (boolean alias) 给 paper-trade / 未来消费者。(C) analyzer._iteration_for(depth) 删除 cfg_enabled 分支：deep → True / 其它（standard / quick legacy / 未知）→ False，同一 depth 输入不再因 YAML 而行为飘移。(D) tests/web/test_analysis_depth.py 全量重写 32 case：normalize_analysis_depth 8 case（boolean true/false / legacy quick / 旧 deep / 旧 standard / boolean 优先 / strict-bool gate / 空输入默认）+ _normalize_depth 8 parametrize（quick→standard 三种大小写 / standard / DEEP / 无效 / 数字）+ VALID_DEPTHS 不含 quick + save_analysis 4 case（含 quick→standard 写盘断言）+ DTO 4 case（含 legacy quick 行 read 端归一化）+ TaskStore 2 case（持久化 deep + 收敛 quick）+ analyzer 4 case（standard 强制 off / deep 强制 on / quick legacy off / 未知 off）。(E) 前端：scripts/test/check-no-quick-mode.mjs 静态守卫 — 扫 src/ JSX 文本节点 + JSX 属性值 + label/placeholder/hint/title/aria-label kvp 不得出现 "快速" 字面量（注释行 // * /* 默认豁免）；package.json 增 npm test = node scripts/test/check-no-quick-mode.mjs；连带把 Sidebar.tsx MoreSheet 描述 "快速导航到其他页面" → "导航到其他页面" 消除假阳性（与 depth ladder 无关但词条相同的副作用清理）。**严格不动** OverviewCard / 8 Card 数据契约 / RenderingExtractor / signal/advice 路径 / paper-trade 流 / API endpoint 命名。验收：pytest tests/web/test_analysis_depth.py tests/web/test_analysis_detail.py tests/web/test_analysis_rendering.py tests/web/test_history_inbox.py tests/web/test_rendering_status_dto.py tests/agents/test_rendering_status.py tests/tasks/test_workers.py tests/tasks/test_task_store.py -q → 104 passed；npm run build 3.7s ✓；npm test ✓ "no user-visible 快速 mode literal in src/"。**Caveat**：vitest 单测基础设施在更早分支已被回滚，本轮没重装。如果将来要做组件级渲染测试，需重新引入 vitest + @testing-library/react + jsdom（约 4 个 devDep）；当前的静态 grep 守卫只能拦字面 "快速"，无法验证 Switch 渲染、操作菜单交互等 — 这两类需 Playwright 在浏览器跑。 | — |
| 2026-05-07 | [paper-trade.md](paper-trade.md) | v1.5 | **纸面交易日度 EOD 闭环 + 计划档位语义校验**（用户 2026-05-07 验收报告，~3h；联动 `data/portfolio.db` NVDA/NFLX `last_eod_date` 卡 2026-04-24 实测）：6 处 grep 核实根因 — (a) `event_executor.process_analysis` 只触发 immediate order 不写 daily_stats；(b) `TaskManager._post_analysis_save` 调用 `process_analysis` 后没接 `DailyUpdater.update_session`；(c) `/api/paper/tickers/<ticker>/eod` 缺 login + 缺 user_id，路由会更新别人 session；(d) 前端日度 tab 没刷新按钮 + 没空态提示；(e) `plan_parser._normalize` 仅校验字段格式不校验交易语义 — LLM/regex 给出 BUY 计划 stop=$202 当前价=$190 直接保存，`order_engine.evaluate_day` `price_below` 一旦 `low <= 202` 就触发 → 买入即"高价止损"；(f) `_execute_order` 新开仓 `stop_loss=None, take_profit=None`，风控完全靠 planned_orders；(g) `pct_target_total` 是 0..1 fraction 但 UI `{o.pct_target_total}%` 把 `0.10` 显示成 `0.1%`。修复 v1.5：(1) `event_executor._sync_daily_stats(session_id)` 在 plan 保存 + immediate orders 触发后调 `DailyUpdater(get_config(), store).update_session(sid)` 把 daily_stats 推到最近交易日，返回 `new_daily_rows` 暴露给 res；TaskManager 自动 paper trade + `/api/paper/track` 手动追踪自动收益（process_analysis 内部已闭环）；(2) `plan_parser._normalize(plan, signal, advice, current_price=None)` 加 `current_price` kwarg + 新增 `_resolve_reference_price` 三级优先（current_price > entry_zone 中点 > advice.entry_low/high 中点 > None）+ `_validate_exit_level`：BUY/OVERWEIGHT 的 `exit_stop.price` 必须严格 < ref，`exit_target.price` 必须严格 > ref；非法档位 drop + 写 `plan["dropped_orders"][i]["skip_reason"]`；pattern 触发器（trailing_ma/breakout_retest/time_stop/immediate）不带可比 price 一律 passthrough；`extract_plan` signature 加 `current_price` 透传到 3 条路径（llm/regex/fallback）；`event_executor` 调 `extract_plan(..., current_price=current_price)`；(3) `/api/paper/tickers/<ticker>/eod` 加 `g.user is None → 401` + `find_session_by_ticker(ticker.upper(), user_id=g.user.id)` 严格用户域，response 加 `session_id` 字段；(4) `order_engine.evaluate_day` 新增 long-position invalid_stop 防御 — 即使 plan 验证漏过的 legacy planned_order，evaluate 阶段对 `exit_stop` + `price_below` + `target >= avg_cost` 或 `target >= close` 写 `strategy_event action=skipped + skip_reason=invalid_stop_above_entry` + `mark_order_triggered(trade_id=None)` 不真触发；(5) `_execute_order` 新开仓/加仓时调 `_collect_plan_levels(orders)` 提取 plan 里第一条价格型 exit_stop/exit_target → `_validated_stop / _validated_target` 两层校验（必须严格 < entry AND < fill / 严格 > entry AND > fill）→ 写入 `open_trade.stop_loss / take_profit`；pattern 触发器无 price 字段时返 None 留 NULL；(6) PaperTradePage 日度 tab 加「刷新日度数据」Button → POST `/api/paper/tickers/<ticker>/eod` → 成功后 `refresh()` 重拉详情；空态 `dailies.length === 0 && hasActivePlan` → "尚未跑 EOD — 点击右侧刷新即可补齐到最近交易日"；(7) PaperTradePage 计划档位仓位显示 `((o.pct_target_total ?? 0) * 100).toFixed(0)%`。新增测试：`tests/strategy/paper_trader/test_plan_semantic_validation.py` 13 case + `tests/strategy/paper_trader/test_order_engine_invalid_stop.py` 12 case + `tests/web/test_paper_eod_endpoint.py` 3 case；既有 21 paper_trader + 6 paper_track 不破坏。验收：`pytest tests/strategy/paper_trader tests/web/test_paper_eod_endpoint.py tests/web/test_paper_track.py tests/tasks/test_workers.py tests/tasks/test_task_manager.py tests/tasks/test_event_emitter.py -q` → **105 passed**；`npm run build` ✓。**约束保留**：不改 `paper_trade_plans / planned_orders / trades / daily_stats / strategy_events` schema、不改 `find_session_by_ticker` 参数顺序、不改 `process_analysis` 返回字段（仅追加 `new_daily_rows` + `dropped_orders`）、不改 PipelineDAG / inbox 实时进度链路。**已知遗留**：vitest 基础设施被并行 session 回滚（package.json `test` script + vitest.config.ts + `__tests__` 目录均移除）— 前端的 pct 显示修复仅靠 `npm run build` 类型检查 + 手测覆盖，不在本次提交内重建 vitest | — |
| 2026-05-07 | [llm-openrouter.md](llm-openrouter.md) | v1.0.2 — 验收 fixup #2 | **OpenRouter v1.0.2 — 4 个 v1.0.1 验收遗留闭环**（用户 2026-05-07 反馈，~3h）。v1.0.1 验收 `64 passed / 7 failed` + 两个结构性缺陷。**(P1-#3 contextvar)** v1.0.1 factory wrapper 在内部调 `get_config()` 读全局 OR preset，对测试 / 临时 config / 未来 per-user 都不可靠；fix 引入 module-level `_OR_ROUTING_CTX: ContextVar[dict|None]`；`_configure_openrouter` 改为返回 routing dict；`_init_graph` 调 `TradingAgentsGraph(...)` 前 `_OR_ROUTING_CTX.set(routing)`，try/finally `reset(token)` 防泄露到其他线程；factory wrapper 改为读 ContextVar，未设则 passthrough。**(P1-#2 per-call state safety)** v1.0.1 `self._active_user_id`/`_depth_override`/`_graph` shared mutable，多 worker 并发会串用户；fix `_init_graph(user_id, depth) -> graph` 改为返回 local，`analyze()` 全程使用 local `graph` 不再读 `self._graph`；`_run_with_weights(ticker, date, graph)` + `_build_quick_llm(user_id)` 显式参数化；删 `_iteration_enabled` 属性 → `_iteration_for(depth)` 纯函数 + `_is_iteration_enabled(depth)` 别名。新增 `test_concurrent_analyze_users_dont_share_graph` 启 2 thread 不同 user_id + 50ms graph init sleep 强制交错，断言 local return 不同 + cache `@42`/`@99` 两 entry 反查匹配。**(P1-#1 langgraph + fixture 复用)** v1.0.1 用户 env 7 failed 全是 `tradingagents.graph.trading_graph` import / patch 失败（`langgraph.prebuilt` 缺失）；fix (a) `requirements.txt` 加 `langgraph>=0.2,<2`；(b) `stub_tradingagents_graph` fixture 提到 `tests/agents/conftest.py` 全局共享 + 改为强制替换 `sys.modules`（v1.0.1 仅缺失时注入，被早期真实 import 污染后失效）；(c) `test_analyzer_provider_switch.py` 4 测试改用 fixture；(d) 新增 `test_tradingagents_graph_module_imports` 真实 import 烟雾测试。**(P1-#4 docstring 清理)** `_configure_openrouter` docstring 删 v1.0 的 "intentionally NOT injected" 过期声明，改写真实行为 + ContextVar 协议说明。**最终验收**：用户原命令 `pytest -q tests/llm tests/web/test_settings*openrouter* tests/agents/test_analyzer_openrouter.py tests/agents/test_analyzer_provider_switch.py` → **73 passed, 0 failed**（v1.0.1 是 64+7F）；扩展 `pytest tests/llm tests/agents tests/web tests/screener tests/integration -q` → **620 passed, 1 skipped, 0 failed**；`npm run build` 3.94s green。`test_analysis_depth.py` 同步从 `_iteration_enabled` 属性迁移到 `_iteration_for(depth)` 函数。**严格不动** FilterSpec / TradingAgents 上游包 / 多租户边界 / Qwen/Gemini 直连 / shadcn primitives. 自写 ~340 LOC（含测试）。 | — |
| 2026-05-07 | [analysis-rendering.md](analysis-rendering.md) v2.1 | — | **AI 分析深度入口收敛 + CompletedRow 移动端溢出修复**（用户验收 P1，~1.5h）。问题：用户实测前端依然暴露已废弃的"快速 / 标准 / 深度"三档 radio 卡片（line 232 DEPTH_OPTIONS），同时分析记录的 CompletedRow 把 [chevron + ticker + signal + depth + date + 创建者 + 相对时间 + 收藏 ★ + 打开 + 删除] 全挤一行 flex-wrap，390px 下"打开 / 删除"被推到屏幕外。修复：(A) 删除 DEPTH_OPTIONS 三项 radio + 改 type AnalysisDepth = "standard" | "deep"（去掉 quick）；新增 useState deepAnalysis: boolean 替代 useState depth: AnalysisDepth；UI 替换为单 Switch 控件（"开启深度分析"标题 + 动态副标"标准 ~2min / 深度 ~5min · 启用迭代"）；提交 wire 仍编码 depth = deepAnalysis ? "deep" : "standard" 兼容现有 worker 调度，并附 deep_analysis: boolean 给未来消费者；handleSubmit + 乐观 InboxRow 同步用 wireDepth；AnalysisDetail / InboxRow 字段类型保留 "quick" | "standard" | "deep" 兼容旧 DB 行；depthLabel 把 quick 收敛到"标准"展示；前端代码全文搜索不再有用户可见"快速"模式文案（仅留兼容注释和 RenderingStatusBanner 历史记录提示）。(B) CompletedRow 重构为三层：外容器 min-w-0 overflow-hidden，px-3 py-2 space-y-1；Row A 主标识行 = chevron + ticker(truncate min-w-0) + signal Badge | ml-auto shrink-0 操作簇 [★ icon-button + ExternalLink icon-button + ⋯ DropdownMenu(删除)]；Row B 元数据行 = depth Badge + date + relative time + 创建者(sm+ 才显示)；删除按钮收进 DropdownMenu 不再外露文字；Star icon 替换 ★/☆ 文本；ExternalLink icon 替换"打开"文字；MoreVertical icon 替换"删除"文字。(C) 信号 Badge 改用 signalLabel 三档归一（Buy/Sell/Hold），detail Header + 信号 Stat 卡 + CompletedRow Row A 三处同步；signalLabel helper 在 v1.3 commit 5491aeb 加过、被并行 session 回滚，本次重新加入 export function signalLabel(signal) 并 wire 三处。(D) 新增 imports Switch / DropdownMenu 系列 / MoreVertical / Trash2 / Star icon。**严格不动** 后端 worker depth 字段语义 / paper-trade replay / API 端点 / depthLabel 兼容 quick alias / OverviewCard / 8 Card 数据契约 / RenderingExtractor / signalVariant 颜色映射函数。验收：npm run build 3.84s ✓；frontend 全文 grep "快速" 仅命中 depthLabel 兼容注释，UI 不再渲染；pytest tests/web/test_analysis_depth.py 14 passed（剩 1 失败 test_analyzer_iteration_toggle_quick_vs_deep 是分析器 v1.0.2 ContextVar 重构遗留 _iteration_enabled → _is_iteration_enabled 改名，与本变更无关，git stash 验证 stash 后该测试同样不存在）。**Caveat**：320/375/414 浏览器视觉验收 + 控制台无 horizontal overflow 须用户实测；本仓库 Playwright/vitest 基础设施在 v1.2 之后被回滚，无自动 mobile 截图测试可跑。自写 ~110 LOC（form 区块 -27 +24 / CompletedRow -55 +85 / 类型与 helper +28）。 | — |
| 2026-05-06 | [llm-openrouter.md](llm-openrouter.md) | v1.0.1 — 验收 fixup | **OpenRouter v1.0.1 — 4 个 P1 + 1 个 P2 验收修复**（用户 2026-05-06 反馈，~2.5h）。v1.0 上线后用户系统验收发现 4 个 P1 设计漏洞 + 1 个 P2 文案问题，本轮全部闭环：(P1-A) **AI 分析主链路 OR provider_order 缺失**：v1.0 在 `_configure_openrouter` 设了 `ta_config["llm_deep_kwargs"]={"extra_body":...}` 但上游 TradingAgents `_PASSTHROUGH_KWARGS` 不含 extra_body/default_headers + `_get_provider_kwargs` 对 openrouter 返 `{}`，整个 7-agent 主链路完全没拿到 OR 跨 vendor fallback；fix：(a) 给 `_oc._PASSTHROUGH_KWARGS` 追加 `("extra_body","default_headers")`；(b) wrap `_factory.create_llm_client`，OR 调用按 model id 匹配 active deep/quick preset 注入 `extra_body.provider={order, allow_fallbacks:True}` + `default_headers={HTTP-Referer,X-Title}`，从 `get_config()` 实时读 preset 不缓存（preset swap 立即生效），idempotent via `_stockai_patched` flag。(P1-B) **用户级 provider 与实际执行不一致**：v1.0 worker 用 `resolve_active_model(cfg, user_id)` 落库 provenance 但 `analyzer.analyze()` 不接 user_id —— 用户 A on qwen + 用户 B on OR 共享一个 graph（先到先得），落库可能写"OR"实跑 qwen；fix `analyzer.analyze(user_id=N)` 透传，`_init_graph` + `_build_quick_llm` 都用 `get_active_provider(cfg, user_id=...)`；**cache key 加 `@<user_id|global>` 后缀**（`qwen@global` / `openrouter:deep-pro:quick-flash@42`）；workers.py `analyze()` 调用 ladder 扩展为 4 级兼容 fake analyzer。(P1-C) **OR 配置变更不重置 analyzer**：v1.0 `_reset_config_dependent_singletons` 只识别 `gemini./qwen./polygon./ib./alerts./llm` 前缀，POST 改 `openrouter.api_key` 不会清 analyzer；fix 加 `touched_openrouter` 进入 reset 条件；`/openrouter/active` POST 显式传 `["openrouter.active","llm_provider"]` 让 reset 路径在日志中可见。(P1-D) **测试 patch path 在依赖缺失环境失败**：用户报 `ModuleNotFoundError: langgraph.prebuilt` 触发 `patch("tradingagents.graph.trading_graph.TradingAgentsGraph")` 整个 import chain 失败；fix `stub_tradingagents_graph` fixture 在 `sys.modules` 预注入 stub modules（`langgraph` / `langgraph.prebuilt` / `tradingagents.graph.trading_graph` / `tradingagents.default_config`）每个挂上 analyzer 期望的符号；新增 `test_init_graph_smoke_when_langgraph_unavailable` 显式断言降级依赖环境下 `_init_graph` 仍能跑通。(P2) **Settings UI 文案误导**：v1.0 SettingsPage 预设池只读但文案没说清；fix 标题加 `只读 · v1.0` badge，footer 改为琥珀色 callout 框明确"v1.0 只支持切换 active 指针，新增/编辑/删除请编辑 yaml 重启" + 列出 v1.1 roadmap。**最终验收**：`pytest tests/llm tests/agents tests/web/test_settings*openrouter* tests/web/test_llm_provider_api.py tests/web/test_settings_api.py tests/screener tests/integration -q` → **414 passed + 1 skipped**（OR live-call gated by env）；`npm run build` 3.72s 全绿。5 个独立 commit（P1-A 5dfb1c9 / P1-B ccca97a / P1-C 7c3d563 / P1-D 428e899 / P2）+ 本 fixup changelog；自写 ~520 LOC（含测试 + 文案）。**严格不动** FilterSpec / TradingAgents 上游包 site-packages / 多租户 v1.18 R-fix-12 边界 / Qwen/Gemini 直连 / shadcn primitives. | 5dfb1c9 → fixup |
| 2026-05-06 | [analysis-rendering.md](analysis-rendering.md) v2.0 | — | **结构化摘要从 best-effort 升级为可观测状态机 + backfill + 详情页降级重构**（用户验收不通过反馈，~3h）。用户报告四个 root cause：(1) RenderingExtractor 失败时 analyzer 仅 logger.warning + result.rendering={}，worker 把空 dict 序列化成空字符串写库，DB 看不出 tried-and-failed vs never-tried；(2) 历史 analysis_history 行无 backfill 路径；(3) TaskManager.save_result 后 task 详情不暴露 rendering 失败状态；(4) 详情页无结构化卡片时把完整论述折叠到 details 调试块，看起来像内容消失。修复：(A) analysis_history 增 3 列 rendering_status (success/partial/failed/empty/pending) / rendering_error / rendering_generated_at；CREATE TABLE + database 迁移 + task_store 迁移三处同步加。(B) 新建 agents/rendering/status.py classify(rendering, source_tabs_present) 状态机：依据 8 tab 是否成功 + 源是否非空区分 success/partial/failed/empty；available_tabs 给 UI 展示徽章。(C) analyzer 把异常写到 result.rendering_error；workers _rendering_outputs(rendering, result) bundle 4 字段，source_present 提升 partial 分类准确度。(D) database.update_rendering(analysis_id, rendering_json, status, error, generated_at) 只动 rendering 列不碰其它给 backfill 用。(E) 新 worker analysis_rendering_backfill 参数二选一：analysis_id 单条 / limit 批量（默认 10 倒序，扫 rendering_json='' OR rendering_status IN ('failed','pending','')）；用 _StubResult duck-type 喂 RenderingExtractor 仅复用已存 7 列 markdown，不重跑 7-agent pipeline。(F) POST /api/history/<id>/rendering/retry 入队 backfill 任务返回 task_id，详情页重试按钮调它。(G) /api/history/<id> DTO 增 rendering_status / rendering_error / rendering_generated_at / rendering_available_tabs 4 字段；_infer_rendering_status_legacy 给 v1.6 行 status NULL 按 rendering_json 内容现算。(H) 前端详情页降级逻辑重写：无结构化卡片但有 markdown 正文时 Markdown 直接作主视图，不再藏到 details；结构化卡片成功时 markdown 仍作 details 二级展示。新增 RenderingStatusBanner 组件位于 tabs 上方，failed/pending/empty 时显示琥珀/灰色提示 + 重试按钮 + 错误详情折叠（success/partial 不显示横幅）；点击重试 30s 后自动 re-fetch /api/history/<id>。AnalysisDetailView 加本地 useState 镜像 prop 让 setDetail 可工作。(I) AnalysisDetail interface 增 4 字段。测试：tests/agents/test_rendering_status.py 7 case 覆盖 classify 全部分支 + tests/web/test_rendering_status_dto.py 7 case 覆盖 DTO 字段 / partial / legacy fallback / retry endpoint 入队 + 404；既有 20 case 全保持兼容。严格不动 OverviewCard / 8 Card 数据契约 / RenderingExtractor 内部 LLM prompt / signal / advice / paper-trade 流 / API path 命名。验收：pytest tests/web/ tests/agents/ tests/tasks/test_workers.py tests/tasks/test_task_store.py -q → 375 passed；npm run build ✓。自写 ~520 LOC（status.py 90 + worker 120 + database 35 + app DTO+endpoint 60 + AnalysisPage 110 + tests 105）。 | — |
| 2026-05-05 | [llm-openrouter.md](llm-openrouter.md) | v1.0 — 实施落地 | **OpenRouter v1.0 实施完成**（用户 2026-05-05 提，~6h）。按 [llm-openrouter.md v1.0](llm-openrouter.md) 设计逐节落地，13 个独立 commit（每节独立 commit + 独立测试）：(§1) `VALID_PROVIDERS` 加 `"openrouter"`；(§2) `router.py` `Provider` Literal 三态 + 新增 `resolve_openrouter_model(config, role, feature?)`（4 级 fallback：feature-pin → role pointer → first matching role → hardcoded safe default，永不抛）；auto-detect 链尾改 OR-first 让 `OPENROUTER_API_KEY` env 在场即激活；`has_provider_key("openrouter")` 接受 env 或 yaml；10 case 测试；(§3) `OpenRouterTextClient`（OpenAI-compatible，default 走 *quick* preset，`extra_body.provider.order` 透传 preset.provider_order + `allow_fallbacks:true`），`get_text_client` 工厂加 OR dispatch；6 case 测试；(§4) `default_config.yaml` 加 `openrouter:` slice 含 3 默认 preset（deepseek-v4-pro deep / gemini-3.1-pro deep / deepseek-v4-flash quick）+ `active:{deep:"deepseek-v4-pro", quick:"deepseek-v4-flash"}`；(§5) `BaseGuruAgent._get_chat_model` 加 `provider == "openrouter"` 分支（复用 `langchain_openai.ChatOpenAI`，role="deep" 给 14 大师，`with_structured_output(GuruSignal)` 零改造）；(§6) `analyzer.py` `_configure_openrouter` 新方法 + `_build_quick_llm` OR 分支 + **graph cache key 升级**为 `f"openrouter:{deep_id}:{quick_id}"`（防切 preset 后复用旧 graph）；4 case 测试覆盖 ta_config 字段填充 / 缺 key 抛 / cache key 在 deep 切换后变化 / quick LLM 用 quick preset；(§7) `workers.py:458` + `pipeline.py:67` `provider` 默认值由硬编码 `"qwen"` 改读 router（`provider=None → get_active_provider`）；(§8) `/api/settings/llm-provider` GET 加 `has_openrouter_key`，POST 错误消息使用 `_PROVIDER_LABEL` dict 输出 zh 标签；新增 `/api/settings/openrouter/active` GET/POST（GET 返 `{deep, quick, presets, active, active_provider}`，POST 校验 role+preset_id 后写 yaml + `_reset_config_dependent_singletons`）；`/api/settings` GET 加 OR slice（key 掩码 + `presets[]` + `active_pointers{}`，`active` bool 表 env-or-yaml usable）；`WRITABLE_SETTING_PATHS` 加 6 个 openrouter scalar 路径；10 case 测试；(§9) `LLMSwitcher.tsx` 两段式重写：上半段 3 provider radio（OR 加未配置 disabled），下半段仅 OR active 时展开（深度推理 / 快速思考两个 RadioGroup + 「管理预设」link to `/settings#openrouter`，env-lock 同时禁用 provider + preset radio）；switch flow 优雅 toast；(§10) `SettingsPage.tsx` 加 OR section（api_key 掩码 + 清空按钮 + base_url + http_referer + x_title + timeout + 只读 preset 表带 ★ deep/★ quick marker；"添加预设"+"测试 ping" 按钮在 v1.0 不实现，footer 引导用户 hand-edit yaml 添加 — 因为新增/删除 preset 需要后端 POST/DELETE endpoint，作 v1.1 deferred 在 changelog 显式标注，避免占位按钮）；后端 DTO 修复：`/api/settings` GET 的 openrouter slice 原本两个同名 `active` key（bool + dict 互相覆盖），重命名 dict 为 `active_pointers`；(§11) `/api/diagnostics/providers` 加 OR 项（HEAD `/v1/models` 探活，`has_key=False` 时短路返"not configured"，10s timeout）；(§12) `tests/integration/test_screener_v3_openrouter.py` CI-gated 实调 OR（pytest `skipif not OPENROUTER_API_KEY env`），驱动 `ScreenerV3Pipeline.run()` 1 ticker × 1 guru，断言 GuruSignal 字段非 fallback；(§13) 验收：`pytest tests/llm tests/agents tests/web/test_settings*openrouter* tests/web/test_llm_provider_api.py tests/web/test_settings_api.py tests/screener tests/integration -q` **401 passed + 1 skipped**（25+ OR 新增 + 376 regression），`npm run build` 4.2s 全绿。**前端单测 deferred**：仓库无 vitest harness（package.json 无 test script），standing up jsdom+vitest 是独立 ~30min 任务，本轮跳过；frontend 改造由 `tsc -b` + `vite build` 类型检验 + 手动验收 cover；列入 v1.1。**v1.0 已知留尾**：(a) 应用层 `RunnableWithFallbacks(OR,qwen)` 三级 fallback 不做（v1.0 仅靠 OR 内部 `provider_order` 跨 vendor 路由）；(b) 功能级 overrides（nl_parser/RenderingExtractor/roundtable 各用不同 model）不做；(c) 多租户 per-user OR key 不做（全局 key 与现有 qwen/gemini 一致）；(d) 上游 TradingAgents 构造自己的 ChatOpenAI 不读 `extra_body`，所以 `_configure_openrouter` 不注入 `provider_order` — analyzer 路径无跨 vendor fallback（guru / screener-internal 路径仍有，因为它们自己构造 ChatOpenAI）；(e) "添加预设" / "测试 ping" UI 按钮 v1.0 不实现（需 backend POST/DELETE/ping endpoints；v1.1 加）；(f) frontend vitest harness 不安装。**测试 ordering 修复**：`_configure_openrouter` 直接 `os.environ[...] = key` 写 env（上游 factory 从 env 读），monkeypatch 不会自动还原此 mutation，导致 `tests/web/test_llm_provider_api.py::test_get_returns_state` 在 OR 测试之后跑会拿到 `active="openrouter"` 而非预期 `qwen|gemini`；`tests/agents/test_analyzer_openrouter.py` 加 autouse fixture snapshot/restore `OPENROUTER_API_KEY` env 修。**严格不动**：数据层 / 多租户边界 / analyzer/screener 业务逻辑 / 14 大师 prompt / RoundtableResult dataclass / TradingAgents 上游包 site-packages / Qwen/Gemini 直连路径 / yaml + env 优先级链 / shadcn UI primitives. 自写 ~1100 LOC（含测试 + 前端 + 后端）。 | ce6d5b8 → §13 |
| 2026-05-05 | [llm-openrouter.md](llm-openrouter.md) | v1.0 | **LLM API 切换为 OpenRouter（聚合层 + 灵活模型切换）**：把 LLM 调用从「Qwen / Gemini 二态直连」升级为「Qwen / Gemini / OpenRouter 三态」+ OR 聚合层下「预设池 + 激活指针」灵活模型切换。**战略路线 C**（加 OR 为第三 provider 默认走，保留 Qwen/Gemini 直连作快路 / 大陆 fallback；不删直连代码）。**已实查 OR 模型目录（2026-05）**：`deepseek/deepseek-v4-pro` ($0.435/$0.87 per 1M, 1M ctx) 作 deep 默认；`google/gemini-3.1-pro-preview` ($2/$12, 1M ctx) 作 deep 备选（注意 OR 上正式名含 `-preview` 后缀，未 GA）；`deepseek/deepseek-v4-flash` ($0.14/$0.28, 1M ctx) 作 quick 默认（与 deep 同 vendor 路由更稳，比 `gemini-3.1-flash-lite-preview` 便宜 4 倍）。**核心抽象**：把 `openrouter.deep_think_model="..."` 单值字段升级为 `presets:[{id,label,model,role,provider_order,kwargs},...]` + `active:{deep,quick}` 激活指针——切模型 = 改 `active.deep` 一行（UI/API/yaml 三路径），加新模型 = `presets` append 一项不动代码；resolve order = active.overrides[feature] → active[role] → first matching role → hardcoded safe default（永不抛）。**改造面**（5 处调用 + 4 处配置/UI，~815 LOC）：(1) [`llm/constants.py`](../../stock_trading_system/llm/constants.py) `VALID_PROVIDERS` 加 `"openrouter"`；(2) [`llm/router.py`](../../stock_trading_system/llm/router.py) `Provider` 三态 + 新增 `resolve_openrouter_model(config,role,feature?)` 解析函数；legacy auto-detect 链尾加 `OPENROUTER_API_KEY` env 在场 → 默认 openrouter；(3) [`llm/client.py`](../../stock_trading_system/llm/client.py) 加 `OpenRouterTextClient`（OpenAI-compatible，同 QwenTextClient 骨架，default 走 quick preset，`extra_body.provider.order` 透传 preset.provider_order）；(4) [`screener/v3/guru_agents/base.py:355`](../../stock_trading_system/screener/v3/guru_agents/base.py) `_get_chat_model` 加 OR 分支（复用 `langchain_openai.ChatOpenAI`，`role="deep"` 给 14 大师；`with_structured_output(GuruSignal)` 通过 tool calling 自动走零改造）；(5) [`agents/analyzer.py`](../../stock_trading_system/agents/analyzer.py) 加 `_configure_openrouter`（TradingAgents 上游 [`tradingagents/llm_clients/openai_client.py`](../../../../opt/anaconda3/lib/python3.12/site-packages/tradingagents/llm_clients/openai_client.py) 已原生注册 `_PROVIDER_CONFIG["openrouter"]`，无需 monkey-patch；仅 `ta_config["llm_provider"]="openrouter"` 即可）+ `_build_quick_llm` OR 分支 + **graph cache key 升级**为 `f"openrouter:{deep_id}:{quick_id}"`（防切 preset 后复用旧模型 graph）；(6) [`tasks/workers.py:458`](../../stock_trading_system/tasks/workers.py) + [`screener/v3/pipeline.py:67`](../../stock_trading_system/screener/v3/pipeline.py) `provider` 默认值改读 router；(7) [`web/app.py`](../../stock_trading_system/web/app.py) `/api/settings/llm-provider` 三态 + 新增 `/api/settings/openrouter/active` GET/POST（切 deep/quick preset，写回 yaml + 触发 `_reset_config_dependent_singletons(["llm_provider"])`）+ `/api/settings` openrouter slice 读写（`api_key` 掩码 `***last4`，POST 空字符串视为不修改）+ `/api/diagnostics/providers` 加 OR 项；(8) [`LLMSwitcher.tsx`](../../stock_trading_system/web/frontend/src/components/shared/LLMSwitcher.tsx) 两段式（provider 选择 + role(deep/quick) preset 选择，仅 OR active 时展开下半段）；(9) [`SettingsPage.tsx`](../../stock_trading_system/web/frontend/src/islands/settings/SettingsPage.tsx) OR section（api_key + http_referer + x_title + presets CRUD 表 + ★ 标当前激活 + "添加预设"弹窗 5 字段 id/label/model/role/provider_order/kwargs + "测试 deep ping" / "测试 quick ping" 1-token hello 按钮）。**默认 yaml 注入** [`config/default_config.yaml`](../../stock_trading_system/config/default_config.yaml)：`openrouter:{enabled:false, base_url:"https://openrouter.ai/api/v1", presets:[deepseek-v4-pro deep / gemini-3.1-pro deep / deepseek-v4-flash quick], active:{deep:"deepseek-v4-pro",quick:"deepseek-v4-flash"}, timeout:120}`；env 优先级链：`OPENROUTER_API_KEY` env > `openrouter.api_key` config。**复用优先**：L0 [model-switch v1.0](model-switch.md) 单一真源链 / `LLMTextClient` Protocol / TradingAgents graph cache / `_reset_config_dependent_singletons` / shadcn 组件；L1 `langchain_openai.ChatOpenAI` + `openai` SDK + TradingAgents 上游 OR 注册 —— **零新增依赖**；L4 自写 ~815 LOC 胶水，0 业务逻辑。**严格不动**：数据层 / [v1.18 R-fix-12](analysis-inbox.md) per-user advice 边界 / analyzer/screener 业务逻辑 / 14 大师 prompt / TradingAgents 上游包 site-packages / Qwen/Gemini 直连路径 / yaml + env 优先级链 / shadcn UI primitives。**OR 内置 fallback**：preset 级 `provider_order:["deepseek","novita"]` + `allow_fallbacks:true` 跨 vendor 自动切；应用层 `RunnableWithFallbacks(OR,qwen)` v1.0 不做（v1.1 评估）；功能级 overrides（nl_parser/RenderingExtractor/roundtable 各异 model）v1.0 不做。**模型名漂移风险**：`gemini-3.1-pro-preview` 含 `-preview` 后缀（OR 上为 preview 状态，未 GA），GA 后可能改名；preset 表设计为用户可编辑，改名时改一行 yaml/UI 即可，不动代码。**测试**：后端 5 文件 ~25 单测（router 三态 4 case / `resolve_openrouter_model` 4 case / OR client 5 case / `_configure_openrouter` 3 case + graph cache key 升级断言 / `/api/settings/openrouter/active` 4 case + endpoint 三态 3 case）；前端 2 文件 ~8 vitest（LLMSwitcher 两段式 4 case + SettingsPage OR section 4 case）；`tests/integration/test_screener_v3_openrouter.py` CI gated by `OPENROUTER_API_KEY` env；手动回归 7 项（配 key → 切 OR → 切 deep preset → 跑 analysis quick depth → 跑 screener-v3 agent_rt 3 大师 → diagnostics 健康 → 切回 Qwen graph cache 命中）。**用户 2026-05-05 决策**：路线 C / quick=deepseek-v4-flash / deep=deepseek-v4-pro 默认 / gemini-3.1-pro-preview 备选 / 预设池 v1.0 三项可后续在 Settings 加 / 功能级 overrides v1.0 不做 / 三级 fallback v1.0 不做。 | — |
| 2026-05-05 | _移动端 UI 治理 Phase 3（用户 2026-05-05 二次反馈，~45min）_ | — | **移动端二次收尾**：Phase 1/2 横扫后剩 4 处用户实测仍溢出/拥挤的细节，本轮专项收尾。(A) `AlertsPage` 活跃警报行：原 `flex items-center gap-3` 强制单行，320px 下 ticker badge / condition / 日期 / 删除按钮被挤到容器外。改外层 `flex flex-wrap items-center gap-2 min-w-0`，ticker+condition 区域 `flex-1 min-w-0` 占满第一行，日期 + 删除按钮迁到 `mobile-action-row sm:ml-auto` wrapper（移动端独占第二行 / 桌面 sm+ 仍 ml-auto 右对齐）；condition 文本加 `break-words` 防长 condition 名（如 `pct_change_above`）撑卡。(B) `ReportsPage` 提交成功 Alert：原 `任务 ID: <code>{uuid}</code>，状态: {status}。<a className='ml-2'>查看任务进度</a>` 全部塞一行，UUID-style task_id 36 字符在 320px 下溢出 Alert 边界。改 AlertDescription 加 `break-words min-w-0 space-y-1`，task_id `<code>` 加 `break-all` 让 UUID 在任意字符断行；状态 + 链接拆成 3 个独立 `<div>` 块（任务 ID 行 / 状态行 / 链接行），链接去掉 `ml-2` 改独占块 + 加箭头 `→` 视觉提示，移动端自然换行不需 hack。(C) `PaperTradePage` 日度数据 tab `DailyDataTab` 权益曲线：原 `<ChartPanel height={360}>` + ECharts `grid: [{left:60,right:20...}, ...]` 桌面端尺寸固定。新增 `useState + matchMedia("(max-width:575.98px)")` + `useEffect` 订阅断点切换，移动态 `gridLeft=38 / gridRight=10`（缩 36% / 50%）+ ChartPanel `height={isMobile?280:360}`，ChartPanel 外层 wrap `<div className='mobile-chart-panel' data-chart-host>` 让 styles/index.css 中的 mobile-chart-panel CSS 变量驱动 chart-host 高度（`--chart-h:260px` 默认值兜底），useMemo dep 加 `isMobile` 让 chartOption 随断点重算。(D) `Sidebar.tsx` MobileMore Sheet：原 `grid grid-cols-4 gap-3 mt-2` 在 320px 下每格 ~70px，长中文 label （如 `纸面交易记录`）4 字以上 wrap 到第二行触碰下一格。改 `grid-cols-3 sm:grid-cols-4`（窄屏 3 列每格 ~93px）+ 每格 a 标签加 `min-w-0 title={label}`（hover/长按看完整名称）+ icon 包 `<span className='min-w-0 shrink-0'>` + label 加 `truncate max-w-full text-center` 强制单行截断，避免布局抖动。**严格不动** Phase 1/2 的全局工具类（mobile-card-header / mobile-action-row / mobile-chart-panel CSS 已就位）/ ChartPanel 组件签名 / Alert UI primitive / Sheet 组件 / ECharts series 配置 / 桌面 sm+ 布局 / 数据契约。验收：`npm run build` 4.6s 全绿（vitest 在本分支已被回滚到 v1.2 之前的状态，前端无单测可跑）；`pytest tests/web/test_settings_api.py tests/web/test_analysis_detail.py tests/web/test_vite_modulepreload.py -q` 11 passed（前端改动不触发后端回归）；代码级 audit：4 处溢出点修复对应 `grep "flex items-center" \| grep -v "flex-wrap"` Alerts/Reports 行不再命中。**视觉验收**：用户/Phase 4 浏览器实测确认 320/375/414 三档 (Alerts 活跃警报 / Reports 提交后 Alert / PaperTrade 日度图表 / MobileMore 8 个 label) 不溢出；纯前端纯样式，自写 ~50 LOC（4 个 islands diff）。 | — |
| 2026-05-05 | [screener-v3.md](screener-v3.md) v1.8 + [screener-v2.md](screener-v2.md) v1.5 | — | **强主题 gate fail-closed + guru prompt 改注册中心驱动**（用户 2026-05-05 提，~2h）。v1.7 修了 `partial_theme_fallback` 标签语义，但 `_apply_theme_fit_gate` 在 v1.7 changelog 里**显式保留「sector 缺失依然 permissive 通过」**。线上实测电力股查询仍有 IBM/ORCL/TSLA 这类 LLM 偶发返回 + DataRouter 漏 sector 字段的组合污染：gate 全部放行，14 大师 LLM call 烧在 off-theme 票上、结果列表也被污染。同时审计发现 `_build_theme_instruction` rule 10 把 storage 的禁止 anchor 名单硬编码进 prompt 模板，新增 power_utilities / traditional_energy / clean_energy / grid_electrification 4 个主题没有 prompt 路径，guru 看不到主题约束（只能靠 spec 字段间接感知）。**修复**：(A) `_apply_theme_fit_gate` v1.5 改 fail-closed —— `theme.is_strong=True` 时 sector 缺失候选移到 `excluded_off_theme`，`reason='missing sector under strong theme {theme.key}'`，UI 仍可显示「因数据缺失被剔除」标签但**不进 guru scoring**；curated universe 成员（NEE/AEP/DUK 等）仍优先短路保留即使 sector 缺失；`extra_when_explicit` 触发的 extras（云存储 → AMZN/MSFT/GOOGL）走同一短路路径；reason 文案明确区分「missing sector」与「sector mismatch」两类，前端可分组渲染；is_strong=False 的弱主题保持 v1.4 permissive 行为（forward-compat，目前 5 个注册主题全部 strong，分支只为未来 weak theme 留口）。(B) `_build_theme_instruction(query, spec, theme_metadata=None)` 接受可选 registry 元数据；新建 `_render_theme_metadata_block(meta, query)` helper 输出 5 行：`主题约束(theme=key)` 标题 / disambiguation_note / curated universe / allowed sectors / 当前 query 触发的 explicit_extras / broad-market 禁止 anchor 列表（BRK-B/JPM/V/MA/UNH/WMT/PG/AAPL/MSFT/GOOGL）。删除 prompt 模板里 storage-only 的 rule 10，storage 现在与其它 4 个主题走完全相同的注册中心路径。`_llm_reason` 从 `context['theme']` 读元数据（pipeline 已经在 phase 4 写入）并透传给 `_build_theme_instruction`。**测试**：`tests/screener/v2/test_theme_scoping_v1_3.py` 删除 `test_theme_fit_gate_keeps_unknown_sector_permissive`，新增三个：`test_theme_fit_gate_fails_closed_on_missing_sector_under_strong_theme`（NEE 在 universe 保留，IBM/ORCL/TSLA 三票全部 excluded with 'missing sector under strong theme power_utilities'）+ `test_theme_fit_gate_universe_member_kept_even_with_missing_sector`（NEE/DUK/AEP 全部 sector 缺失但都在 universe → 全部保留）+ `test_theme_fit_gate_explicit_extras_pass_through`（云存储查询下 AMZN 通过 gate / MU 保留 / AAPL 剔除）。`tests/screener/v3/test_theme_aware_prompts.py` 修订 `test_theme_instruction_includes_required_storage_carveout` 改用 `theme_metadata=tu.theme_metadata('memory_storage')` 显式驱动注册中心路径；新增 `test_theme_instruction_omits_explicit_extras_when_not_triggered`（普通 `存储龙头股` 不广告 AMZN/MSFT 为成员）+ 4 个独立主题 prompt block 测试（power_utilities / traditional_energy / clean_energy / grid_electrification 各自验证 universe 成员 + allowed sectors + disambiguation_note 透传 + clean_energy universe 段不能出现 XOM/CVX）。验收：`pytest tests/screener -q` 232 passed（含 39 个新/修订主题测试）。**严格不动** GuruSignal Pydantic schema / RoundtableResult / `_enforce_theme_fit` cap rules / `_aggregate` / concurrency.run_guru_units / data bundles / FilterSpec dataclass 字段 / 前端任何文件 / API 端点 / TaskStore schema / theme_universe 注册中心字段。自写 ~150 LOC（pipeline 50 + base prompt 60 + 测试 40）。关联 commits: dcacc1f refactor(guru): registry-driven theme prompt block / ea36845 feat: mobile UI baseline + screener-v3 gate v1.5 / bca84f2 test(screener-v3): align with v1.5 fail-closed gate + registry prompts | dcacc1f / ea36845 / bca84f2 |
| 2026-05-05 | _移动端 UI 治理 Phase 2（用户 2026-05-05 提，~1.5h）_ | — | **移动端 Phase 2：剩余 8 页横扫 + Playwright 36 用例全量验收**。承接 Phase 1 基线，把横扫扩展到剩余页面：(A) `DashboardPage` — 净值曲线 Header `mobile-card-header`（标题 + chips ChipRow + 重新计算按钮），ChipRow 父 `min-w-0 max-w-full overflow-hidden` 让 6 时间范围 chips 真正横向滚动而不是撑卡，重新计算按钮 `shrink-0`；持仓行 `flex-wrap sm:flex-nowrap`（mobile 第一行 ticker+market+shares、第二行 PnL，desktop 单行）；运行中任务进度行加 `min-w-0 truncate flex-1`；额外把 `Stat` 顶层 primitive 加 `min-w-0 truncate / shrink-0` 防 label/icon 错位；(B) `PortfolioPage` — Header 替 `mobile-card-header`（"持仓管理" + "买入" 按钮），持仓 mobile 卡 3 列 `成本/盈亏/现价` 改 `flex-wrap justify-between gap-x-3 gap-y-0.5` 320px 自动 2-1 排版，交易记录 `BUY 100 @ $123.45` 文案 `flex-wrap text-safe--wrap`；(C) `TasksPage` 列表 + 详情 — 列表行外层 `flex-wrap` + 内层时间/类型 badge `flex-wrap shrink-0`，详情 Header `mobile-card-header`（返回 + 标题 + 时间 + 类型 + 状态 badge），Event rows 每行 `flex-wrap min-w-0 + text-safe--wrap` 长 title 兜底，操作按钮区改 `mobile-action-row justify-center` 取消/重试/删除 320px 优雅换行；(D) `BacktestPage` — Header `flex-wrap` + ticker/strategy badge `shrink-0`/`whitespace-normal max-w-full` 长策略名 break，交易明细 Header `mobile-card-header` + 再次运行 `mc-actions`，mobile 交易卡 `flex-wrap`；(E) `AlertsPage` Header `mobile-card-header` + 新建警报 `mc-actions`，触发历史行 `flex-wrap + sm:ml-auto` + condition badge `whitespace-normal max-w-full`，message `break-words`；(F) `ReportsPage` 报告卡 Header `mobile-card-header` 标题 break-words + 时间 badge mc-actions，Markdown content 加 `[&_table]:block [&_table]:max-w-full [&_table]:overflow-x-auto [&_pre]:overflow-x-auto [&_code]:break-words` 让生成的 GFM table / fenced code 横向滚动而不撑卡；(G) Playwright spec 升级：`PAGES` 数组从 4 个 ScreenerV3/Settings/PaperTrade/Analysis 扩到 12 个（按用户原始验证清单顺序：dashboard / analysis / reports / screener-v3 / portfolio / alerts / paper-trade-list / paper-trade / tasks / backtest / settings / history），新增 8 个 `e2e/dev/{page}.html` mount harness；截图归档目录 `docs/qa/mobile-2026-05-05-phase2/` 36 张 PNG（4 列 × 12 页）。验收：`npx playwright test --reporter=list` **36/36 passed in 50.1s**（12 页 × 320/375/414 全部满足 `document.documentElement.scrollWidth <= window.innerWidth + 1`）；`npm run build` 3.6s 全绿；dashboard-320.png 实测：4 个 Stat 卡片整齐、净值曲线 chips + 重新计算按钮在标题下方独占一行、当前持仓 / 运行中任务 / 快捷操作 2-col grid 全部不溢出。**未做**：ECharts grid `left/right` 移动端响应式参数（净值曲线轴标签 320px 仍偏大但不溢出 — 视觉细节非阻塞，留作 Phase 3）。**严格不动** AppShell 导航 / api 端点 / islands 数据契约 / 桌面端 sm+ 布局。自写 ~120 LOC（6 islands diff + Stat primitive 加固 + 8 个 dev HTML + spec 扩展） | — |
| 2026-05-05 | _移动端 UI 治理 Phase 1（用户 2026-05-04 提，~2h）_ | — | **移动端 icon / 按钮 / chip 溢出全站治理 Phase 1**。背景：用户报告 320/375/414px 多页面 icon、按钮、chip、badge、日期、工具栏溢出卡片，根因 (a) `Card/CardHeader/CardContent` 桌面优先 `px-5` padding 移动端不收窄；(b) `flex justify-between` 缺 `min-w-0`/`flex-wrap`，子节点拉伸父容器；(c) `Button` 默认 `whitespace-nowrap` 长 label 撑出；(d) `ChipRow` 缺 `min-w-0/max-w-full`，`overflow-x-auto` 不触发；(e) `TabsList` 8-tab 详情页未默认横向滚动；(f) 多处 `ml-auto` 在 mobile flex-wrap 行里行为异常。范围（**Phase 1 只覆盖 4 个最严重页 + 全局基线，剩余 8 页 Phase 2 处理**）：(A) `src/styles/index.css` 增加 8 个 mobile 工具类 `mobile-card-header` / `mobile-toolbar` / `mobile-action-row` / `mobile-scroll-row` / `mobile-tabs-scroll` / `mobile-chart-panel` / `icon-fixed` / `text-safe`（含 `--wrap` 变体），CardHeader/CardContent 通过 `data-card-header=""` / `data-card-content=""` attr 触发 ≤575.98px 自动 `px-4` 收窄；(B) 4 个 UI primitive 改造：`Card` 加 `min-w-0 max-w-full` 防整卡溢出；`Button` 新增 `size="wrap"` 变体允许长 label 换行；`ChipRow` 强制 `min-w-0 max-w-full flex-nowrap` 让 `overflow-x-auto` 真正生效；`TabsList` 内置 `mobile-tabs-scroll` 包裹 — 不再要求每页手动加 `tabs-scrollable`；(C) 4 页面横扫修复 32 处 `ml-auto/justify-between/font-mono` 溢出点：`ScreenerV3Page` 17 处（首页 BETA badge / ResultsView Header / banner 4 个警告 Badge `w-full sm:w-auto sm:ml-auto` / 候选移动端卡片两行布局 / KV/Section helper 加 `min-w-0` + `text-safe--wrap` / 历史列表 ScreenHistoryRow 操作组迁移 `mobile-action-row sm:ml-auto`），`SettingsPage` 8 处（Header `mobile-card-header` / API key masked text 限 `max-w-[12rem]` 截断 / Switch shrink-0 / scheduler 状态行 flex-wrap / admin "立即跑一次" + msg `mobile-action-row`），`PaperTradePage` 7 处（顶部 ticker badge `mobile-card-header` / 计划档位行两行布局 / PlanHistory date `sm:ml-auto` / EventTimeline 行 `flex-wrap` + `text-safe--wrap` / Daily 移动端卡片 3-stat 行 flex-wrap），`AnalysisPage` 6 处（分析记录 Header `mobile-card-header` / KV/InboxToolbar 加 `flex-wrap min-w-0` / RunningRow & CompletedRow 时间戳 `sm:ml-auto shrink-0` / TabsList 移除冗余 `tabs-scrollable` / K 线高度 380→260 通过 `useState + matchMedia("(max-width:575.98px)")` 响应式切换）；额外修了 `PaperTradeListPage` 会话卡 ticker truncate + PnL 行 flex-wrap + skip_reason badge `whitespace-normal max-w-full break-words`。**Phase 2 待办**：剩余 8 页 Dashboard / Portfolio / Tasks / Backtest / Alerts / Reports / History / 任务详情页 整治；ECharts grid `left/right` 移动端配置（dashboard 净值曲线）；Playwright `scrollWidth ≤ innerWidth+1` 自动验收（当前仓库无 Playwright，~250MB chromium 安装本轮不做）。**视觉验收降级说明**：本轮无浏览器自动化，仅做代码级 audit（`grep "ml-auto" \| grep -v "sm:ml-auto"` + 全 `justify-between` 必有 `min-w-0/flex-wrap` 校验全部通过），320/375/414 实测需用户/Phase 2 补足。验收：`npm run build` 4.5s 全绿；`pytest tests/web tests/screener -q` 439 passed（前端改动不触发后端回归）；不动 islands 数据契约、不动 socket/api endpoints、不动桌面端 ≥sm 布局。自写 ~250 LOC（CSS 130 + 4 primitives + 4 islands diff） | — |
| 2026-05-04 | [screener-v2.md](screener-v2.md) v1.4 + [screener-v3.md](screener-v3.md) v1.7 | — | **候选池设计纠偏（用户 2026-05-04 第二次反馈，~1.5h）**：v1.3 把"主题固定名单"当退化方案打补丁，但仍把 LLM 短答案误标 `partial_theme_fallback`，让 UI 误以为主路径失败。本次纠偏明确"输入驱动 + 动态生成 + 主题相关性校验 + fallback 降级"四段链路：(A) `UniverseFilter.filter_by_spec` 删除 `partial_theme_fallback` source，Layer A LLM 任意非空输出统一标 `dynamic_llm`，Layer B 仅在 LLM 不可用/空时触发标 `theme_fallback`，Layer D 强主题硬保底也归 `theme_fallback`，禁止用源标签糊弄"是否降级"信号；(B) `QwenProvider.materialize_universe(spec=FilterSpec, ...)` 新签名直接吃 FilterSpec，新增 `_spec_to_criteria_blob` 把 sectors/themes/keywords/criteria 分行喂 LLM 替代旧 `；` 拼接 criteria string，背景：让 LLM 看清结构化约束而非靠模糊语义猜测；保留 `materialize_universe(criteria="…")` 旧签名向后兼容；`UniverseFilter._llm_universe` 改用 `spec=spec` kwarg 调用；(C) `ScreenerV3Pipeline._apply_theme_fit_gate` 改为"任意主题命中即跑 gate"（不再仅 strong 主题），扩展剔除项 schema 由 `[ticker, ...]` 升级为 `[{ticker, sector, reason}]`，每条剔除原因显式声明"sector=X 不在主题 Y 约束 sectors=[…]"，支持 UI hover tooltip / 直接展示；sector 缺失依然 permissive 通过；(D) `_v3_run_metadata` DTO 增 `raw_query / intent_summary / parsed_sectors / parsed_themes / parsed_keywords` 5 个字段从 persisted FilterSpec 投影出来 + `excluded_off_theme` 改为对象数组（向后兼容字符串数组旧记录）；(E) `ScreenerV3Page.tsx` banner 三行重排：第 1 行 [原始查询 「…」] [核心意图 →] [候选来源（按 dynamic_llm/theme_fallback/default 着绿/黄/橙）] [⚠️ 主候选生成失败/未命中主题 警告 Badge]；第 2 行 [🎯 theme] [行业] [主题] [关键词] [on-theme N]；第 3 行 [剔除 off-theme N：AAPL · Technology, JPM · Financial Services, …]，每个剔除标签 `title=reason` 鼠标悬停显示完整原因；新增 `sourceLabel(src)` 把枚举值翻译为"dynamic_llm（LLM 动态生成）/ theme_fallback（保守降级）/ default（大盘默认池）"中文 gloss；(F) 测试同步：`test_universe_filter_storage.py` / `test_universe_theme_fallback.py` / `test_pipeline_storage_fallback.py` / `test_screener_integration.py` / `test_theme_scoping_v1_3.py` 5 文件 8 处 source 断言从 `("theme_fallback", "partial_theme_fallback")` 简化为单值 `theme_fallback`，从 `"llm"` 改为 `"dynamic_llm"`；`test_theme_scoping_v1_3.py` `excluded_off_theme` 字段断言由 sorted-string-list 改为 dict-list 含 `ticker/sector/reason`。验收：`pytest tests/screener tests/web/test_screen_v3_trigger.py tests/llm/test_screener_integration.py` 237 passed；4 query (电力能源股/电力股龙头/能源股龙头/新能源龙头) 各自路由到独立 theme constraint（power_utilities/power_utilities/traditional_energy/clean_energy）+ 各自候选池差异化 + 0 broad-market leak；mock LLM ok 路径 source=`dynamic_llm` 且 BRK-B/AAPL 经 `filter_off_theme` 清除；`npm run build` 3.9s 全绿。**严格不动** FilterSpec dataclass 字段、screen_stocks v1 签名、theme_universe 注册中心、_DEFAULT_US 内容、guru SYSTEM_PROMPT、roundtable.py 行为、API 端点路径。自写 ~280 LOC（含测试调整） | — |
| 2026-05-04 | [screener-v2.md](screener-v2.md) v1.3 + [screener-v3.md](screener-v3.md) v1.6 | — | **强主题候选池退化修复（用户 2026-05-04 提，~3.5h）**：现状 V3 选股对"电力能源股"/"电力股龙头"/"能源股龙头"等强主题查询会退回 `_DEFAULT_US` 大盘池 → 结果出现 BRK-B/AAPL/MSFT/V/JPM/META/AMZN 污染票。线性根因：(a) `theme_universe.py` 仅注册 `memory_storage` 一个主题，`UniverseFilter` 自己又维护一套 `_STORAGE_KEYWORDS/_THEME_FALLBACKS_US/_CLOUD_STORAGE_KEYWORDS/_STORAGE_EXCLUDED_US` 双轨表；(b) `UniverseFilter._llm_universe` 用 `qwen.screen_stocks(criteria=, market=, count=)` 但 `QwenProvider.screen_stocks` 实际签名是 `(candidates, strategy, top_n)` → **每次调用 TypeError 静默失败**，永远走不到 LLM 一阶 universe materialisation；(c) `_fallback_spec` 仅消歧存储；(d) NLParser system prompt 没有电力/能源/新能源消歧规则。方案（**全主题统一 fail-closed**）：(A) `theme_universe.py` 升级为唯一注册中心，`Theme` 加 `sectors / disambiguation_note / is_strong`；新增 `theme_metadata(theme_key)` + `is_strong_theme(theme)`；UniverseFilter 删双轨表全部委托给 theme_universe；(B) 注册 4 个电力/能源主题族（power_utilities / traditional_energy / clean_energy / grid_electrification）；(C) NLParser Rule 12 中文消歧（电力股=Utilities / 能源股=油气 / 新能源=EV+光伏+风电+储能）+ `_fallback_spec` 4 个新主题专属模板；(D) `QwenProvider.materialize_universe(criteria, market, count)` 新方法替代错配的 screen_stocks 调用；(E) `filter_by_spec` 强主题 fail-closed：filter_off_theme + theme_fallback_universe，禁止落 `_DEFAULT_US`，新 source `partial_theme_fallback`；(F) ScreenerV3Pipeline `_get_candidates` 后加候选级 theme_fit gate 剔除 off-theme + 计入 `excluded_off_theme`；(G) `/api/screen/v3/results _v3_run_metadata` 加 `parsed_theme/universe_source/on_theme_count/excluded_off_theme`，前端 banner 透明显示。新增 `tests/screener/v2/test_theme_scoping_v1_3.py` 4 路径 + 4 验收 query (电力能源股/电力股龙头/能源股龙头/新能源龙头) 无 BRK-B/AAPL/MSFT 等污染票。**严格不动** FilterSpec dataclass 字段、`_DEFAULT_US` 内容、screen_stocks v1 签名、guru SYSTEM_PROMPT、roundtable.py、pipeline mode/cancel/cache 路径、API 端点。自写 ~450 LOC（含测试） | — |
| 2026-05-02 | [analysis-rendering.md](analysis-rendering.md) | v1.2 | 结构化卡片 ErrorBoundary 兜底闭环（用户 2026-05-02 提，~2.5h；联动 v1.0/v1.1）：现状生产 `/analysis/17`（SNDK）即使 v1.21 后端 `_normalize_card` 已就位，前端"概览"tab 仍触发 per-tab ErrorBoundary 显示"结构化卡片渲染失败" — 因为 (a) `OverviewCard` 入口只判 `typeof === "object"` 数组也通过，(b) 卡片只依赖后端单层 normalize，DB 里历史脏数据未走 backfill 时直接到客户端，(c) `ErrorBoundary` 静态 fallback 把错误吞掉，操作员看不到根因。方案：(A) `defensive.ts` 新增 `isRecord` / `safeRecord` / `safeEnum` / `safeBool` / `describeShape` / `normalizeCardForClient(tabKey, struct)` — 客户端二次 normalize 镜像后端 `_normalize_card`，对 8 tab 各自做 record/array/finite-number guard，输入非 record 直接返回 null（drop card），输入 record 但子字段脏（如 `summary.debate_synthesis: ["arr"]`）就地 collapse 到 null/[]；(B) `OverviewCard` 入口改 `safeRecord(data)`，`debate_synthesis` 走 `safeRecord` 否则置空，三派 `aggressive/conservative/neutral` 各自 `safeRecord`，`decision_drivers` / `key_metrics` 仅保留 record item（string/null/array item 直接丢弃），所有 React child 走 `safeText`；(C) 同样 record-guard 应用到 `MarketCard / SentimentCard / NewsCard / DebateCard / RiskCard / DecisionCard / FundamentalsCard`，array `.map(item => item.x)` 前先 `filter(isRecord)`，子字段（entry_zone / valuation / growth / profitability / balance_sheet）改 `safeRecord` 入口；(D) `AnalysisPage.tsx` per-tab `ErrorBoundary` 改 render-function fallback (`<CardFallback error={...}>`) 文案降级为"结构化摘要暂不可用，已显示完整论述"+ `<details>` 展开错误名/消息，`onError` 回调 `console.error` 输出 `analysis_id / tab_key / error_name / error_message / struct_shape`（仅 key+type 两层深，绝不打报告正文）；传给 `<AnalysisCards>` 前调 `normalizeCardForClient`；(E) `tests/web/test_analysis_sndk_smoke.py` 新增 7 case 覆盖 `summary` 是数组、`debate_synthesis` 是数组/string/scalar/partial、`decision_drivers` 含混合 string/null/object/array item、`key_metrics.value` 是 object/array、`rating/confidence` 是 trailing-whitespace/wrong-case/object、`Market.indicators` 含混合 string/null/record；(F) 新增 `__smoke__/malformed-payloads.ts` (Storybook-like fixture) + `__smoke__/normalize.smoke.ts` 纯函数 smoke harness（无 runtime test runner 依赖，`tsc -b` 校验类型 + `RUN_ANALYSIS_SMOKE=1` 环境变量驱动 self-execute）。验收：`pytest tests/agents/test_rendering_extractor.py tests/agents/test_rendering_schemas.py tests/agents/test_rendering_data_sources.py tests/web/test_analysis_rendering.py tests/web/test_analysis_rendering_normalize.py tests/web/test_analysis_sndk_smoke.py tests/web/test_analysis_detail.py -q` 58 passed；`cd stock_trading_system/web/frontend && npm run build` ✓ tsc + vite 全绿。**不动 schema、不动 8 Card 数据契约、不动后端 `_normalize_card`**；新增前端 ~280 LOC + 后端测试 ~140 LOC | — |
| 2026-05-02 | [screener-history.md](screener-history.md) | v1.0 | 选股记录与运行入口（用户 2026-05-02 提，~8h）：现状 `/screener-v3` 单 island，提交后跳 `/tasks/<task_id>`（**用户抱怨"只能从任务中心查"根因**），无 v3 历史 API（仅 v2 有），无 `/screener-v3/history` 路由。评估**不**学 analysis-inbox 单页合并：选股表单重（NL 输入 / 14 大师多选 / mode / market / candidate_n / 成本预估）+ 结果页重（v1.2 KPI 6 列 + Top 5 圆桌）+ 频次低（1-2 次/天，对比 analysis 高频多并发）+ 单次耗时长（agent_rt 15-30 分钟）。方案 3 入口结构：(A) `/screener-v3` 主页加 `<RecentScreensCard>` 顶部 3 卡 + 右上角 [查看全部 →] 链接；(B) `/screener-v3/history` 独立路由 `<ScreenerHistoryList>` 复用 v1.18 R-fix-12G HistoryPage 工具栏/行/多选 + 加 mode/market 多选筛选 + [包含失败] toggle + [复制配置重跑] 按钮；(C) `/screener-v3?prefill=<task_id>` 主页 ScreenerForm 自动用历史 task params_json 预填（NL/market/candidate_n/gurus/mode/with_roundtable）+ banner 提示；(D) 提交后从 `/tasks/<task_id>` 改为 `/screener-v3?task=<task_id>`，新增 `<ScreenerRunningView>` 同页内 PipelineDAG + `<GuruParallelProgress>` 大师并发矩阵（订阅 unified-progress `guru_unit_done` 事件） + 完成 replaceState 到 `?result=<task_id>` + 失败留在运行页带返回。后端：(E) `TaskStore.list_screen_v3_history(user_id, modes, markets, limit, offset, include_failed)` 新方法返每行 summary（candidates_count / avg_score / votes / consensus_rate_pct / top3_tickers / llm_calls / cache_hit_pct / duration_sec）从已有 payload.candidates + payload.metrics 实时聚合，不需 schema 变更；(F) `get_screen_v3_history_one(task_id, user_id)` 单条端点供 prefill；(G) `/api/screen/v3/history` `/api/screen/v3/history/<task_id>` 新端点（多租户 v1.18 R-fix-12 边界仅返 created_by==g.user.id）。**Sidebar 不动**（不新增"选股记录"菜单项，保持 1 项 `[选股]`，"查看全部"作主页二级链接）。`<ResultsView>` v1.0+v1.2+v1.3 / `<ScreenerForm>` 既有字段 / 任务中心 / screen_v3 schema 全部不动。复用 v1.18 HistoryPage ~70% 代码 + unified-progress + shadcn 组件库；自写 ~700 LOC | — |
| 2026-05-02 | [screener-v3.md](screener-v3.md) | v1.3 | 选股主题污染修复 V3 联动（用户 2026-05-02 提，~3h；联动 [screener-v2.md](screener-v2.md) v1.2）：现状 `存储龙头股` 经 v3 ScreenerV3Pipeline → guru agents → roundtable 全链漏掉主题——guru 收不到 query 信息按"全市场龙头"打分，roundtable 辩论与 query 无关，pipeline `_get_candidates` 异常路径裸 mega-cap fallback 含 BRK-B/JPM/V。方案 v1.3（注入主题约束到 BaseGuruAgent / roundtable / pipeline 三处，**不动** 14 大师 prompt 内容、aggregator 字段、RoundtableResult dataclass）：(A) `BaseGuruAgent._build_theme_instruction(query, spec)` 渲染 query + filter_spec verbatim 进 SystemMessage，包含 theme_fit SubAnalysis 强制要求、显式 broad-market 黑名单（BRK-B/JPM/V/MA/UNH/WMT/PG）、required pure-play (MU/WDC/STX/SNDK)、cloud carve-out（"云存储"/"对象存储"/"S3"/"Azure Storage" → AMZN/MSFT/GOOGL）；`_llm_reason` 拼 instruction 入 SystemMessage；4 个具体 guru (buffett/graham/lynch/munger) 调用签名加 `nl_query`/`filter_spec` 透传；(B) `_enforce_theme_fit(signal, context)` 后处理：theme_fit<4 → total_score≤60 + bullish→neutral；theme_fit<2 → total_score≤45 + bullish→bearish；缺 theme_fit subanalysis 的 legacy 输出不动（return same instance），不会上调 signal；(C) `roundtable._build_debate_prompt(guru, ticker, sig, stance, query, spec)` 加 query/spec 入参 + 内嵌 3 个 theme-fit 问题（"是否直接符合用户查询主题"/"是否主题内的龙头"/"主题匹配"）；(D) `ScreenerV3Pipeline._get_candidates` 异常路径走 v2 `theme_fallback_universe`（依赖 v2 v1.2），off-theme 回 trimmed mega-cap（经 v2 `_clean_theme_tickers` 过滤 7 个 polluters）。验收：`tests/screener/v3/test_theme_aware_prompts.py` 12 case + `test_pipeline_storage_fallback.py` 2 case 全绿。**不开新 LLM call、不发明 v3 端点、复用 v2 theme_universe 模块**；自写 ~250 LOC | — |
| 2026-05-02 | [screener-v2.md](screener-v2.md) | v1.2 | 选股主题污染修复 V2 基础（用户 2026-05-02 提，~2h；联动 [screener-v3.md](screener-v3.md) v1.3）：用户报告 `存储龙头股` 返 BRK-B/JPM/V/MA/UNH/WMT/PG（broad-market 蓝筹），根因（线性）：(1) NLParser system prompt 没有"龙头=主题内"语义约束 + 没有 storage 子主题消歧 → LLM 把 `存储` 默认项目到 cloud storage 或干脆出 mega-cap padding；(2) UniverseFilter LLM 不可用时裸用 `_DEFAULT_US` → 主题查询直出 BRK-B；(3) v3 pipeline 异常路径同样裸 mega-cap。方案 v1.2 **三层防御**：(A) 新建 `screener/v2/theme_universe.py`：`detect_theme(query, intent_summary, themes)` 当前注册 1 主题 `memory_storage`（关键词正则匹配 存储/内存/DRAM/NAND/闪存/SSD/HDD/memory chip 等）；`theme_fallback_universe(theme, query)` 返 required pure-play (MU/WDC/STX/SNDK) + extended semis (MRVL/AVGO/INTC/AMD/NVDA) + cloud carve-out（仅 query 含 `云存储`/`对象存储`/`S3`/`Azure Storage` 时打开 AMZN/MSFT/GOOGL）；`filter_off_theme(tickers, theme, query)` 大写化 + 黑名单过滤；`broad_market_blacklist()` = (BRK-B/JPM/V/MA/UNH/WMT/PG)；(B) `nl_parser._SYSTEM_PROMPT` 追加 Rule 9（中文存储主题消歧 + 6 子词）+ Rule 10（"龙头=主题或行业内部" + 显式 forbidden examples + cloud carve-out）+ cloud 仅 explicit keyword 打开；(C) `UniverseFilter.filter_by_spec` 引入 `_STORAGE_EXCLUDED_US = {BRK-B/BRK.B/BRKB,JPM,V,MA,PG,WMT,UNH}` + `_clean_theme_tickers` 二次清洗：LLM 输出 / theme_fallback / heuristic-narrow 三路全过 — `_DEFAULT_US` 本身**不动**；LLM 不可用 → theme_fallback；off-theme 查询保持 `_heuristic_filter(_DEFAULT_US)` 路径但同样过 cleaner（行为对称）；返 `(tickers, source)`，source∈{"theme_fallback","llm","heuristic","default"}。验收：`test_nl_parser_theme_rules.py` 4 + `test_theme_universe.py` 8 + `test_universe_filter_storage.py` 5 + `test_universe_theme_fallback.py` 5 case 全绿；off-theme path 100% 不变；`tests/tasks/test_task_store.py` 同期补 47 行覆盖 advice 隔离/idempotency/orphan recovery 已有契约（不是新设计）。**不动 FilterSpec dataclass 字段、不开新 LLM endpoint**；自写 ~330 LOC | — |
| 2026-05-02 | [analysis-inbox.md](analysis-inbox.md) | v1.0 | AI 分析 + 分析记录合并（用户 2026-05-02 提，~9h）：现状 3 island 分散——`/analysis` 主页（表单 + 5 卡）/ `/analysis/<task_uuid>` 运行中视图 / `/history` 独立分页列表（v1.18 R-fix-12G） / `/tasks` 任务中心，用户提交后跳 `/analysis/<task_uuid>` 离开主页 + 想看历史得切 `/history` + 不知道运行中跑哪了被迫去任务中心。根因：**没有页面同时展示运行中 + 已完成全状态**。方案：1 主入口（`/analysis`）= 紧凑表单 + 时间线 Inbox（混合运行中 task + 已完成 analysis 按 created_at DESC 排序），详情页 `/analysis/<numeric-id>` 完整 8 tab 保持独立路由（深链 / 分享 / SEO 友好），任务中心 `/tasks` **保持不变**（跨类型 task 总览）。后端：(A) `TaskStore.list_tasks_by_user_and_type(user_id, task_type, statuses)` 新方法；(B) `/api/history?include_running=true` 合并 tasks（pending/running/failed/cancelled）+ analysis_history 排序，response 加 `running_total / completed_total`，运行中**全返不参与 limit**，`exclude_task_ids` 防 task_completed 与 DB 写入间的瞬时重复；(C) `/history` 路由 301 redirect 到 `/analysis` 保留 query string。前端：(D) `<AnalysisHomeInbox> + <AnalysisFormHeader> + <AnalysisInboxList>` 主框架；(E) `<RunningRow>` 运行中卡 inline PipelineDAG（v1.17 R-fix-11F 已修对的组件直接复用，桌面默认展开 / 移动默认折叠+进度条 / 按 575.98px 断点同 mobile-optimization）；(F) `<CompletedRow>` 抽取自 v1.18 HistoryPage 行渲染 + 展开摘要 + 操作按钮 + 多选对比（功能完整保留）；(G) 提交不跳页 → 列表顶部乐观插运行中卡 → 订阅 task_events 流（unified-progress per-user room） → task_completed 替换为 analysis 行 / task_failed 替换为失败卡带重试 / task_cancelled 同；(H) 旧 `/analysis/<task_uuid>` URL 前端 `replaceState` 到 `/analysis?task=<uuid>` + Inbox 滚动锚定；(I) Sidebar 删 "分析记录" 菜单项（桌面 line 36 + 移动 line 140），`/history` 路由仍可作书签 redirect。`<AnalysisDetailView>` 完整 8 tab 详情页 + analysis-rendering v1.0/v1.1 8 Card 组件 + PipelineDAG v1.17 / unified-progress v1.0 / 多租户隔离 v1.18 全部不动。复用 v1.18 HistoryPage ~80% 代码 + unified-progress catch-up + shadcn 组件库；自写 ~310 LOC | — |
| 2026-05-01 | [screener-v3.md](screener-v3.md) | v1.2 | 选股结果决策透明化（用户 2026-05-01 提，~5h）：现状信号列全 `-`、看多/看空 KPI 全 0、大师数都是 4（无信息量）、圆桌辩论看不到。Audit 根因：`pipeline._aggregate` 已经计算 bullish/bearish/total 但**没把 verdict 写回 candidate.signal**；前端 ResultsView 只展示 `result.roundtable.summary` 一段而非按 ticker 分卡；展开行只 slice(0,80) 字 reasoning；运行模式 / LLM call / cache 命中 / 大师列表全无显示；价格/PE 有 `/api/fundamentals` 缓存但没接入。方案 v1.2（保持 `RoundtableResult` dataclass `consensus/dissent/split/debate_snippets` 字段不变）：(A) 后端 `_aggregate` 末尾追加每 candidate `signal` (bullish/bearish/neutral/split) + `votes` (bullish/bearish/neutral/total 计数) + `consensus` (unanimous/majority/split chip) + `confidence_range` (min/max/avg) + `top_bull_argument` / `top_bear_argument` (highest-confidence guru reasoning 200 字)；(B) `/api/screen/v3/results/<id>` payload 加 `run_metadata` (mode/llm_calls/cache_hit_pct/duration_sec/gurus_used/candidates_count/roundtable_enabled)，从 pipeline 已输出的 metrics 字段重命名透传；(C) 前端 4 块视觉一次落地：① KPI 扩 6 列（候选/均分/看多/看空/中性/共识率）；② 运行模式 banner（mode chip + LLM call + cache 命中 + 耗时 + gurus_used Avatar 链）；③ Top 5 圆桌独立 grid 按 ticker 卡片化（边框色按 split 标黄/统一标绿，CONSENSUS / CONTESTED Badge，debate_snippets 按 🟢🔴⚖️ 分色 border-l-2 渲染）；④ 候选表扩列（`#/代码/综合分/信号/投票分布条/共识度/现价/PE/操作`，现价/PE 走 `/api/fundamentals` 30s LocalCache 懒加载）+ 展开当前行（不开新页）显示 4 张大师卡（signal Badge + confidence + reasoning 完整段最大 240 字 line-clamp-6 + tier chip + philosophy hover）+ KPI 底栏（200-SMA / PE / ROE / D/E）+ `[→ 跑 AI 分析]` `[→ 加观察列表]` 操作。强约束：不动 `_aggregate` 已有字段（仅追加），不动 `RoundtableResult` 字段名，不开独立投票详情页路由，不发明 fundamentals 端点（复用 v1.6 R-perf 缓存） | — |
| 2026-05-01 | [analysis-rendering.md](analysis-rendering.md) | v1.1 | News / Fundamentals 改混合数据源（用户 2026-05-01 提，~2h 增量）：现状 quick-info 三卡显示 `FINAL TRANSACTION PROPOSAL: **BUY** # 宏观经济与...` 是 LLM markdown 报告头，因为 `extractFundamentals` regex 抽不到数字 / `newsSnippet.slice(0,200)` 截断 LLM 报告。Audit 发现项目里**已有真实数据源**：`data_manager.get_fundamentals(ticker)` (yfinance `Ticker.info` 含 PE/PB/ROE/D/E/营收增长/利润率全套) + `data_manager.get_news(ticker)` (Polygon `list_ticker_news` 优先, yfinance fallback 返 [{title, url, published, source}])，且 `/api/fundamentals/<ticker>` `/api/news/<ticker>` 端点已就绪。方案：(A) 新建 `stock_trading_system/agents/rendering/data_sources.py` 把 yfinance .info → Valuation/Growth/Profitability/BalanceSheet schema 映射 + Polygon news → Headline 映射；缺字段保 None 不发明；(B) 修订 RenderingExtractor 构造时多传 `data_manager`，News tab 调 `fetch_news_headlines` 拉真头条后让 LLM 仅做 sentiment/impact 标注 + catalysts + summary，Fundamentals tab 调 `fetch_fundamentals_facts` 拉真数字后让 LLM 仅写 vs_industry / quality_score / summary；其它 6 tab 不变；(C) **Hard guard**：headlines 必须 title 命中真集合（防幻觉），valuation/growth/profitability/balance_sheet 字典以真 facts 覆盖（防 LLM 改数字）；(D) 前端 quick-info 三卡（最近新闻 / 基本面指标 / 多空辩论）改为直接拉 `/api/news/<ticker>` + `/api/fundamentals/<ticker>` 显示真实 3 条头条 + 6 个真指标 + 多空辩论用 `detail.rendering["Investment Debate"]` 的 bull/bear arg 数 + verdict + key_disagreement；删除 `extractFundamentals / extractDebateCount` regex + `newsSnippet.slice(0,200)` 截断三个脆弱函数；(E) `/api/fundamentals` `/api/news` 已有 LocalCache（v1.6 R-perf 30s TTL），quick-info 卡与 8-tab extractor 共用 cache 不双拉。schema 不变，数据填充策略变 | — |
| 2026-05-01 | [analysis-rendering.md](analysis-rendering.md) | v1.0 | AI 分析结构化渲染（用户 2026-05-01 提，~10h）：现状 8 tab 直接渲染 LLM markdown → 决策结论/KPI/三派辩论被埋长文里。方案分层：每 tab = 结构化卡片头 + Markdown 主体（折叠）。后端：新建 `stock_trading_system/agents/rendering/`（schemas.py 8 个 Pydantic：OverviewCard/MarketCard/SentimentCard/NewsCard/FundamentalsCard/DebateCard/RiskCard/DecisionCard + 共享 KeyMetric/Stance/Argument；extractor.py RenderingExtractor 用 quick_think_llm + with_structured_output 并发 8 个 LLM call），analyzer 在 pipeline_done 前调 extractor 写 `result.rendering`，worker 序列化为 `rendering_json`，`analysis_history` 加 `rendering_json TEXT` 列 + idempotent ALTER；TaskStore `_save_analysis_result` 同步写；`/api/history/<id>` DTO 加 `rendering: {[tab_key]: dict\|null}`，绝不暴露原 JSON 字符串。**严格保证 rendering_json 是共享研究字段**，不含个人 advice/仓位（v1.13 R-fix-7D 边界），技术面客观价位（支撑/阻力/SMA 止损位）允许保留。前端：8 个 Card 组件（OverviewCard/MarketCard/.../DecisionCard）+ 4 共享子组件（RatingBadge/KpiRow/StanceCard/ConfidenceMeter/MoodGauge）；Tab 渲染优先卡片头，markdown 主体走 `<details>` 折叠保留 react-markdown + rehype-sanitize；rendering 缺失或 tab key=null → 单 tab 降级到纯 markdown。8 calls 并发用 ThreadPoolExecutor，单 tab 超时 45s/失败 raise 不影响其它 tab；rendering 失败不阻断分析任务（best-effort）；老分析无 rendering_json 自动降级。验收：8 schema 单元 + extractor 失败隔离 + 详情 DTO 测试 + 前端快照 + npm build。复用 v1.13 R-fix-7B 元数据迁移模式 + paper-trade v1.3 F3 ExecutiveSummary 模式 + v1.13 R-fix-7F rehype-sanitize 通道 + shadcn 组件库；自写 ~1500 LOC | — |
| 2026-04-30 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.12 | v1.11 修了代码没修数据：实测 daily_snapshots 仅 3 行（2026-04-14/15/16/19），距今缺 11 天，`task_scheduler.py:105` take_snapshot 调用存在但调度器没真跑。新增 R-fix-6 共 4 子项（~3h）：(a) `migrations/backfill_daily_snapshots.py` 历史回填（按交易日重放 transactions + yfinance 收盘价 → upsert 幂等）；(b) 修 APScheduler 启动 + cron 美东 16:30 + 多用户迭代 + 启动期 log；(c) Dashboard "↻ 重新计算"按钮 + `/api/portfolio/snapshots/backfill` 异步 task；(d) Settings 页"调度器状态"卡 + `/api/scheduler/status` & `/api/scheduler/run-now`。验收：DB ≥ 14 行 snapshot，dashboard ALL 14 连续点 | — |
| 2026-04-30 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.13 | AI 分析模块 7 大产品&技术缺口（用户提，~10h）：(A) **TVChart 修初始 Skeleton 早 return**——`TVChart.tsx:108` `if (loading && data.length===0) return <Skeleton/>` 导致 chart 容器永不挂载；改为始终渲染容器 + overlay 三态（loading/empty/error）+ `onRetry`；详情页 K 线主路径 `/api/quote/history?days=90`、回退 `/api/chart/<ticker>?period=3mo`；(B) **analysis_history 元数据补齐**——schema 加 `created_by/provider/config_hash/task_id/duration_sec/bookmarked` 6 列 + idempotent ALTER TABLE，TaskStore `_save_analysis_result` INSERT 同步写，worker 注入 `provider/model/config_hash/duration_sec/created_by/task_id`，详情页 Header 显示创建者/Provider/耗时/创建时间；(C) **废 `/api/analyze` 旧路径**——删 daemon thread + 直写 history + 硬编码 `gemini.deep_think_model`，改 thin wrapper 转 TaskManager 返 `{task_id, status}`；(D) **拆分共享分析与个人 advice**——新建 `user_analysis_advice` 私有表（user_id+analysis_id+持仓 snapshot+action+entry/stop/take）；`analysis_history` 仅共享研究；`/api/history/<id>` LEFT JOIN advice WHERE user_id=current（别人的 advice 永远不返）；(E) **`/analysis` 产品闭环**——首页 5 条最近卡 + 深度 RadioGroup（quick/standard/deep）+ 详情 8 tab（"决策"独立）+ 操作按钮 5 个（再次分析/加入持仓追踪/导出 PDF/MD/分享/收藏 per-user）；新增 `GET /api/history/<id>/export?format=pdf\|md` + `POST /api/history/<id>/bookmark` + `POST /api/portfolio/track`；(F) **Markdown sanitize**——`react-markdown` 加 `rehype-sanitize` + 白名单 schema（table/code/pre），防 LLM 注入；(G) **修 `_record_agent_scores` 双重记录**——之前为拿 analysis_id 先 save 半成品行 + worker 主路径再 save 完整行 → 同次分析 2 行；改为 TaskManager 落库取得真 analysis_id 后调 `scorer.record_analysis`，确保唯一。剩余 ~13h | — |
| 2026-05-01 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.14 | R-fix-7 验收 4 处遗留（用户 2026-05-01 提，~2.5h）：(A) **worker/analyzer progress_cb 契约对齐**——`workers.py:57` 无条件传 `progress_cb=` 但 `FakeAnalyzer.analyze(self,ticker,date)` 不收 → `pytest tests/tasks/test_workers.py` 红；统一契约为"analyzer.analyze 必须接受可选 progress_cb=None"，FakeAnalyzer 加 kw + 至少 1 case 验证 events；(B) **`/api/history/<id>` advice_json 跨用户泄露关闭**——legacy fallback 加 `record.created_by == g.user.id` 守卫；非创建者也 strip action/confidence/position_pct/entry_low/entry_high/stop_loss/take_profit 反推字段；`_migrate_analysis_history` 把 advice_json 搬到 user_analysis_advice 成功后**清空共享行的 advice_json + 7 结构化字段**（事务内）；新增测试 `test_bob_does_not_see_alice_legacy_advice_json`；(C) **TaskStore advice 后门关闭**——`_save_analysis_result` 删除 `if result.get("advice")` 兼容分支，advice_json 硬编码 `""`、action/confidence/position_pct/entry/stop/take_profit 硬编码 `None`；新增测试 `test_save_analysis_result_strips_advice` + `test_worker_advice_payload_routes_to_user_advice`；(D) **depth 参数真实生效**——`analysis_history` schema 加 `depth TEXT` 列 + idempotent ALTER；worker 读 `params.depth` 归一化到 `{quick,standard,deep}` 写入 result 与 DB；最小行为差异化：quick 强制关 iteration、deep 沿用 config iteration、standard 默认；详情页 Header meta 行展示"分析深度"；前端 RadioGroup 文案降级为不绝对化（quick 跳过迭代 / deep 启用迭代如已配置）；新增测试 `test_worker_persists_depth` / `test_worker_default_depth_is_standard` / `test_worker_unknown_depth_falls_back_to_standard` / `test_detail_returns_depth`。剩余 ~10.5h | — |
| 2026-05-01 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.17 | AI 分析 4 链彻底闭环（用户 2026-05-01 提，~4.5h）：v1.14/v1.15 多项设计**未真实落地**本轮真改 + 加 2 项 NEW。(A) **worker / Analyzer `progress_cb` 契约统一**——`StockAnalyzer.analyze(ticker,date,progress_cb=None)` 必选 kw；worker 调用加 `TypeError` fallback 兼容旧适配器（fallback 到 `analyze(ticker,date)`）；`tests/tasks/test_workers.py FakeAnalyzer` 加 `progress_cb=None` 入参 + 验证事件转发 case；(B) **`/api/history/<id>` advice_json 跨用户守卫**——legacy fallback 前置 `is_creator = (record.created_by == g.user.id)` 守卫；非创建者额外 strip `action/confidence/position_pct/entry_low/entry_high/stop_loss/take_profit` 反推字段；`_migrate_analysis_history` 把 advice_json 搬到 user_analysis_advice 后清空共享行 advice_json + 7 结构化字段（事务内）；新增 `test_bob_does_not_see_alice_legacy_advice_json`；(C) **TaskStore advice 后门彻底关闭**——`_save_analysis_result` 删除 `if result.get("advice"): advice_raw = json.dumps(...)` 兼容分支；INSERT 时 `advice_json` 硬编码 `""`、7 结构化字段硬编码 `None`；CREATE TABLE 加 `depth TEXT` 同步；新增 `test_save_analysis_result_strips_advice` + `test_worker_advice_payload_routes_to_user_advice`；(D) **provider/model cache_key 错配修复（NEW）**——`StockAnalyzer._init_graph` 废弃 `cfg["llm"]["model"]`（配置里根本没这条）；新建 `_resolve_active_model()`：qwen→`qwen.deep_think_model \|\| qwen.model \|\| 'qwen-plus'`，gemini→`gemini.deep_think_model \|\| gemini.model \|\| 'gemini-2.5-flash'`；cache_key=`{provider}:{真实 model}`；同 provider 不同 model 切换创建新 graph，切回原配置复用；worker `_resolve_active_provider_model` + `_hash_llm_config` 用同路径；3 个 provider switch 测试；(E) **depth 真实端到端生效**——`analysis_history` schema 加 `depth TEXT` 列 + idempotent ALTER；worker 读 `params.depth` 归一化 `{quick,standard,deep}` 未知值回落 standard，写入 result 与 DB；analyzer 加 `set_depth()` 钩子（quick 强制关 iteration、deep 沿用 config、standard 默认）；详情页 Header meta 显示"分析深度"；前端 RadioGroup 文案降级为不绝对化；新增 4 个测试 `test_worker_persists_depth / _default_is_standard / _unknown_falls_back / _quick_disables_iteration` + `test_detail_returns_depth`；(F) **PipelineDAG 契约对齐（NEW）**——前端 STAGES 改为与后端 `PIPELINE_STEPS` 完全一致 7 节点 `market/social/news/fundamentals/debate/risk/decision`（之前是 9 个 `market_agent/sentiment_agent/.../trader` 完全错位）；事件按 `payload.type` 分支：`pipeline_start` 仅设 first→running 不 done，`step_start` 按 step 标 running，`step_done` 按 step id 匹配标 done + next pending→running，未知 step 忽略不前进；废除 `currentIdx++` 顺序 fallback（之前导致 pipeline_start 就跳完所有节点）；(G) **"加入持仓追踪"按钮拆分**——改名"加入观察列表"调 `/api/portfolio/track`（toast 明示"不会自动下单"）+ 新增"按此建议纸面交易"调 `/api/paper/track` 返 `{plan_id, num_orders, triggered}`，按 toast 显示"已生成 / 立即成交 N 单 / N 单待触发"。剩余 ~3h | — |
| 2026-05-01 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.16 | Dashboard / 持仓多租户契约修复（用户 2026-05-01 提，~5h）：(A) **PortfolioDatabase 默认 schema 与运行时对齐**——`_init_tables` 默认 CREATE positions/transactions/daily_snapshots/alerts 全部含 `user_id`；positions `UNIQUE(user_id,ticker)`、daily_snapshots `UNIQUE(user_id,date)`；老 DB 兜底用 `ALTER TABLE ADD COLUMN user_id` + 复合 UNIQUE INDEX `ux_positions_user_ticker / ux_snapshots_user_date`；NULL user_id 自动 backfill 到 first active user；fresh DB 直接调 add_position/sell_position/take_snapshot/add_alert 不再 OperationalError；(B) **`/api/search` 补 user_id 过滤**——positions/transactions/alerts 全加 `user_id=g.user.id`；transactions 不再用 notes 参与匹配（避免私人备注泄露）；analysis_history 共享研究保留；加 `@login_required`；(C) **AlertMonitor + `/api/dashboard.alerts_count`**——`list_alerts(user_id, scope='user'\|'all')` / `check_alerts(...)`；`/api/dashboard` 仅当前用户；后台 cron 显式 `scope='all'`；(D) **持仓交易校验 + 卖空守卫**——新建 `_validate_trade(data, require_existing, user_id)` helper：ticker 字母数字 / shares>0 / price>0 / 卖出必须先有持仓 + shares ≤ 持仓；校验失败 400 不写 transaction；`PortfolioManager.sell_position` 删除"无持仓也写 transaction"兜底，改 raise；(E) **交易记录字段契约统一**——`/api/portfolio/transactions` 返 `action` 大写 (BUY/SELL) + `timestamp` + `date` 别名兼容老前端；前端按大写上色绿/红；(F) **today_pnl 文案/数据修正**——`api_portfolio_summary` 不再让 today_pnl 复用 total_pnl；新增 `_compute_today_pnl(user_id, current_value)` 基于昨日 daily_snapshot 算真实日内变化，无昨日时返 null；前端 `today_pnl != null` 显示"今日 PnL"否则降级"总盈亏"；(G) **测试** `test_fresh_db_add_position_works / _sell / _take_snapshot / _add_alert` + `test_search_positions_isolated / _alerts_isolated / _transactions_notes_not_indexed` + `test_dashboard_alerts_count_only_self` + 校验 6 个用例 + 字段契约 3 个用例。剩余 ~3.5h | — |
| 2026-05-01 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.15 | AI 建议 → paper trade 执行链贯通（用户 2026-05-01 提，~6h）：(A) **SignalLoader 切到 `user_analysis_advice`**——`SignalLoader` 的 `__init__/load/get_one/backfill_all` 全加 `user_id` 入参；主源改读 `user_analysis_advice` 并转成 paper trade 期望字段（双键名 `suggested_position_pct↔position_pct` / `entry_price_low↔entry_low` / `entry_price_high↔entry_high`）；`analysis_history.advice_json` 仅当 `created_by==user_id` 才允许 legacy fallback，非创建者一律 advice 返 `{}`；(B) **`TaskManager._post_analysis_save` 加第 3 步驱动 paper trade**——`save_user_advice` 之后追加 `ensure_ticker_session(user_id) + process_analysis(...)`，传 analysis_id/ticker/date/signal/advice + trade_decision/risk/debate；尝试拿 router 当日价以触发 immediate order；失败仅 warning 不影响 task；(C) **`/api/paper/track` 升级**——`@login_required`，读 `pdb.get_user_advice(g.user.id, analysis_id)` 喂给 `process_analysis`；返 `{ok, session_id, plan_id, num_orders, triggered}`；旧 `manual_track` 行为改为 audit log；(D) **UI 拆分观察列表 vs 纸面追踪**——`AnalysisDetailView` 操作栏"加入持仓追踪"改名"加入观察列表"（仍 `/api/portfolio/track`）+ 新增"按此建议纸面交易"按钮调 `/api/paper/track`；toast 文案区分"已生成 plan / 立即成交 N 单 / N 单待触发"；(E) **收敛主链路 + 列表字段补全**——`PaperTradeStore.list_ticker_sessions` SQL 子查询返 `active_plan_count / pending_orders_count / triggered_orders_count / open_position_shares / last_eod_date / last_skip_reason`；`/api/paper/tickers?mode=forward\|replay` query 过滤；列表页 tab 切换前向追踪/历史回放；(F) **PaperTradeStore schema 自初始化补 v1.3 列**——`_SCHEMA_TRADING_PLANS` 加 `fingerprint/reconfirmed_count/reconfirmed_at/analysis_ids` 4 列；`_init_schema` 幂等 ALTER 老 DB + 创建 `ix_plans_session_ticker_fp` 索引；`migrations/paper_trade_v1_3.py` 保留作历史路径但核心功能不再依赖；(G) **测试**——10 个新增/扩展用例：`test_signal_loader_reads_user_advice / _legacy_fallback_only_for_creator / _normalizes_dual_keys` + `test_paper_track_returns_plan_id / _uses_user_advice_not_shared / _bob_cannot_use_alice_advice / _immediate_execution` + `test_post_analysis_save_drives_paper_trade / _paper_trade_failure_is_swallowed` + `test_fresh_db_save_plan_works_without_migration / _existing_db_idempotent_migration`。剩余 ~4.5h | — |
| 2026-04-27 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.11 | §6.1 R-fix-1~5 全部真修：(1) **R-fix-1** 净值曲线后端：`PortfolioDatabase.get_snapshots(days=None)` 不过滤、`days=N` 改用 `WHERE date >= today - N` 并 `ORDER BY date ASC`；`PortfolioManager.get_history` 透传 None；`/api/dashboard?history_days=all\|<int>` 解析；DashboardPage 默认 `?history_days=all` 让 ChipRow 客户端切片完整序列。功能验证：14 天 snapshot（2026-04-14 → 2026-04-27）all 全显，7d 仅 8 行。(2) **R-fix-2** Dashboard 持仓概览每行加 `+$X.XX` 绝对值列（取 `holding.pnl`，颜色 >0 绿/<0 红/=0 灰），后端 `get_holdings()` 已返回 `pnl` 字段。(3) **R-fix-3** `/api/screen/v3/results/<task_or_result_id>`：先按 task UUID 反查、依 `result_ref` 解析 `screen_results_v2:N` 或 `task_results_generic:N` 两种存储，整数 fallback 兜底；新增 `_result_ref_to_int` / `_params_dict` helpers。三种 ID 形态全部 200，bogus 返 404。(4) **R-fix-4** PaperTradePage `useState(mainTab)` 提到所有 conditional return 之上 — 这是真正的 ErrorBoundary 触发根因（违反 React rules of hooks）；额外加 `[...orders].sort` 不可变、`pct_target_total ?? 0`、`STATUS_ICONS[o.status] ?? <Clock4>`、`start_capital ?? 0` 兜底。(5) **R-fix-5** `/api/quote/history?ticker=&days=` 新建：days→period 映射（5d/1mo/3mo/6mo/1y/2y/5y），返 `{ticker, days, bars: [{date,open,high,low,close,volume}]}`，错误返 200 + 空 bars 不阻塞 UI；AnalysisPage K线主路径切到新端点，`/api/chart/<ticker>` 作 fallback。验收：`tsc --noEmit` 净、`npm run build` ✓、4 项 server-side R-fix live smoke pass、原 P0 81 tests 仍 pass | — |
| 2026-04-26 | [product-architecture-optimization-instructions.md](product-architecture-optimization-instructions.md) | v1.2 | §7 P0 全部 8 项落地：(1) `set_llm_provider` 改用 `_reset_config_dependent_singletons` 并清空 `_analyzer`；(2) `SettingsPage.tsx` 重写为 dotted-path schema（gemini.* / qwen.* / alerts.email.* / alerts.telegram.*），删除旧聚合 provider/model/api_keys/alerts；移除 OpenAI/Anthropic/DeepSeek 字段；(3) 任务 scope 前后端契约统一为 `mine\|shared_research\|all`，未知 scope 在 web 层降级到 mine、在 store 层用不可能谓词兜底，TasksPage 增加"共享研究" tab；(4) `GET /api/tasks/<id>` 接入 `_check_task_ownership`，新增 `_sanitize_shared_task` 按 type 白名单脱敏 params_json；(5) screen_v2/screen_v3/paper_trade/paper_backfill 路由全部显式传 `created_by=g.user.id`；(6) `TaskManager._emit` 直接 emit 到注入的 socketio 并按 `user:{owner}` room 分发，event_emitter 拆分 `persist_event` / `emit_event`；(7) 新建 `tests/conftest.py`：临时 `STOCK_CONFIG_DIR` + 临时 portfolio.db + admin/alice/bob 三 fixture client，`test_llm_provider_api.py` / `test_task_api.py` 改写为登录后业务测试 + 跨用户隔离 case；(8) 前端 npm build 修：新建 `vite-env.d.ts` 声明 css module，TVChart 改 v5 `chart.addSeries(CandlestickSeries/HistogramSeries)`，删 PipelineDAG 死代码，AnalysisDetail 加 `task_id`，PaperTradePage Badge `secondary→muted`，ScreenerV3Page Guru 类型补全 principles/tier。验收：`pytest tests/auth tests/web/test_llm_provider_api.py tests/tasks/test_task_api.py tests/tasks/test_task_manager.py` 81 passed；`npm run build` ✓ | — |
| 2026-04-26 | [product-architecture-optimization-instructions.md](product-architecture-optimization-instructions.md) | v1.1 | 验收回归补充指令：合并本轮代码验证结果，新增 §7 P0 修复清单（LLM 切换 `_invalidate_singletons` 运行时错误、Settings dotted path schema、任务 scope 泄漏、任务详情权限、screen_v2/v3 created_by、TaskManager 事件、鉴权测试夹具、前端 build）+ P1 产品/架构建议 + 可复制给实现会话的执行指令 | — |
| 2026-04-21 | [ui-react-island.md](ui-react-island.md) | v1.0 | UI React Island 迁移方案：新建 `web/frontend/`（Vite 8 + React 19 + TS + Tailwind v4）+ `vite_helpers.py` 读 manifest + `layout.html` 共享 Jinja 壳 + 4 个 island entry（screener-v3 / tasks / paper-trade / dashboard）+ 共享 `lib/api.ts`（CSRF/fetch）+ `lib/socket.ts`（SocketIO + catch-up）+ `components/ui/` 14 个 shadcn 风格组件 + `components/shared/ProgressStream` 复用 unified-progress envelope + manualChunks（react-vendor/radix/ui 三共享 chunk）+ Railway Nixpacks 加 nodejs-18 + 7 简单页保留 Jinja + 7 Phase 实施计划（~28h）| — |
| 2026-04-21 | [ui-react-island.md](ui-react-island.md) | v2.0 | 完整迁移：新增剩余 11 页页面规格（Portfolio / History / Alerts / Reports / Backtest / Paper list / Analysis 列表+详情 / Settings / Login / Register / Reset）+ 新增共享组件（`<AppShell>`、`<DataTable>`（@tanstack/react-table）、`<Form>`（react-hook-form+zod）、`<EChartsPanel>`、`<AuthCard>`、`<SettingsTabs>`、`<FilterBar>`）+ Phase 8-19 共 ~47h 实施计划 + Phase 18 废弃旧 index.html / app.js / Bootstrap（减 5000+ LOC）+ v2.0 新依赖清单（@tanstack/react-table / react-hook-form / zod / echarts / react-markdown）| — |
| 2026-04-24 | [ui-react-island-pages.md](ui-react-island-pages.md) | v1.0 | 剩余 11 页完整施工图（独立文档）：每页 10 子节（路由 / 当前 Jinja 诊断 / 桌面+移动 mockup / TS 数据模型 / 页面状态机 / 组件树 / 后端 API 契约 / 交互清单 / 空态loading error 边界 / 移动端+a11y）+ 7 共享组件详规（AppShell / DataTable / Form / EChartsPanel / AuthCard / SettingsTabs / FilterBar）+ 页面矩阵总览 + 10 个新增后端端点汇总 + 共享 types.ts 索引 + v2.0 复用审计（自写 ~3600 LOC，库替代 ~2500 LOC）| — |
| 2026-04-24 | [ui-migration-validation.md](ui-migration-validation.md) | v1.0 | 架构级迁移完整验证计划：6 层验证（L0 冒烟 / L1 基础 / L2 功能回归 / L3 跨模块集成 20 场景 / L4 数据完整性 snapshot+invariant / L5 对抗性）+ 新增 `stock_trading_system/validation/` 目录（snapshot / compare / invariants / cross_user_access / sign_off 脚本）+ 迁移前数据快照机制 + 业务不变量 SQL 检查（10 条） + 执行流程（pre-flight / 上线日 / post-deploy）+ 签字凭证生成 + 回滚预案 + 性能基线对比 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.0 | 迁移回归修复方案：合并 11 页功能回归（59 项：8 CRITICAL / 20 HIGH / 23 MEDIUM / 1 LOW，含 Portfolio 卖出消失、Paper-trade 权益图消失、Analysis 7-tab 报告全失、Dashboard stat 溢出 等）+ 移动端适配 15 项（与 mobile-optimization.md 断点不一致）+ R-1~R-7 七 Phase 实施 ~42h + 横切组件升级（Stat 强制 --fs-stat clamp / ChartPanel 共享 / form-row-mobile 套接 / tabs-scrollable / 表格→卡片）+ 严格"只补不改"约束 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.1 | 补充修订：(1) paper-trade 列表页 PT-P1-2 升级 P0（实测 `/paper-trade` 路由不存在，整个入口缺失，新建 island/paper-trade-list/） (2) 新增 §4.12 菜单重组方案：6 大组（概览/分析/选股/持仓/纸面交易/系统）+ 11 叶子；含桌面 Sidebar 分组组件 + MobileTabbar 5+更多 sheet 设计；总工时 ~42h → ~49h；CRITICAL 8 → 9 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.2 | 补充修订：Tasks 4 项升级 P0 CRITICAL（T-P0-1 历史无分页/无限滚动 + T-P0-2 类型 chip-row 8 类 + T-P0-3 scope tab 我的/全部 + T-P0-4 9 类 task → 落地页 URL 映射表 `lib/tasks.ts::getTaskResultUrl(task)` + T-P0-5 详情操作齐全）；新增 R-3b Phase（Tasks 改造 ~4h）；总工时 ~49h → ~53h；CRITICAL 9 → 14 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.3 | 补充实测发现的 4 个 P0：(1) PT-P0-3 paper-trade ticker 详情空白 bug（pathname 末尾斜杠 → ticker 解析为空）；(2) T-P0-6 任务中心空白 bug（后端 `{items}` vs 前端期望 `{tasks}` schema 不匹配）；(3) SE-P0-1 设置缺 GEMINI_API_KEY + QWEN_API_KEY 字段；(4) MS-P0-1~4 整个 Model-Switch UI 缺失（后端 100% 已就绪，前端 0 组件）—— 新增 §4.13 LLMSwitcher 详细规格（4 状态：active/缺 key/env 锁定/loading），挂 NavTopbar；R-1 含 LLMSwitcher（+3h）+ R-3b 含 schema 修复（+1h）+ 新增 R-5b（Settings keys 1h）；CRITICAL 14 → 22；总工时 ~53h → ~57h | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.4 | 实施进度审计：R-1~R-7 commits（6e583b4..5370863）落地后实测 22 P0 中 18 ✅ DONE / 6 ❌ MISSING（MS-P0-1~4 LLMSwitcher 全套 + SE-P0-1 Gemini/Qwen key + A-P0-3 Pipeline DAG）+ 1 ⚠ PARTIAL（T-P0-6 schema）。新增 §0 实施进度状态表 + §6.1 R-1.x 收尾批次合并指令（~5h：LLMSwitcher 3h + Settings keys 10min + Pipeline DAG 2h + Tasks schema 验证 5min）；完成 R-1.x 后 P0 闸门绿 → 可 sign-off | `6e583b4..5370863` (R-1~R-7) |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.4.1 | 修订 T-P0-6 状态：实测前后端均已对齐（app.py:1713 双字段 + TasksPage.tsx:70 fallback 已含 items），由 PARTIAL → DONE。DONE 19，余 6 项（仍是 LLMSwitcher 4 + Settings keys 1 + Pipeline DAG 1）。R-3b.1 标 NO-OP，移出工作清单 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.5 | 实测发现 R-1~R-7 仍有 6 项 P0 + 1 错位需修：(1) HE-P0-1 History 跳转 analysis 详情 URL `/analysis/analysis_history:33` 类损坏（int vs string id 路由不匹配 → 404）；(2) SV3-P0-1 Screener V3 结果页**完全未建**（仅表单，无 result rendering）；(3) A-P0-3a Pipeline DAG 位置错（仅表单视图有，详情页 161-285 无）；(4) A-P0-4 K线应用 TradingView widget 替代 ECharts candlestick；(5) A-P0-5 缺新闻+基本面 quick-info 侧卡；(6) B-P0-1 Backtest 结果详情页未建（仅表单，跳 /tasks）；(7) D-P0-5 dashboard /backtest-v2 死链。新增 6 个 R-1.x 子任务（~7h），R-1.x 总剩余 5h → 12h；总 P0 缺失 6 → 12 项 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.6 | 性能 PERF-P0-1：实测 Dashboard / Portfolio 加载 ~3s 慢，根因后端 `get_pnl()` / `get_allocation()` 都内部调 `get_holdings()` → 单次 HTTP request 拉 2 次全量实时价格。新增 R-perf（~1.5h）：Layer A `PortfolioManager.get_holdings()` 加 Flask `g` request-scoped memoize（一次 request 只拉一次价，~30min，砍半）+ Layer B `data_manager.get_price()` 复用 LocalCache 加 30s TTL（30 秒内复访 < 200ms，~1h）。总剩余工作 12h → 13.5h。固化 doc-first 工作流到长期记忆 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.7 | 实测两个 P0：(1) PT-P0-4 paper-trade ticker 详情完全空白（PaperTradePage.tsx:68 pathname.split 仍老代码，v1.3 PT-P0-3 audit 误判 DONE，需真修 regex/filter(Boolean) + 加 React ErrorBoundary）；(2) A-P0-7 AI 分析提交后跳 /tasks/<id>，用户看不到 Pipeline DAG 实时 + 7-tab 流入，需改跳 /analysis/<task_id>，配合 R-5.1 显示 DAG + 占位 tabs，完成后切 7-tab。新增 R-3.1（30min）+ R-5.4（1h）；总剩余 13.5h → 15h | — |
| 2026-04-26 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.8 | 功能补强 2 项：(1) D-FEAT-1 仪表盘净值曲线自动回溯（后端 get_history(days=None) 全量，前端 range switcher 全部/1Y/6M/3M/1M/7D + dataZoom，从最早 daily_snapshot 起）；(2) P-FEAT-1 持仓表加"盈亏 $"绝对值列（后端 get_holdings.pnl 已就绪，前端补显示，dashboard 概览 + 移动 m-card 同步），与"盈亏 %" 并列。共 ~2h；剩余 15h → 17h | — |
| 2026-04-26 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.9 | A-P0-8：实测 AI 分析提交后页面完全空白。运行中态完整布局（合并强化 v1.7 A-P0-7 + R-5.1/2/3）：跳 /analysis/<task_id> 立即显示 K线 + 三列侧卡（新闻/基本面/多空比，独立拉 API） + Pipeline DAG 实时（订阅 agent_stage_done） + 7-tab 占位 skeleton（agent 完成后逐个填 markdown），完成后 history.replaceState 切 /analysis/<analysis_id>。~3h（含在 R-5.x 工作量）；剩余 17h → 20h | — |
| 2026-04-26 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.10 | 生产环境实测仍 5 项未真修（之前轮次号称 DONE 但漏改）：(1) D-FEAT-1 后端 `get_history(days=30)` 硬编码，前端 ChipRow 无效；(2) P-FEAT-1 Dashboard 持仓 top3 漏加绝对值列；(3) SV3-P0-1 前端 task_id (UUID) 拉 `/api/screen/v3/results/<id>` 路由期望整数；(4) PT-P0-4 PaperTradeContent 内部抛错触发 ErrorBoundary，需 null check；(5) R-5.3 TVChart 已挂但 klineData 空，需 `/api/quote/history` 端点。新增 R-fix-1~5（~3h）；剩余 20h → 23h | — |
| 2026-04-20 | [unified-progress.md](unified-progress.md) | v1.0 | 统一实时进度系统（后端 + 前端）：新建 `task_events` 表 + `emit_event()` 统一入口替代散落的 `socketio.emit` + Per-user SocketIO room 隔离 + `GET /api/tasks/events?since=<seq>` 断线续传 + `GET /api/tasks/running` reconnect 同步 + 前端 `ProgressStream` 组件（3 布局 compact/detail/inline-badge × 5 态 connecting/streaming/stalled/disconnected/done）+ 5 页面统一挂载（任务中心/分析/screener-v3/batch/backtest）+ 断线 banner UX + 全局连接指示灯 + 7 天 events 保留 + 11 种 task 事件标准 envelope | — |
| 2026-04-19 | [paper-trade.md](paper-trade.md) | v1.3 | UX / 数据 surface 5 处修正：F1 Plan 内容指纹 dedup（fingerprint + reconfirmed_count + analysis_ids）+ F2 底部 "AI 最终决策" 改渲染 analysis_history.trade_decision 全文 + F3 新增 analysis_history.executive_summary 列并用 `with_structured_output(ExecutiveSummary)` 抽取（禁写字面量 "regex 解析"）+ F4 日度图表 ECharts 双 grid + drawdown markArea + visualMap + F5 "时间轴 / 策略历史" 合并为 "执行记录" 单 tab 内嵌双视图切换（按 Plan / 按 Event） | — |
| 2026-04-19 | [screener-v3.md](screener-v3.md) | v1.0 | 智能选股 V3：新建 `stock_trading_system/screener/v3/` + 14 大师 agent（12 位 virattt clean-room 重写 + Marks/Dalio 自建）+ 并发 Semaphore(10) + `(ticker,guru,date)` 缓存 + 成本预估动态校准 + Round-table 辩论（Top 5）+ 流式 WebSocket + 预选配置面板 + 经典阈值模式兼容保留（v2 gurus 不动）+ `BaseGuru` 接口保留 aggregator/regime 零改动 | `042a98d` (P0), `8c3ceea` (P1), `40bbad4` (P2+3), `5e0ca93` (P4), `141f8fa` (P5), `cca0ed1` (P6) |
| 2026-04-19 | [screener-v3.md](screener-v3.md) | v1.1 | 复用审计修订（依据 [engineering-principles.md](../engineering-principles.md)）：§4.1 改用 LangChain `ChatOpenAI.with_structured_output(GuruSignal)` 替代自写 JSON 解析；§4.3 改用 `tenacity` 替代自写重试循环；§4.9 复用 TradingAgents `bull_researcher` / `bear_researcher` / `conservative_debator` / `reflection` 作辩论图基底；新增 §12 复用清单；自写 LOC ~31% 下降 | — |
| 2026-04-19 | [engineering-principles.md](../engineering-principles.md) | v1.0 | 新建跨文档原则：复用优先（L0 项目内 → L1 依赖库 → L2 vendor → L3 clean-room → L4 自写）+ 应用规则 + 4 份近期设计审计矩阵 + 对 screener-v3 产生 3 处 P0 修订 | — |
| 2026-04-19 | [multi-tenant.md](multi-tenant.md) | v1.0 | 多租户：新建 `stock_trading_system/auth/` 模块（password/session/invite/repository/decorators/bootstrap）+ 3 张新表（users/invite_codes/user_settings）+ `analysis_bookmarks` 共享收藏 + 6 类私有表 + paper 子表 user_id FK 迁移 + Flask session 认证 + admin 首启自动迁移脚本 `migrations/to_multi_tenant.py` + model-switch router 签名加 user_id + 前端 login/register 两页 | `af6965c` (P0), `01354f3` (P1), `1ac99a5` (P2), `f69b526` (P3), `e1db42b` (P4), `5d32293` (P5) |
| 2026-04-19 | [mobile-optimization.md](mobile-optimization.md) | v1.0 | 移动端统一优化（11 页 × 3 断点）：设计 tokens（断点/字号 clamp/触摸 44px）+ 7 个通用组件（form-row-mobile / num-responsive / table-to-cards / tabs-scrollable / collapse-row / btn-group-wrap / chip-row）+ 11 页逐项清单。含 screener v1.2 移动端专项 | `e198acd` (P0), `1bfd290` (P1), `2380d09` (P2) |
| 2026-04-18 | [model-switch.md](model-switch.md) | v1.0 | 全局模型切换：新建 `stock_trading_system/llm/` 模块（router + client + constants），analyzer graph 按 provider 缓存，screener V2 call site 改用 LLMTextClient 抽象，Nav 下拉 + `/api/settings/llm-provider` GET/POST | `f682855` (P1), `dffbab4` (P2), `a549b90` (P3), `e21f6ac` (P4) |
| 2026-04-18 | [self-iterating-agents.md](self-iterating-agents.md) | v3.0 | 自我迭代能力模块（包裹 TradingAgents）：最大化复用版。Agent Scorer(唯一核心新模块) + Darwinian 权重(采用 atlas-elenchus 常量) + Meta Agent(采用 atlas MUTATOR_SYSTEM_PROMPT) + A/B 验证复用 paper trade sessions。仅 2 张新表 | `e083788` |
| 2026-04-18 | [batch-analyze-holdings.md](batch-analyze-holdings.md) | v1.0 | 一键持仓分析方案：batch_analysis worker + 逐只推送 + 跳过近期 + 进度面板 | — |
| 2026-04-12 | [ui-ux-redesign.md](ui-ux-redesign.md) | v1.0 | 初版 UI/UX 重设计方案：9 页面线框图、移动端适配、组件规范（8 大类） | — |
| 2026-04-12 | [technical-design.md](technical-design.md) | v1.0 | 初版技术方案：前端改造 + 后端 API 补全 + 回测引擎 + PRD 追踪矩阵 | — |
| 2026-04-15 | [architecture-upgrade.md](architecture-upgrade.md) | v1.0 | 架构升级方案：Qwen 主导数据层 + TradingView 图表 + 异步任务系统 | — |
| 2026-04-15 | [architecture-upgrade.md](architecture-upgrade.md) | v1.1 | 补充：三层数据策略、TaskManager 设计、缓存策略、配置扩展 | — |
| 2026-04-15 | [screener-v2.md](screener-v2.md) | v1.0 | 智能选股 V2：8 Agent 并行打分 + 8 Guru 哲学 + Regime 自适应权重 | — |
| 2026-04-16 | [screener-v2.md](screener-v2.md) | v1.1 | 修订：NL 驱动优先，strategy chip 退化为辅助提示 | — |
| 2026-04-16 | [paper-trade.md](paper-trade.md) | v1.0 | 纸面交易方案：AI 信号回放 + 前向追踪 + 权益曲线 | — |
| 2026-04-16 | [paper-trade.md](paper-trade.md) | v1.1 | 修订：独立菜单 + 可选留痕 + 个股绑定 analysis_tracked 表 | — |
| 2026-04-16 | [paper-trade.md](paper-trade.md) | v1.2 | 修订：全量自动追踪（所有非 ERROR 分析自动进入默认 session） | — |
| 2026-04-15 | — | — | Phase A-E 架构升级实施完成（commit `034efe0`） | `034efe0` |

## 决策摘要

| 决策 | 选择 | 原因 |
|------|------|------|
| K 线图方案 | TradingView Widget（展示）+ ECharts（净值/回测） | TV 零数据成本，专业体验 |
| 回测数据源 | yfinance + LocalCache，不走 Qwen | 精度 + 可复现性 |
| 异步任务 | ThreadPoolExecutor + SQLite，非 Celery | 单人项目，零运维 |
| 数据主源 | Qwen（语义/判断/单点查询），Local（精确结构化） | LLM 做语义，专业 API 做数据 |
| 选股 V2 NL 优先 | 自然语言输入为主，strategy 为辅 | 用户反馈 |
| 纸面交易追踪 | 全量自动，非 opt-in | 100% 覆盖率，AI 效果可量化 |
