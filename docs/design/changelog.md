# 设计方案变更记录

| 日期 | 文档 | 版本 | 变更内容 | 关联 Commit |
|------|------|------|---------|-------------|
| 2026-05-04 | [model-switch.md](model-switch.md) | v1.2 | **Settings env-lock 提示**：用户实测保存 qwen.model=qwen3.6-max-preview 不生效（重启后回 qwen3-max-preview）。根因 config/settings.py:55 `_apply_env_overrides` 把 `QWEN_MODEL` env 映射到 `config[qwen][model]`，**优先级高于 user YAML**（line 105 注释明文 env > user > default），Railway 上 `QWEN_MODEL=qwen3-max-preview` 每次 `load_config()` 把刚保存的 `.6` 覆盖回去，UI 写盘成功但读取阶段被吞。**修复（不动 env 优先级机制，仅补 UI 反馈）**：(A) `/api/settings` GET 增 `env_locked: {dotted_path: true}` 字段镜像 `_apply_env_overrides` env_map；(B) `SettingsPage.tsx` qwen.model 字段在 env_locked 时 disable + 显示琥珀色 hint：当前由环境变量 `QWEN_MODEL` 锁定，UI 修改无效，请到 Railway Variables 改值后重启或删除该 env var；(C) handleSave POST 前 drop env-locked key，避免后端写盘成功但下次 load_config 立即覆盖造成的迷惑性 toast。**用户操作**：Railway Variables 把 `QWEN_MODEL` 改成 qwen3.6-max-preview 或删除该 env var，重启 service。**不动** env > user > default 优先级 / `_apply_env_overrides` env_map / WRITABLE_SETTING_PATHS / 写盘逻辑。`pytest tests/web/test_settings_api.py -q` 4 passed；`npm run build` ✓ | — |
| 2026-05-04 | [model-switch.md](model-switch.md) | v1.1 | **配置**: Qwen 默认模型切到 `qwen3.6-max-preview`（用户 2026-05-04 报告新版上线）。**改动**：(A) `default_config.yaml` `qwen.model`: `qwen-plus` → `qwen3.6-max-preview`；`iteration.model`: `qwen3.6-plus` → `qwen3.6-max-preview`；`iteration.fallback_model` 故意保留 `qwen-plus` —— preview 模型 5xx/429 时退回稳定线网，避免双 preview 同时挂掉时 Meta Agent 全停。(B) 源码默认值（YAML 缺值时兜底）跟随更新：`llm/client.py` / `llm/resilient_chat.py` / `agents/analyzer.py` / `data/qwen_provider.py` / `agents/iterative/config.py` 的 `qwen-plus` / `qwen3.6-plus` 默认全部 → `qwen3.6-max-preview`，`iterative.fallback_model` 默认仍为 `qwen-plus`。(C) `web/frontend/.../SettingsPage.tsx` qwen 模型 placeholder → `qwen3.6-max-preview`。(D) `app.py` 缓存键说明注释从 `qwen:qwen-plus` → `qwen:qwen3.6-max-preview`。**不动** 测试 fixture 中的 `qwen-plus` 显式值（测试是检验逻辑不是检验默认）/ provider router / cache_key 算法 / Gemini 路径。验收：`pytest tests/test_iterative.py tests/test_qwen_extensions.py tests/llm/ tests/agents/test_analyzer_provider_switch.py tests/tasks/test_workers_qwen_errors.py -q` 139 passed（1 pre-existing 失败与本变更无关：iterative 测试的 mock.process_signal 返 MagicMock，clean 树上同样失败）；`npm run build` ✓；`npm test` 61 passed。**用户可在 settings UI 或 ~/.stock_trading/config.yaml 自行覆盖**（如想保留 qwen-plus）；自写 ~30 LOC（无新测试，contract 不变） | — |
| 2026-05-04 | [paper-trade.md](paper-trade.md) | v1.4 | **Paper-Trade（个股详情页结构化决策同步）**：用户截图 `/paper-trade/MSFT` 暴露三处与 [analysis-rendering v1.0+](analysis-rendering.md) 8 tab 卡片体系脱节的旧渲染。**问题清单**：(P-1) 「当前策略」卡 [`PaperTradePage.tsx:155-180`](../../stock_trading_system/web/frontend/src/islands/paper-trade/PaperTradePage.tsx) 用 [`plan_parser.py`](../../stock_trading_system/strategy/paper_trader/plan_parser.py) regex 解析的 `plan.thesis` + 暴露 `parse_method=regex` / `Plan #x` 实现细节，与新 OverviewCard / Decision banner / Executive Summary 视觉**完全不一致**；(P-2) 「AI 最终决策」卡 [`PaperTradePage.tsx:240-256`](../../stock_trading_system/web/frontend/src/islands/paper-trade/PaperTradePage.tsx) `<div whitespace-pre-wrap>{data.latest_trade_decision}</div>` 大段 raw markdown，与详情页 8 tab 结构化卡形成**双套**视觉语言；(P-3) 「执行记录·按 Plan」 [`PaperTradePage.tsx:432-466`](../../stock_trading_system/web/frontend/src/islands/paper-trade/PaperTradePage.tsx) `<PlanHistory>` 每行 `Plan #id + thesis + <details>markdown</details>`，但用户心智 = **该股票的 AI 分析历史列表**，需要每行可点跳转 `/analysis/<id>` 看完整 8 tab。**根因**：v1.3 F3 引入 `with_structured_output(ExecutiveSummary)` + `analysis_history.rendering_json` / `executive_summary` 列后，[`/analysis/<id>`](../../stock_trading_system/web/frontend/src/islands/analysis/AnalysisPage.tsx) 详情页已完全切到结构化卡，但 `/paper-trade/<ticker>` 个股详情页**没有同步接通** —— `api_paper_ticker_detail` 仍下发 raw `latest_trade_decision` + `plan.thesis`，前端按旧路径渲染。**修法**（后端 ~70 LOC + 前端 ~150 LOC，0 业务逻辑新增）：**[后端]** 新增 `_signal_to_tri_state(raw)` mirror 前端 [`signalLabel`](analysis-inbox.md#v13)（Sell 优先以避免 "underweight" 被 "buy" 误中）+ `_rendering_summary_for_analysis(analysis_id, db)` helper（读 `_parse_rendering(rendering_json).summary` + `analysis_history.executive_summary`，输出 `{analysis_id, signal_raw, signal_tri, rating, action_direction, executive_summary, one_line_takeaway, confidence_pct, created_at}`，缺失 rendering 静默降级）；`api_paper_ticker_detail` 给 `active_plan` 附加 `analysis_summary` + `plan_history[]` 删除 `trade_decision` raw 字段改为 `analysis_summary` + 新增顶层 `latest_analysis_summary` 字段（active_plan 缺失时 fallback）；`latest_trade_decision` 字段保留向后兼容但置 None。**[前端 A]** 重写「当前策略」卡为 `<ActiveStrategyCard>`：Decision banner = signalVariant 颜色 + signalLabel 三态文本 Badge + Rating Badge + ConfidenceMeter + "查看完整分析 →" 链接 → `/analysis/<analysis_id>`；`📍 action_direction` 一句话方向；Executive Summary 段（`border-l-4 border-primary/60 bg-primary/5` + `<ScrollText>` icon，复用 [analysis-rendering v1.6 OverviewCard](analysis-rendering.md#v16) 视觉）；Plan 元信息底栏简化为 `策略 #id · 创建时间 · 持有 N 个月`，**不再暴露 parse_method / regex**。**[前端 B]** 删除「AI 最终决策」卡整段（信息已被 28.2.2 + 28.2.4 覆盖；保留只会形成双轨）。**[前端 C]** 「执行记录·按 Plan」改为 `<AnalysisHistoryList>`：每行 = signalVariant Badge + signalLabel 文本 + Rating Badge + 置信 % + action_direction + executive_summary `line-clamp-2`；整行可点 → `/analysis/<analysis_id>`；首行（最新）打 `★ 当前` 标记；视觉复用 [analysis-inbox v1.18 R-fix-12G HistoryPage](analysis-inbox.md) 行风格（divide-y + hover bg-accent + 三态 Badge）。**「按 Event」tab 不动**。**严格不动**：`paper_trade_plans` schema / `session_store.save_plan` 事务 / supersede 逻辑 / `event_executor.py` 触发链 / `plan_parser.py` regex（作为 LLM 失败 fallback 仍保留）/ `latest_advice` per-user 隐私边界（[v1.18 R-fix-12](analysis-inbox.md)）/ `events`/`dailies`/`trades`/`active_orders`/`session` 字段 / `analysis_history.rendering_json` / `executive_summary` 列 schema / OverviewCard / 8 tab Card / RoundtableResult / screener 链路。**复用优先**：L0 复用 `_parse_rendering` / `get_analysis_by_id` / `get_user_advice` / 前端 export `signalLabel`+`signalVariant`+`<ConfidenceMeter>` + shadcn Badge + lucide ScrollText；L1 无新增。**测试**：`tests/frontend/paper-trade/active-strategy-card.test.tsx`（5 vitest case：rating+tri-state+executive 渲染 / fallback 路径 / 双 null 不渲染 / `/analysis/<id>` 链接 / 不出现 "AI 最终决策" 文案）+ `tests/test_paper_trade_ticker_detail_v1_4.py`（active_plan 携带 analysis_summary / plan_history 不再含 trade_decision raw md / `_signal_to_tri_state("Underweight")=="Sell"`）；手动回归 `/paper-trade/MSFT` 三处对照截图。 | — |
| 2026-05-03 | [mobile-react-island-fixes.md](mobile-react-island-fixes.md) | v1.1 | **Mobile（v1.0 验收残留补丁，3 项）**：v1.0 主体通过、`npm run build` ✓、`npm test` 5 文件 61 测试全绿后，人工验收发现 3 个小风险，本期一次性补齐。**问题清单**：(P-1) [PortfolioPage.tsx:214](../../stock_trading_system/web/frontend/src/islands/portfolio/PortfolioPage.tsx) 移动持仓卡片底部三列 `flex justify-between` 的成本/盈亏/现价行，320px 视口配 6 位以上美元值会重叠或裁切；(P-2) [ScreenerV3Page.tsx:1483](../../stock_trading_system/web/frontend/src/islands/screener-v3/ScreenerV3Page.tsx) 选股记录行右侧"查看 + 复制配置重跑"按钮组用 `ml-auto flex gap-1`，父级虽 flex-wrap 让按钮 div 换行，但新行 ml-auto 失效、按钮仍紧凑、触摸目标偏小；(P-3) [index.css:125-126](../../stock_trading_system/web/frontend/src/styles/index.css) `--fs-stat`/`--fs-hero` v1.0 漏改、仍为 `clamp(...vw...)`，导致 Dashboard 4 张统计卡数字字号在 360–430 视口仍连续抖动。**方案**：(P-1) 金额行外层加 `flex-wrap gap-x-3 gap-y-1`，三值放得下保持 `justify-between` 桌面排布、放不下自动换行不溢出；不做 truncate（截断金额比换行更危险）；(P-2) 按钮组改 `flex gap-1 w-full sm:w-auto sm:ml-auto`，两按钮加 `flex-1 sm:flex-initial` —— 移动端两按钮平分整行宽度、触摸目标自动达标，桌面 (`sm:` ≥640px) 恢复 `ml-auto` 紧凑布局；(P-3) `--fs-stat: 20px`（grep 实际消费方 [stat.tsx:27](../../stock_trading_system/web/frontend/src/components/ui/stat.tsx)，Dashboard 4 卡数字，原 320px 16px / 桌面 22px → 固定 20 小屏稍大但不撑破，桌面 -2px 可接受），`--fs-hero: 28px`（React 无消费方，保留兜底未来）。**复用优先**：沿用 v1.0 既有 token 体系，不新增 CSS 变量、不引入 hook、不改 `Stat` 组件本体；`style.css` 遗留 Bootstrap 模板的同名定义不动（模板与 React 是两条独立栈）。**严格不动**：桌面端视觉 baseline、Stat 组件 API、ScreenerV3 / Portfolio 业务逻辑、后端 API、v1.0 已落地的 8 处改动。**验收**：`npm run build` ✓ + `npm test` 全绿；DevTools 320px 模拟 Portfolio 移动卡（长金额）、ScreenerV3 选股记录行（窄屏按钮组）、Dashboard 统计卡（拖动 360→430 字号不抖动）。 | — |
| 2026-05-03 | [mobile-react-island-fixes.md](mobile-react-island-fixes.md) | v1.0 | **Mobile（React Island 时代二轮修复）**：审计发现 React Island 全量迁移后，首轮 [mobile-optimization.md](mobile-optimization.md) v1.0 沉淀的部分组件类（`tabs-scrollable` 等）只在少数页面被显式引入，其余页面退回组件默认值，移动端出现 6 个新回归。本期一次性补齐，不重做 token 体系。**问题清单**：(M-1, P1) [AppShell.tsx:7](../../stock_trading_system/web/frontend/src/components/shared/AppShell.tsx) 主内容只 `pb-16`，但 [Sidebar.tsx:158-160](../../stock_trading_system/web/frontend/src/components/shared/Sidebar.tsx) MobileTabbar 额外加 `env(safe-area-inset-bottom)`，iPhone 全面屏导航实际超过 64px → 列表末尾 / 提交按钮被遮挡；(M-2, P1) [dialog.tsx:34](../../stock_trading_system/web/frontend/src/components/ui/dialog.tsx) `DialogContent` 是 `fixed + w-full + max-w-lg + p-6`，无 `max-height` / `overflow-y-auto` / 移动端边距 → 持仓买卖 / 警报新建弹窗 footer 在小屏 / 横屏被推到屏外；(M-3, P2) [ReportsPage.tsx:277](../../stock_trading_system/web/frontend/src/islands/reports/ReportsPage.tsx) 用裸 `<Markdown>` 而不是 `MarkdownBody`，且 `prose` 容器无表格 / 代码块 / 长链接横向滚动 → LLM 宽表 / fenced code / 长 URL 撑破容器；(M-4, P2) [tabs.tsx:14](../../stock_trading_system/web/frontend/src/components/ui/tabs.tsx) `TabsList` 默认 `inline-flex` 无横滑，AnalysisPage 单独显式加了 `tabs-scrollable`，PortfolioPage / AlertsPage 没用 → 计数变大后小屏溢出；(M-5, P2) [DashboardPage.tsx:347](../../stock_trading_system/web/frontend/src/islands/dashboard/DashboardPage.tsx) 净值卡头部 `CardTitle + 6 档 ChipRow + 重新计算 Button` 同一行，[DashboardPage.tsx:424](../../stock_trading_system/web/frontend/src/islands/dashboard/DashboardPage.tsx) 持仓行长 ticker / 大金额会挤压；(M-6, P3) [index.css:17-23](../../stock_trading_system/web/frontend/src/styles/index.css) 全局字号 token 用 `clamp(... vw ...)`，360–430 视口字号连续抖动，交易密集数字尤其敏感。**方案**：(M-1) 新增 `--mobile-tabbar-height: calc(64px + env(safe-area-inset-bottom, 0px))`（≥768px 置 0），AppShell `<main>` 与 MobileTabbar 共读同一变量；(M-2) DialogContent 改 `w-[calc(100vw-24px)] max-h-[calc(100dvh-24px)] overflow-y-auto overscroll-contain p-4 sm:p-6`，沿用 `max-w-lg` 桌面尺寸；(M-3) 升级 [MarkdownBody.tsx](../../stock_trading_system/web/frontend/src/components/shared/MarkdownBody.tsx) 为唯一 Markdown 入口（`components` 覆写 `pre/table/a` + `mdx-body` 局部 CSS scope），ReportsPage 改用之并删除 react-markdown 直接 import；(M-4) [tabs.tsx](../../stock_trading_system/web/frontend/src/components/ui/tabs.tsx) `TabsList` 默认追加 `flex max-w-full overflow-x-auto scrollbar-none`，全站受益；(M-5) DashboardPage 净值卡 + 持仓行加 `flex-wrap` + `min-w-0` + `truncate`；(M-6) 字号 token 去 vw 化（12/13/14/15/17/20/24），保留 `--fs-stat` / `--fs-hero` 大字号 clamp（专门给单卡大数字用，撑破风险低）。**复用优先**：沿用首轮 [mobile-optimization.md](mobile-optimization.md) 的 `--touch-min` / `--page-pad-mobile` token、现有 `MarkdownBody` / `ChipRow` / `Tabs` / Radix `DialogContent` 组件，不新增 token 文件、不新增 hook、不引入 viewport 检测。**严格不动**：桌面端 (≥768px) AppShell / Tabs / Dialog 视觉、各页面业务逻辑、后端 API、首轮 7 个通用类。**验收**：iPhone SE 2 / 14 / 14 Pro Max / SE 横屏 / Pixel 7 五机型 + DevTools 375/390/430 三视口；`npm test` 全绿、`npm run build` ✓；桌面 pixel-match baseline 无变化。 | — |
| 2026-05-03 | [analysis-rendering.md](analysis-rendering.md) | v1.9 | **UI**: `/analysis` 列表行 + 详情页 Header `<Badge>` 文本统一三档 Buy/Sell/Hold（用户 2026-05-03 截图反馈，~30min）。**问题**：截图实测 Badge 直显原始 `row.signal` / `canonicalSignal(detail)`（含 `Overweight` / `Strong Sell` / `BUY` / `bullish` / `Hold` / `Neutral` 等 7 档评级 + 中文混杂），用户读不清；`signalVariant(...)` 已把所有变体映射到 4 色（buy/sell/hold/default）但显示文本没归一化。**修复**（仅前端 +1 helper +2 替换 +1 测试文件）：(A) `AnalysisPage.tsx` `signalVariant` 旁新增导出 `signalLabel(signal)` 三档归一化 helper —— Sell 优先（`underweight/减仓/REDUCE/bearish/sell`）→ Buy（`overweight/加仓/ADD/bullish/buy`）→ Hold 兜底（`hold/neutral/wait/中性/未知/空`），全部小写+trim 比较；(B) 列表行 `<RunningRow>` Badge 文本从 `{row.signal || "—"}` 改为 `{signalLabel(row.signal)}`；(C) `<AnalysisDetailView>` Header Badge 文本从 `{canonicalSignal(detail) || "N/A"}` 改为 `{signalLabel(canonicalSignal(detail))}`；颜色 variant 仍由 `signalVariant(canonicalSignal(detail))` / `signalVariant(row.signal)` 决定（不动）；(D) 新建 `__tests__/signal-label.test.tsx` 25 case：Buy 7 case（Buy / Strong Buy / Overweight / BUY / bullish / 加仓 / ADD）+ Sell 7 case（Sell / Strong Sell / Underweight / SELL / bearish / 减仓 / REDUCE）+ Hold 9 case（Hold / HOLD / Neutral / neutral / WAIT / 中性 / 空 / whitespace / unknown junk）+ nullish 2 case（null / undefined）。**严格不动** OverviewCard 内 `<RatingBadge rating={...}>` 7 档评级文本（评级维度独立）/ `analysis_history.signal` DB 列原文 / `decision_action` 字段语义 / 后端 DTO/API/DB / `signalVariant(...)` / `canonicalSignal(...)` 函数 / inbox 主体 + 顺序 / `<HistoryPage>` 同名 helper（独立 history island 不在本期范围）。验收：`npm test -- signal-label` 25 passed；`npm test` 43 passed（18 既有 + 25 新）；`npm run build` ✓。自写 ~30 LOC（含测试） | — |
| 2026-05-03 | [analysis-depth-mode.md](analysis-depth-mode.md) | v1.0 | **AI 分析模式收敛 quick/standard/deep 三档为「标准 / 深度」二档（用户 2026-05-03 提，~3.5h）**：现状 v1.16 引入的 `depth ∈ {quick, standard, deep}` 三档存在两个语义模糊：(1) `quick` vs `standard` 对用户不可感知——worker 端走同一函数路径，"~30s · ~$0.05 · 跳过辩论/反思"是早期愿景从未落地；(2) `standard` 行为受 `config.iteration.enabled` 隐式 gate——同一选项 ops 改 config 后变成"7 Agent + iteration"用户不知情、产品语义不可预测。方案：**(A) 后端归一化**：保留 `_normalize_depth(v) -> str` 作为旧 API 入口，但 `VALID_DEPTHS = ("standard", "deep")` 收窄，旧 `quick` 在归一化层映射为 `standard`；新增 `normalize_analysis_depth(params: dict) -> {"depth": "standard"\|"deep", "deep_analysis": bool}` 作为统一入口，`deep_analysis` 优先于 `depth`，5 个调用点（`api_analyze` / `api_history` 列表 DTO / `api_history_detail` DTO / `TaskStore._save_analysis_result` / `workers.make_analysis_worker`）全部走它。**(B) Iteration 强制语义**：`StockAnalyzer._iteration_enabled` 收敛——`depth==standard` **永远 False**（不再读 `config.iteration.enabled`，避免产品开关被系统侧静默改写）；`depth==deep` 在 `config.iteration.enabled=true` 时 True，**`config.iteration.enabled=false` 时明确降级为 standard 运行**并 `logger.warning + self._iteration_downgrade_reason = "system_iteration_disabled"`，worker 把降级原因写入 `out["iteration_downgrade_reason"]` 供后续 UX banner（v1.0 仅持久化字段不强制 banner）；不允许静默装作 deep。**(C) DB 迁移**：`PortfolioDatabase._migrate_analysis_history` 末尾追加 `UPDATE analysis_history SET depth='standard' WHERE depth='quick' OR depth IS NULL OR depth=''`（幂等，无 schema 改动）；`TaskStore._ensure_analysis_history_table` 同步追加。**(D) DTO**：`/api/history` 列表 + `/api/history/<id>` 详情同时返回 `depth: "standard"\|"deep"` 与 `deep_analysis: bool`（= `depth=="deep"`），保持向后兼容老 client。**(E) 前端**：删除 `AnalysisDepth = "quick"\|"standard"\|"deep"` 类型 + `DEPTH_OPTIONS` 三段数组 + 三段 RadioGroup UI；改为 `<Switch>` 单一开关 `开启深度分析`，off label `标准分析（7 Agent 单次）` / on label `深度分析（启用迭代/Darwinian 权重）`；`useState<boolean>(false)` 初值默认标准；提交体改 `params: { ticker, date, deep_analysis }`（不再传 `depth`）；`depthLabel(d, deepAnalysis?)` 优先读新字段——`true→深度 / false→标准`，fallback 旧 `depth` 字段 `quick→标准 / null→标准`，**任意输入都不返回「快速」**；列表 Badge / 详情页 Header / Inbox RunningRow 全部接 `depthLabel(row.depth, row.deep_analysis)`。**(F) 测试**：`tests/web/test_analysis_depth.py` 改造现有 9 case（参数化 `("Quick","standard")` / 列表 DTO 测试不再断言 `quick` 透传 / iteration 测试改 `standard→False`）+ 新建 `tests/web/test_analysis_depth_mode.py` 8 新 case（deep_analysis=true→deep / =false→standard / quick 兼容 / 系统禁 iteration 用户选 deep 降级 / DTO 双字段 / 旧 NULL row DTO 返 standard / migration 后无 quick 行 / `normalize_analysis_depth` 优先级）+ `tests/agents/test_analyzer_iteration_gating.py` 4 新 case（standard 永远 False 不读 config / deep+config=true / deep+config=false 降级 + reason / 旧 quick 即 standard）+ `tests/frontend/AnalysisPage/depth-label.test.tsx` 6 新 case（true/false/null/undefined/quick/unknown 全不返回「快速」）。验收：`pytest tests/web/test_analysis_depth.py tests/web/test_analysis_depth_mode.py tests/agents/test_analyzer_iteration_gating.py -q` 全绿；`npm run build` ✓；`npm test` 全绿；grep `frontend/src/islands/analysis/` 不再出现 `"quick"` / `"快速"`。**复用**：`_normalize_depth` / `analysis_history.depth` 列 / `_depth_override` 属性 / `progress_cb` 链 / 现有 `depthLabel` 调用点；**不动**：`AnalysisResult` dataclass、`/api/history` items schema 其它字段、`_run_with_weights` 内部、PipelineDAG 步骤、详情页 8 tab；自写 ~280 LOC（含测试） | — |
| 2026-05-03 | [analysis-inbox.md](analysis-inbox.md) | v1.3 | 列表 / 详情 Header signal Badge 统一三态（用户 2026-05-03 截图反馈，~30min）：现状 `/analysis` 列表行 [`AnalysisPage.tsx:1711`](../../stock_trading_system/web/frontend/src/islands/analysis/AnalysisPage.tsx) 直显原始 `row.signal`，截图实测 7 档评级（Hold / Overweight / Sell / 等）混杂在三档操作语之间用户读不清；详情页 Header line 1083 同样问题。`signalVariant(...)` 已把所有变体映射到 4 个 Badge color variant 但**显示文本**没归一化。方案纯前端单点 helper（不改 DTO/DB/LLM 输出）：(A) 在 `AnalysisPage.tsx` 加 `export function signalLabel(signal): "Buy" \| "Sell" \| "Hold"`，规则 — Strong Buy/Buy/Overweight/BUY/ADD/加仓/bullish/含"buy"子串 → **Buy**；Strong Sell/Sell/Underweight/SELL/REDUCE/减仓/bearish/含"sell"子串 → **Sell**；Hold/HOLD/Neutral/WAIT/中性/neutral/含"hold"或"neutral"子串 → **Hold**；其它/缺失/空白 → **Hold**（保守默认）；Sell 检测顺序优先于 Buy（避免 "underweight" 错命中其它）；(B) 列表行 Badge 文本 `{row.signal}` → `{signalLabel(row.signal)}`（`signalVariant` 仍接原文决定颜色 — 4 variant `buy/sell/hold/default` 不变）；(C) 详情 Header Badge 同样 `{canonicalSignal(detail) \|\| "N/A"}` → `{signalLabel(canonicalSignal(detail))}`；(D) `signalLabel` 加 export 关键字以便测试 import。**不动** OverviewCard 内 `<RatingBadge rating>` 7 档评级文本（评级维度独立于 signal 列）/ `analysis_history.signal` DB 列原文 / `decision_action` 字段语义 / `signalVariant` 函数 / `canonicalSignal` 函数 / 后端任何 DTO/API/DB / inbox 主体 + 顺序 / `<HistoryPage>` 内同名 helper（仅 `/analysis` 主页 + 详情页修，独立 history island 无影响）。新增 `tests/frontend/AnalysisPage/signal-label.test.tsx` 24 case（7 Buy 变体 / 7 Sell 变体 / 10 Hold 变体含 null/undefined/unknown junk）；自写 ~50 LOC（含测试） | — |
| 2026-05-03 | [analysis-rendering.md](analysis-rendering.md) | v1.8.1 | **Fixed**: AnalysisDetailView 残留旧布局块 + PipelineDAG 时间启发式（用户验收反馈，~30min；联动 v1.8 详情页板块顺序）：v1.8 reorder 加了新顺序但**没删旧 K-line + 8-Tab 块**，真实 DOM 是 `Header → PipelineDAG → 8 Tab → K线 → Quick-info → K线 → 8 Tab` —— 前后两份共用同一 `klineSectionRef`，React 让 ref 指向后面那个，IntersectionObserver gating 失灵；旧布局位于 `AnalysisPage.tsx:1376/1390`。同期审 v1.8 PipelineDAG 显示条件 `isRecent && !detail.summary && !!task_id`（`AnalysisPage.tsx:934`）：5 分钟时间窗口 + summary 启发式不可靠 — 跑超 5 分钟的真 running 任务不显示 DAG，近期落库失败但缺 summary 的记录又会误显示。修复：(A) 删除 `AnalysisPage.tsx` AnalysisDetailView 内重复的 K-line `<Card ref={klineSectionRef}>` + 7-tab `<Card ref={tabsRef}>` block（约 116 行 JSX），保证页面唯一顺序为 Header → PipelineDAG（仅 running） → 8 Tab → K 线 → Quick-info；(B) 引入 `taskStatus: string \| null` state，detail mount 时拉 `/api/tasks/<task_id>` + 订阅 `task_started/task_completed/task_failed/task_cancelled` envelope live 更新；`isRunning = taskStatus === "running" \|\| "pending"`，无 task_id 或终态全部隐藏 PipelineDAG —— 不再依赖时间窗口和 summary 字段；(C) 测试 `AnalysisDetailView.order.test.tsx` 升级 v1.4.1：3 个 anchor 各断言 `querySelectorAll('[data-testid=...]').length === 1` 锁唯一性（v1.4 仅检查首个出现位置，对重复布局不敏感），增加严格三段顺序断言 + completed history 不显示 PipelineDAG 断言。验收：`npm test` → 18 passed（12 既有 + 6 新增 = 3 唯一性 + 1 顺序 + 1 legacy stats + 1 completed-no-DAG）；`npm run build` ✓；`pytest tests/web/test_analysis_detail.py tests/tasks/test_progress_truth_source.py -q` 13 passed。**约束**：不改 PipelineDAG 内部 / TVChart 懒加载机制 / Quick-info 卡内部 / OverviewCard / 后端 DTO / `/api/tasks/<id>` 契约。说明：测试组里仅保留 success-status → 隐藏 DAG 一个正向断言；其它分支（no task_id / failed / running 正向）受 jsdom 测试污染（PipelineDAG 的 polling fetch + socket 订阅 + TVChart 懒加载 lightweight-charts canvas 错误跨 test cleanup 不死）影响在同文件多 `render()` 时 body 变空，下沉到手测覆盖 | — |
| 2026-05-03 | [llm-fallback.md](llm-fallback.md) | v1.0 | LLM 跨 provider 自动 fallback（用户 2026-05-03 提：「Gemini 限流应自动切 Qwen 不报错」，~4.5h）：3 处 ChatModel 直接构造（`agents/analyzer.py:_build_quick_llm` / `screener/v3/guru_agents/base.py:_get_chat_model` / `screener/v3/roundtable.py` judge+rebuttal）无 cross-provider fallback，Gemini `ResourceExhausted (429)` 直接抛上层 → 用户看到"评估失败"。方案（**复用 LangChain `RunnableWithFallbacks` 不自写 retry loop**）：(A) 新建 `stock_trading_system/llm/rate_limit.py::is_rate_limit_error(exc)` —— type-based 检测 google `ResourceExhausted/TooManyRequests` + openai `RateLimitError/APIStatusError(429)` + `httpx.HTTPStatusError(429)`，加字符串 fallback `429/rate limit/quota/resource_exhausted/too many requests` 覆盖 wrapped/translated 错误；(B) 新建 `stock_trading_system/llm/resilient_chat.py::build_resilient_chat(config, kind="quick"\|"deep", user_id, timeout)` —— 内部 `_build_chat(provider, kind)` 抽取既有 ChatOpenAI/ChatGoogleGenerativeAI 构造逻辑；用 `RunnableLambda(_wrap_primary).with_fallbacks([counted_secondary], exceptions_to_handle=(_RateLimitMarker,))` 单次请求级捕获限流异常切到备用 provider，`_RateLimitMarker` 自定义异常**仅**包装限流类，其它 exception（auth/network/validation）直通向上抛不被静默；模块级 `_fallback_counter: dict[gemini→qwen, qwen→gemini]` 仅 telemetry，`get_fallback_counters()` / `reset_fallback_counters()` 供 worker 读写；secondary 缺 api_key 或 `config.llm.fallback_enabled=false` → 降级返回 bare primary 不引入 regression（向后兼容默认开启）；(C) 接入 3 处 — `BaseGuruAgent._get_chat_model` 用 builder（覆盖 14 大师 + roundtable Round 3 rebuttal）/ `_build_quick_llm` 用 builder（覆盖 RenderingExtractor 8 tab）/ `roundtable._run_roundtable` 的 `llm_call` 闭包用 builder（judge）；(D) worker 任务级 reset/snapshot counter 写入 metrics — V3 `_v3_run_metadata.fallback_counts`、analysis `result["fallback_counts"]`，前端可选 v1.2 banner 末尾"备用 LLM 触发 N 次"角标（不强制本期）；(E) `with_structured_output(Schema)` 透明 passthrough — `RunnableWithFallbacks` 原生 inherit Runnable 接口；builder __init__ 时 `hasattr` 检查防 LangChain 跨版本破坏；pin `langchain-core >= 0.3`。**严格不动** `get_active_provider` 路由优先级（env > user_settings > config > legacy auto）/ cache.py / 既有 tenacity 同 provider 3 次重试（v1.4 R-fix-9）/ DB schema / API 端点 / 前端 / v1.5 14 大师 prompt + 圆桌 prompt / analyzer 主链路 ta_config（tradingagents 上游 graph 单独迁移留下一版 — 本期范围限 3 处直接构造点）。新增 `tests/llm/test_rate_limit_classification.py` 10 case（type-based + 字符串 + 拒识 500/auth/timeout/validation）+ `tests/llm/test_resilient_chat.py` 6 case（缺 key 降级 / disabled 降级 / 限流切换 / 非限流不切 / structured_output passthrough / 反向 qwen→gemini）；自写 ~200 LOC（含测试）。**风险**：两 provider 同时限流 → counter 计 2 次 + 抛 `_RateLimitMarker` 到 caller，concurrency 现有 try/except + fallback 信号路径不变；字符串 fallback 误判（如 "rate" 出现在 reasoning 文本）— 仅检查 exception message 不检查 LLM 输出；并发任务 counter 加总而非隔离 OK（telemetry 非 correctness） | — |
| 2026-05-03 | [analysis-progress-truth-source.md](analysis-progress-truth-source.md) | v1.0 | 统一 AI 分析进度真相源（用户 2026-05-03 提，~3h）：现状分析记录列表行的进度百分比、状态 badge、流水线 DAG、任务详情页 4 处显示来自不同事实源——row.progress_pct 来自 `/api/history` 持久值（落后），DAG 来自 socket 的 `analysis_pipeline` 事件（实时），「运行中 15% + 全部完成」是常见组合；根因是 `TaskManager._emit` 广播 raw payload 而 `subscribeTaskStream.onAny` 通过 `"task_id" in env` 过滤，**legacy emit 在 live 路径上根本到不了订阅者**——前端只能靠重连后 `/api/tasks/events` 回放才看到 `task_progress/task_completed`，而且 analyzer 7 步的 `step_done` 事件也没桥接到 `task_progress`，所以即使 envelope 修了 row pct 也只在 init/85/98 跳三档。方案 v1.0 三层修复：(B-1) `TaskManager._emit` 广播 `event_emitter.persist_event` 返回的 envelope `{task_id,user_id,seq,event,payload,emitted_at}`；缺 user_id 时用同形 fallback envelope，schema 与 catch-up 一致；(B-2) `progress_cb` signature 加 `stage` kwarg，`task_progress` payload 暴露 `task_id`/`stage`/`status`，所有 lifecycle 事件 payload 同时带 `id` 和 `task_id` 兼容老订阅者；(B-3) 分析 worker `_analysis_progress` 把 analyzer 的 `step_done` event 映射到 `progress_cb`，5%(init)→16/28/39/51/62/74/85(7 step linear)→90(advice)→98(finalize)→100(success)；(F-1) `PipelineDAG` 加 `initialSteps` / `onProgress` props——initialSteps 让父组件用 `/api/history.progress_step` 做 SSR-friendly bootstrap，`onProgress` 在 step_done/pipeline_done 时回调 pct 让 inbox 行与 DAG 对齐；(F-2) AnalysisPage inbox 维护 `liveProgress: Record<task_id, {pct, step, stage}>` map，订阅 `task_progress`/`analysis_pipeline.step_done`/`pipeline_done` 实时合并，`task_completed`/`task_failed`/`task_cancelled` 删除条目并 `refreshInbox()`；`RunningRow` 渲染 `Math.max(live.pct, row.progress_pct)` 永不回退 + 显示 step 文案；(F-3) 折叠态 mobile 进度条同样消费 `displayedPct`。验收：`pytest tests/tasks/test_progress_truth_source.py tests/tasks/test_workers.py tests/tasks/test_task_manager.py tests/tasks/test_workers_qwen_errors.py tests/tasks/test_event_emitter.py -q` → 79 passed（含新增 4 case：envelope shape / progress payload contract / 5→85 linear mapping / structural stage id）；`npm test` 15 passed；`npm run build` ✓。**约束保留**：不改 envelope schema、unified-progress catch-up 协议、analyzer.PIPELINE_STEPS、analysis_pipeline 事件 payload；老 `cb = lambda p, s=None, partial=None: ...` 兼容写法在测试 fixture 加 `**_` 即可不破坏。Subscriber 现在不再依赖 catch-up 才能见到 task_progress；DAG 全绿时 row pct 通过 `pipeline_done` 立即 ≥85，advice/finalize 跟进 90/98；row 与 DAG 来自同一份 envelope 流，断网/刷新通过 `/api/tasks/events?since=<seq>` 回放恢复正确阶段；任务详情与分析记录页显示一致。自写 ~280 LOC（含测试） | — |
| 2026-05-03 | [analysis-rendering.md](analysis-rendering.md) | v1.6 | Executive Summary 入 OverviewCard 操作建议位（用户 2026-05-03 截图反馈）：现状概览 tab 顶部"轻松支撑 645 亿美元的 AI 资本支出"零散小字（来自 `detail.executive_summary`，paper-trade v1.3 F3 抽取列）脱离 Decision banner，无视觉容器；banner 内仅 `action_direction`（"分批建仓"）一句话方向，缺具体可执行操作建议。后端 DTO 已 expose（[`app.py:1564`](../../stock_trading_system/web/app.py) `history_detail_dto.executive_summary`），改动仅前端：(A) [`OverviewCard.tsx`](../../stock_trading_system/web/frontend/src/components/analysis/OverviewCard.tsx) 加可选 prop `executiveSummary?: string \| null`，渲染在 `action_direction` 下方、`KpiRow` 上方，视觉 = `<ScrollText>` 图标 + "执行总结" 标题 + left border accent (`border-l-4 border-primary/60` + `bg-primary/5`) + `text-sm leading-relaxed line-clamp-4` 段落（保持 banner 紧凑，完整段落仍在折叠 markdown body）；(B) [`AnalysisPage.tsx`](../../stock_trading_system/web/frontend/src/islands/analysis/AnalysisPage.tsx) 渲染 OverviewCard 时透传 `detail.executive_summary`（不进 schema、不进 `rendering_json`）；(C) [`lazy-bundle.tsx`](../../stock_trading_system/web/frontend/src/components/analysis/lazy-bundle.tsx) `AnalysisCards` dispatcher 接收 `executiveSummary` prop 仅向 OverviewCard 透传，对其它 7 tab 无副作用；(D) 缺失或纯空白时静默不渲染（用现有 `nonEmptyStr` defensive helper，与 v1.2 兜底风格一致）；(E) markdown body 折叠区保留原 `executive_summary`（双展示无害：banner 结构化露出操作建议 + body 完整原文）。**不动** OverviewCard Pydantic schema / RenderingExtractor / `rendering_json` 存储 / 其它 7 tab Card / `detail.summary` 字段含义（仍是 `executive_summary \|\| trade_decision` fallback 给 markdown body / inbox 行展开摘要用）/ inbox 行展开摘要逻辑。新增 4 个 vitest case 验证位置（action_direction < executive < KpiRow）/ 缺失降级 / whitespace 兜底；自写 ~80 LOC（含测试） | — |
| 2026-05-03 | [screener-v3.md](screener-v3.md) | v1.4 | **Screener V3 8 处契约缺口修复（用户 2026-05-03 提，~6h）**。- **Fixed**: Screener V3 classic mode now uses real V2 threshold path instead of empty results. (`pipeline._run_classic_mode` 复用 V2 `BuffettGuru/GrahamGuru/LynchGuru.evaluate`，把 V2 `GuruMatch.match_pct` 映射成 V3 `GuruSignal` 进入既有 `_aggregate`，无 LLM 调用) - **Fixed**: Screener V3 cancel now stops remaining guru units and preserves partial progress. (pipeline 在每 phase / 每 ticker / 每 unit 边界检查 `cancel_check`，`run_guru_units` 在 semaphore 进入前 + LLM 调用前再次检查；worker 捕获 `result.status==cancelled` 后用 `TaskStore.save_result + update(result_ref)` 持久化部分结果再 raise `_CancelledError`，TaskManager 标 `cancelled`；UI 顶部加 `[停止]` 按钮 → POST `/api/tasks/<id>/cancel`，cancelled 态显示 `[查看部分结果][复制配置重跑]`) - **Fixed**: Screener V3 validates guru ids, candidate_n and mode at trigger time. (`/api/screen/v3/trigger` 用 `_load_guru_registry()` 校验所有 14 注册名，未知返 400 `invalid_guru` + 列出错误项；`candidate_n ∈ {10,20,30,50}` 严格集合返 400 `invalid_candidate_n`；`mode ∈ {classic,agent,agent_rt}` 严格返 400 `invalid_mode`；`with_roundtable` 仅在 `agent_rt` 允许，其它返 400 `invalid_roundtable`；`mode=agent_rt` 缺 flag 时 mode 优先 auto-correct) - **Fixed**: Screener V3 cache hit metrics now come from real cache usage. (`run_guru_units` 返 `(signals, RunStats)`，`RunStats(total_units, cache_hits, new_calls, failed_units)` 来自计数器而非 `confidence==0 && "失败" in reasoning` 启发式；pipeline metrics 加 `total_units / new_llm_calls / failed_units`，`llm_calls` 保留为 `new_llm_calls` 别名；`/api/screen/v3/results _v3_run_metadata` 同步加 4 字段，`cache_hit_pct` 分母改 `total_units`，legacy payload 自动 fallback) - **Improved**: Screener V3 roundtable uses real debate pipeline with fallback status. (`pipeline._run_roundtable` 改为 `await roundtable.run_roundtable(top_signals, llm_call=...)`，judge LLM 走 `BaseGuruAgent._get_chat_model` 同路径；返 `(results, status)` 其中 status∈{success, fallback, skipped}；payload 加 `roundtable_status`) - **Improved**: Screener V3 progress emits start/done/failed/cached unit states. (concurrency 加 `on_unit_start / on_unit_failed` 回调；pipeline + worker 转发 `guru_unit_start / guru_unit_failed`；前端 `<GuruParallelProgress>` 4 态色 running 蓝脉动 / done 绿 / cached 蓝⚡ / failed 红，stale start 防降级 race) - **Improved**: Deeper data bundles + coverage caveat. (`_prepare_data_bundles` 新增 `news_recent`（5 条头条）+ `price_history_summary`（30/90/180-day return + sma200_distance + max_drawdown）+ `sector_industry`；`BaseGuruAgent._llm_reason` 通过 `context["__bundle__"]` 拿 bundle，`_build_data_coverage_caveat` 检测空字段 → SystemMessage 追加 "**历史/新闻数据不足，不得仅凭快照下高分**") - **Improved**: Consensus algorithm correctness. (`_aggregate` 重写票数判定：`unanimous` 仅当 top-vote==total；`split` 票数并列，bullish/bearish 互判优先标 split；`majority` 严格领先；`neutral` 不再被错当 bullish/bearish 反方票) - **测试**：新增 `tests/screener/v3/test_v14_contracts.py` 12 case + `tests/web/test_screen_v3_trigger.py` +7 case；既有 `test_screen_v3_run_metadata::test_results_returns_run_metadata` 更新为 v1.4 字段契约。验收：`pytest tests/screener/v3 tests/web/test_screen_v3_results.py tests/web/test_screen_v3_run_metadata.py tests/web/test_screen_v3_trigger.py tests/web/test_screen_v3_history.py tests/tasks/test_workers.py -q` → 244 passed；`npm run build` ✓。**复用**：V2 `build_gurus(buffett/graham/lynch)` + 既有 `roundtable.run_roundtable` + DataRouter.get_news/get_history_for_backtest + TaskManager `_CancelledError`；**不动** GuruSignal dataclass / RoundtableResult / `/api/screen/v3/results` DTO 已有字段 / ResultsView 前端契约。自写 ~600 LOC（含测试） | — |
| 2026-05-03 | [ui-react-island.md](ui-react-island.md) | v3.0 | **Performance**: 页面点击与首屏加载性能优化（用户 2026-05-03 提，~1h）。**变更**：(A) `vite.config.ts` 扩展 `react-vendor` manualChunks，从 `["react","react-dom"]` 扩到 `["react","react/jsx-runtime","react-dom","react-dom/client","scheduler"]` —— 把 React DOM runtime 从共享 `card` chunk 抽出到独立的 `react-vendor-*.js`；(B) 静态资源缓存策略已就位（`app.py::add_static_asset_cache_headers`，2026-05-02 已加）：`/static/dist/assets/*` → `public, max-age=31536000, immutable`；`/static/dist/.vite/manifest.json` → `no-store`；其它 dist → `public, max-age=3600`；新增 `tests/web/test_static_cache_headers.py` 2 case 锁这两个契约；(C) socket / ChartPanel 按需加载——审计确认 `PipelineDAG / ScreenerV3Progress / TasksPage / DashboardPage / AnalysisPage / ScreenerV3Page` 全部用 `import("@/lib/socket")` 动态形式；`DashboardPage / BacktestPage / PaperTradePage` 全部用 `React.lazy(() => import("@/components/shared/ChartPanel"))`，无需源码改动；(D) 扩展 `tests/web/test_vite_asset_base.py`（v1.5 已建）+4 case：socket chunk 不得在任何 real-time 页（dashboard/tasks/analysis/screener-v3）的静态 imports 中、ChartPanel chunk 不得在 dashboard/backtest/paper-trade 的静态 imports 中、`react-vendor-*.js` chunk 必须存在（命名+`scheduler` typo 防御）且 `>50kB raw`（未含 ReactDOM 防御）、`card-*.js` 不得超过 `250kB raw`（pre-split 是 340kB，post-split 是 158kB，250kB 阈值有余量）。**构建后实测体积变化**：`card-*.js` 340.79kB / 107.24kB gz → 157.93kB / 49.86kB gz（-54%）；新出 `react-vendor-*.js` 185.65kB / 58.03kB gz（独立 immutable cache）；其它 lazy chunks 不变。验收：`pytest tests/web/test_vite_asset_base.py tests/web/test_static_cache_headers.py tests/web/test_vite_modulepreload.py tests/web/test_analysis_detail.py tests/web/test_analysis_sndk_smoke.py -q` 26 passed；`npm run build` ✓；`npm test` 8 passed；manifest 中 socket / ChartPanel 仅出现在 dynamicImports，不在 imports。**不动** vite base / Flask 静态挂载 / lazy chunk 协议；自写 ~140 LOC（含测试） | — |
| 2026-05-03 | [analysis-rendering.md](analysis-rendering.md) | v1.5 | **Fixed (P1)**: AI 分析详情页结构化摘要看似"暂不可用"实为静态资源 404（用户 2026-05-03 实测验证）：v1.3 修复了 chunk 循环依赖、v1.4 修复了大师评分同质化，但生产 `/analysis/<id>` 概览仍稳定走 fallback。**真实根因**：`React.lazy(() => import("@/components/analysis/lazy-bundle"))` 触发的动态 import 让 Vite 运行时调用其 `__vitePreload` helper 拉 chunk 关联的 CSS（`card-_CXQ8uMa.css`）；helper 用 `vite.config.ts` 里的 `base` 字段拼 URL，**默认是 `/`**，于是 helper 实际请求 `/assets/card-*.css` → Flask 在 `/static/dist/` 下挂载 → 404 → 抛 `Unable to preload CSS for /assets/card-*.css` → per-tab ErrorBoundary 接住 → 用户看见"结构化摘要暂不可用"fallback。**HTML 头里的 modulepreload/stylesheet 都对**（`vite_helpers.py:79-102` 显式拼了 `/static/dist/`），就 lazy chunk 的运行时 helper 用错前缀。修复：(A) `vite.config.ts` 加 `base: "/static/dist/"`，让 Vite 烘焙的 preload helper 内嵌 `return"/static/dist/"+i`；(B) 重新 build，验证 `dist/assets/preload-helper-*.js` 不再有 `return"/"+i` 形式，全场也不再有裸 `"/assets/"` URL；Vite manifest 仍用相对路径（`assets/foo.js`）所以 `vite_helpers.py` 不需要改、不会双前缀（`/static/dist//static/dist/...`）；(C) `AnalysisPage.tsx::CardFallback` 区分 chunk-load 错误与数据错误——`error.message` 含 `Unable to preload` / `Failed to fetch dynamically imported module` / `Loading chunk` / `Importing a module script failed` 时文案改为"结构化摘要组件加载失败，已显示完整论述"，区别于数据形态问题的"结构化摘要暂不可用"，开发者详情区原 `error.name: error.message` 保留；(D) **构建验收测试** `tests/web/test_vite_asset_base.py` 4 case：`preload-helper-*.js` 必须存在且唯一、必须含 `"/static/dist/"` 字符串、不得有 `return"/"+` 默认形式、扫描所有 dist js chunks 不得有裸 `"/assets/` URL、Vite manifest entries 不得以 `/` 开头（防 vite_helpers.py 双前缀）。验收：`pytest tests/web/test_vite_asset_base.py tests/web/test_vite_modulepreload.py tests/web/test_analysis_detail.py tests/web/test_analysis_sndk_smoke.py -q` 20 passed；`npm run build` ✓；`npm test` 8 passed；rebuild 后 `grep -c '"/static/dist/"' dist/assets/preload-helper-*.js` = 1。**不动** `vite_helpers.py`、`layout.html` modulepreload 输出、Flask `/static/` 挂载；自写 ~95 LOC（含测试）。优先级 P1 — 影响所有使用 lazy AnalysisCards 的分析详情页 | — |
| 2026-05-02 | [screener-v3.md](screener-v3.md) | v1.4 | **Fixed**: V3 选股"大师评分详情"四张大师卡内容同质化（用户 2026-05-02 提，~1.5h）：现象——同一 ticker 的 Buffett/Lynch/Graham/Munger 卡片首屏 reasoning 摘要几乎完全一致，都以"该公司主题匹配 …"或"该 ticker 与用户主题 …"开头，看不出每位大师自己的投资框架。根因（grep 核实）：(a) `base.py::_build_theme_instruction` 含强约束 "reasoning 必须明确说明: - 该公司与用户主题是否直接相关 / - 主题相关性的证据 / - 若不相关，为什么不能作为该主题候选" → LLM 一律以主题段开篇，把每位大师自己的框架（护城河/PEG/估值/思维模型）全部挤到后段甚至省略；(b) `CandidateExpanded` 直接 `(s.reasoning ?? "").slice(0, 240)` 截断展示，自然只显示主题段，看不到差异化；(c) `title={s.philosophy}` tooltip 是 copy/paste bug——philosophy 每位大师不变，hover 不带额外信息（应是 `s.reasoning`）；(d) `<Card key={i}>` 用数组 index 作 key——若父排序重排 guru_signals，React 复用错卡片的 `<details>` open 状态。方案：(A) **后端**：`_build_theme_instruction` 移除 "reasoning 必须明确说明 [theme stuff]" 段，主题描述只放 `sub_analyses[name=theme_fit].details`；新增 `_build_reasoning_format_instruction(framework_lead)` 强制 reasoning 三段结构 [段一: 框架结论 / 段二: 1-2 条核心依据 / 段三: 主要风险或反方观点]，明确禁止 "该公司主题匹配 …"、"该 ticker 与用户主题 …" 作开头；`BaseGuruAgent.framework_lead` 新增 class attr，`_llm_reason` 拼装 `system_prompt + theme_instruction + reasoning_format + schema_instruction`；Buffett (`护城河 / 自由现金流 / 安全边际`) / Lynch (`成长阶段分类 / PEG / 散户可理解性`) / Graham (`估值（PE/PB/NCAV）/ 资产负债安全 / 安全边际`) / Munger (`商业质量 / 持久竞争优势 / 复杂度规避`) 各自显式声明 framework_lead，其余 10 位 guru fallback 到 `philosophy`（已经各异）；(B) **前端**：`GuruScore` 接口增 `sub_analyses?: GuruSubAnalysis[]` + `total_score?: number`；`CandidateExpanded` 不再 slice，新建 `<GuruSummaryCard>` 子组件，结构化展示 [结论 / 核心依据 / 风险\|反方] 三行 + theme_fit 小 Badge（取自 `sub_analyses[theme_fit].score`）+ `<details>展开完整推理</details>` 兜底；`reasoningSentences` / `pickRiskSentence` / `pickSupportSentences` 三个工具按句号/换行切分 + 关键词命中（风险/担忧/limitation/concern）+ 段三 fallback；tooltip 改为 `title={s.reasoning ?? ""}`（移除 philosophy 误用）；卡片 `key={s.guru}` 替换 `key={i}` 防排序复用错；(C) **测试**：`tests/screener/v3/test_guru_signal_uniqueness.py` 13 case → guru_signals 按 ticker 唯一、四位 guru 各有 framework_lead 且两两不同、四位 guru 各自 lead 必须 mention 框架关键词（护城河/PEG/PE+资产负债/质量+竞争优势）、`_build_reasoning_format_instruction` 包含 framework_lead + 三段结构 + 主题禁用引导、v1.3 regression vector "reasoning 必须明确说明：" 必须不再出现、`_llm_reason` 真把 framework_lead 注入 SystemMessage（mock LLM 捕获 messages 验证）、合成检查首屏 120 字四张卡两两不同。验收：`pytest tests/screener/v3/ -q` 178 passed（含原 165 + 新 13）；`npm run build` ✓；`npm test` 8 passed（analysis-cards 不受影响）。**不动** GuruSignal Pydantic schema、roundtable 协议、aggregator 字段、14 大师 evaluate_deep 业务逻辑；自写 ~290 LOC（含测试） | — |
| 2026-05-02 | [analysis-rendering.md](analysis-rendering.md) | v1.3 | **Fixed**: AI analysis history structured summary cards failed to render — analysis lazy bundle had a circular chunk dependency (`lazy-bundle.tsx` 静态 import 回 `src/islands/analysis/main.tsx`) that broke runtime helper resolution. **Added**: real runtime smoke tests for structured analysis cards via vitest + @testing-library/react (8 cases, real `render()` calls, asserts fallback phrases never appear). 联动 v1.2 — v1.2 加 `normalizeCardForClient` 后生产 `/analysis/17` 概览仍稳定显示"结构化摘要暂不可用"。审 vite manifest 发现 `lazy-bundle.tsx` 静态 import 回 analysis entry chunk — Rollup 看到 `AnalysisPage.tsx` 直接 import `defensive.ts`，把 helper 划到 entry chunk，lazy-bundle 反向 import 形成 chunk 级循环依赖（lazy chunk 解构 import 在 entry binding 初始化前先 read，拿到 `undefined`，cards 内部 throw → ErrorBoundary 显 fallback）。修复：(A) `lazy-bundle.tsx` 改为直接 import 8 个具体 card 文件（不走 `@/components/analysis` barrel）；(B) lazy-bundle dispatcher 内部调 `normalizeCardForClient(tabKey, data)` 归一化后再 dispatch，归一化失败返 `null` 静默不再让 boundary 触发；(C) `AnalysisPage.tsx` 删除 `normalizeCardForClient/describeShape` 的 import，本地 inline `describeShapeForTelemetry`（仅供 ErrorBoundary onError 日志），把 raw `detail.rendering[tab]` 直接传给 `<AnalysisCards>` — entry chunk 不再接触 `defensive.ts`，循环消失。新增前端运行时测试：(D) 装 `vitest@2 + jsdom + @testing-library/react + @testing-library/jest-dom`，`vitest.config.ts` + `src/test/setup.ts`，`npm run test` / `npm run test:watch`；(E) `src/components/analysis/__tests__/AnalysisCards.test.tsx` 真实 `render(<AnalysisCards tabKey="summary" data={...} />)` 8 case：clean SNDK 形态、`debate_synthesis: [...]`、`decision_drivers` mixed string/null/object/array、`key_metrics.value` is object/array、`summary` 是数组（dispatcher 返 null 不显 fallback）、Market `support_resistance` 含 `price:"—"`/string-level/null item、Decision `structural_stop:"n/a"` + `entry_zone:null`、unknown tabKey；每个 case `expect(document.body.textContent).not.toContain("结构化摘要暂不可用" / "结构化卡片渲染失败")`。验收：rebuilt manifest 中 `lazy-bundle.tsx` imports 不再含 `src/islands/analysis/main.tsx`（只剩 4 个共享 chunk）；`pytest tests/agents/test_rendering_extractor.py tests/agents/test_rendering_schemas.py tests/agents/test_rendering_data_sources.py tests/web/test_analysis_rendering.py tests/web/test_analysis_rendering_normalize.py tests/web/test_analysis_sndk_smoke.py tests/web/test_analysis_detail.py -q` 58 passed；`npm test` 8 passed；`npm run build` ✓。**保留 ErrorBoundary 作异常兜底**，结构化路径不会再走 fallback；自写 ~280 LOC（含测试） | — |
| 2026-05-02 | [task-failure-visibility.md](task-failure-visibility.md) | v1.0 | 任务失败原因可见化（用户 2026-05-02 截图反馈，~2h）：现状 `/tasks/<task_id>` AI 分析失败时进度卡满格红条 + 「已完成 0/1」+ 错误区空白，必须开 DevTools 才能看到真实失败原因。事实审计（grep 核实）：(a) `task_manager.py:267-276 _fail()` 已写入 `error_message + error_trace` 到 tasks 表并 emit `task_failed{id, error_message}`；(b) `task_manager.py:214 _on_progress` 已写入 `progress_step`；(c) 但 `TasksPage.tsx:17-21 interface Task` 缺 `error_message/error_trace/progress_step/duration_ms` → TS 端无法消费；(d) `TasksPage.tsx:305` `task_failed` 事件读 `p.error \|\| ""`，**后端从来没发过 `error` 这个 key**（实际是 `error_message`）→ 实时失败事件 meta 永远空白；(e) `TasksPage.tsx:280-281` `isTerminal` 一律 width: 100% → failed/cancelled 进度条满格但完成数 0/1 自相矛盾；(f) `app.py:3002-3015 /api/tasks/<task_id>` 走 `_sanitize_shared_task` 但未剥离 `error_trace` → shared_research type 非创建者也能拿到含路径/环境变量/可能 key 的 traceback。属于纯前端展示链路 bug，**不是**分析管线本身的问题。方案 v1.0（**三层修复，不改 envelope/schema/`_check_task_ownership`/analysis-rendering v1.2 per-tab ErrorBoundary**）：(F-1) Task interface 增 4 字段全 optional 兼容老行；(F-2) failed 状态在进度卡下方渲染 `<Alert>` 标题「分析失败」+ `error_message ?? "任务失败，但后端未返回错误详情"` + 可折叠 `<details>` 限高 240px 显示 `error_trace`；(F-3) `task_failed` 改读 `p.error_message ?? p.error ?? ""` 保留 `p.error` fallback 不破坏老队列；`analysis_pipeline` 且 `p.type === "pipeline_error"` 时 meta 显示 `p.error \|\| p.message`；(F-4) failed/cancelled 不再「已完成 0/1」，改「失败于：{task.progress_step \|\| 最近 pipeline step \|\| '分析管线'}」，进度百分比优先 `task.progress` 不再硬编码 100%；(B-1) `_sanitize_shared_task` 新增 owner/admin 判定，**非 owner 非 admin 剥离 `error_trace`**（保留 `error_message`，因为 message 是 worker 包装过的人话对所有人安全，trace 含 traceback 路径/env/key 片段）；(B-2) `/api/tasks/<task_id>` 显式确认返回 `error_message/error_trace/progress_step/duration_ms`，store 缺 `duration_ms` 时按 `completed_at - started_at` 现算；(W-1) AI worker `except` 分支新增可读包装：`KeyError("QWEN_API_KEY")` 缺 key → 「Qwen API Key 未配置」/ DashScope 401/403 → 「Qwen 认证失败，请检查 API Key」/ 404 model → 「Qwen 模型不可用：{model}」/ timeout → 「LLM 请求超时，请稍后重试或切换 quick 深度」/ 其它原样 `str(e)`；`error_trace` 始终保留原始 traceback。验收：`tests/web/test_task_detail_failure.py` 新增 4 case + `tests/tasks/test_workers_qwen_errors.py` 新增 5 case + `npm run build` ✓ tsc + vite 全绿。共享研究边界：`error_message` 同租户共享，`error_trace` 仅 owner/admin 可见。约束：**不改** task_failed envelope 协议、tasks 表 schema、unified-progress catch-up、_check_task_ownership 规则；自写 ~375 LOC | — |
| 2026-05-02 | [screener-history.md](screener-history.md) | v1.2 | 主页板块顺序对齐 analysis-inbox（用户反馈，~10min）：v1.0 实施 [ScreenerV3Page.tsx:69-74 ScreenerHomeView](../../stock_trading_system/web/frontend/src/islands/screener-v3/ScreenerV3Page.tsx) 把 `<RecentScreensCard>`（最近 3 卡）渲染在前、`<ScreenerForm>`（表单）渲染在后，与 [analysis-inbox v1.1](analysis-inbox.md)（发起分析在上、记录在下）规则不一致。调整：`ScreenerHomeView` 两块换序为 `<ScreenerForm prefillTaskId>` → `<RecentScreensCard>`，主动作（提交新选股）位于第一屏不需滚动可见，最近记录降为下方次要参考。同时确立全局规则：所有「表单 + 历史」混合主页统一 **表单在上、历史在下**（analysis / screener 两个入口对齐降低用户切换成本）。`<RecentScreensCard>` 内部不变；`<ScreenerForm>` prefill banner 与字段位置不变；其它路由（`?task=` / `?result=` / `/screener-v3/history`）不变。无 schema / API / 后端改动 | — |
| 2026-05-02 | [screener-history.md](screener-history.md) | v1.1 | 选股运行视图换 V3 专属进度组件（用户 2026-05-02 截图反馈，~2h）：现状 `/screener-v3?task=<id>` 渲染了 AI 分析的 `<PipelineDAG>` → 显示"技术面/情绪面/新闻/基本面/多空辩论/风险评估/最终决策"7 节点，与选股链路无关；agent_rt 圆桌辩论阶段在进度区完全不可见。根因：(a) `PipelineDAG.STAGES` 硬编码 analyzer 7 步且仅监听 `analysis_pipeline` 事件；(b) `ScreenerV3Pipeline` 6 phase 无自有 stage 事件；(c) `pipeline._run_roundtable` 没调 `roundtable.run_roundtable` 模块函数 → `roundtable_start/done` 事件永远不发；(d) worker `_on_progress` 已就绪但上游空转。方案（**纯增量**）：(A) `ScreenerV3Pipeline.run` 在每 phase 头尾发 `screen_v3_stage_start/done`，stage∈{`parse / universe / bundle / guru / roundtable / aggregate`}；`_run_roundtable` 内按 ticker 发 `roundtable_start({tickers}) + roundtable_done({ticker, consensus, dissent})`；末尾 `aggregate_done`；(B) `workers.py make_screen_v3_worker._on_progress` 转发 `screen_v3_stage_start/done / aggregate_done`；(C) 新建 `components/shared/ScreenerV3Progress.tsx` 独立组件 STAGES=[解析条件/构建股票池/拉取行情/大师并行评分/圆桌辩论/生成结果]，圆桌阶段仅 `mode==="agent_rt"` 显示，状态机消费 stage 事件 + bundle/guru/roundtable 子事件，5s GET 兜底；(D) `<ScreenerRunningView>` 渲染 `<ScreenerV3Progress mode taskId>` + 既有 `<GuruParallelProgress>`，**移除 `<PipelineDAG>`**；mode 从 catch-up GET 的 params_json 读。验收：`tests/screener/v3/test_pipeline_progress_events.py` 新增 → assert pipeline 至少发 `screen_v3_stage_start{stage=parse}` + `_done{stage=guru}` + `aggregate_done`；agent_rt 模式 assert `roundtable_start`。**不动** unified-progress 协议、TaskStore schema、14 大师、aggregator、RoundtableResult、`ResultsView` | — |
| 2026-05-02 | [analysis-inbox.md](analysis-inbox.md) | v1.1 | 顺序修正 + 空态文案 + `/api/tasks/submit` task_id 字段修复（用户反馈，~1.5h）：v1.0 三处问题：(1) "分析记录"卡放在"发起分析"卡上方与产品意图相反；(2) 用户提交后"看不到反馈"——根因不是前端逻辑缺失，而是 `/api/tasks/submit` 返回 `{id, type, ...}` 但前端 `AnalysisPage.tsx:320` 读 `res.task_id`，**字段名不匹配** → 乐观插入分支永远进不去 → 必须手刷；(3) 空态文案与新顺序不再对应。修正：(A) `AnalysisPage.tsx` form 视图 children 顺序改为 [标题] → [发起分析卡] → [分析记录卡]，仅交换 JSX 块；(B) 空态文案改 "暂无分析记录，提交一个新分析后会在这里显示进度"；(C) **后端 `/api/tasks/submit` 响应追加 `task_id` 字段**（值=`task["id"]`，全 `task` 字段保留向后兼容）→ 前端 v1.0 乐观插入逻辑真正生效；同时也修了 `<ScreenerForm>` 提交跳转 `/screener-v3?task=<id>` 的同源问题。验收：(1) 顺序对；(2) 提交后 running row **立即**出现；(3) 进度条实时；(4) 完成 row 替换为 completed；(5) `/history` 重定向不变；测试 `tests/web/test_history_inbox.py` 新增 `test_running_row_carries_all_required_fields`（kind/task_id/ticker/depth/status/submitted_at/progress_pct/progress_step/error 字段契约锁定）+ `test_submit_then_inbox_sees_running_row_immediately`（端到端 submit→inbox，blocking worker stub）。**不动** /api/history 契约、optimistic insert 逻辑、socket 订阅、`<RunningRow>/<CompletedRow>` 实现 | — |
| 2026-05-02 | [analysis-rendering.md](analysis-rendering.md) | v1.2 | 结构化卡片 ErrorBoundary 兜底闭环（用户 2026-05-02 提，~2.5h；联动 v1.0/v1.1）：现状生产 `/analysis/17`（SNDK）即使 v1.21 后端 `_normalize_card` 已就位，前端"概览"tab 仍触发 per-tab ErrorBoundary 显示"结构化卡片渲染失败" — 因为 (a) `OverviewCard` 入口只判 `typeof === "object"` 数组也通过，(b) 卡片只依赖后端单层 normalize，DB 里历史脏数据未走 backfill 时直接到客户端，(c) `ErrorBoundary` 静态 fallback 把错误吞掉，操作员看不到根因。方案：(A) `defensive.ts` 新增 `isRecord` / `safeRecord` / `safeEnum` / `safeBool` / `describeShape` / `normalizeCardForClient(tabKey, struct)` — 客户端二次 normalize 镜像后端 `_normalize_card`，对 8 tab 各自做 record/array/finite-number guard，输入非 record 直接返回 null（drop card），输入 record 但子字段脏（如 `summary.debate_synthesis: ["arr"]`）就地 collapse 到 null/[]；(B) `OverviewCard` 入口改 `safeRecord(data)`，`debate_synthesis` 走 `safeRecord` 否则置空，三派 `aggressive/conservative/neutral` 各自 `safeRecord`，`decision_drivers` / `key_metrics` 仅保留 record item（string/null/array item 直接丢弃），所有 React child 走 `safeText`；(C) 同样 record-guard 应用到 `MarketCard / SentimentCard / NewsCard / DebateCard / RiskCard / DecisionCard / FundamentalsCard`，array `.map(item => item.x)` 前先 `filter(isRecord)`，子字段（entry_zone / valuation / growth / profitability / balance_sheet）改 `safeRecord` 入口；(D) `AnalysisPage.tsx` per-tab `ErrorBoundary` 改 render-function fallback (`<CardFallback error={...}>`) 文案降级为"结构化摘要暂不可用，已显示完整论述"+ `<details>` 展开错误名/消息，`onError` 回调 `console.error` 输出 `analysis_id / tab_key / error_name / error_message / struct_shape`（仅 key+type 两层深，绝不打报告正文）；传给 `<AnalysisCards>` 前调 `normalizeCardForClient`；(E) `tests/web/test_analysis_sndk_smoke.py` 新增 7 case 覆盖 `summary` 是数组、`debate_synthesis` 是数组/string/scalar/partial、`decision_drivers` 含混合 string/null/object/array item、`key_metrics.value` 是 object/array、`rating/confidence` 是 trailing-whitespace/wrong-case/object、`Market.indicators` 含混合 string/null/record；(F) 新增 `__smoke__/malformed-payloads.ts` (Storybook-like fixture) + `__smoke__/normalize.smoke.ts` 纯函数 smoke harness（无 runtime test runner 依赖，`tsc -b` 校验类型 + `RUN_ANALYSIS_SMOKE=1` 环境变量驱动 self-execute）。验收：`pytest tests/agents/test_rendering_extractor.py tests/agents/test_rendering_schemas.py tests/agents/test_rendering_data_sources.py tests/web/test_analysis_rendering.py tests/web/test_analysis_rendering_normalize.py tests/web/test_analysis_sndk_smoke.py tests/web/test_analysis_detail.py -q` 58 passed；`cd stock_trading_system/web/frontend && npm run build` ✓ tsc + vite 全绿。**不动 schema、不动 8 Card 数据契约、不动后端 `_normalize_card`**；新增前端 ~280 LOC + 后端测试 ~140 LOC | — |
| 2026-05-02 | [screener-history.md](screener-history.md) | v1.0 | 选股记录与运行入口（用户 2026-05-02 提，~8h）：现状 `/screener-v3` 单 island，提交后跳 `/tasks/<task_id>`（**用户抱怨"只能从任务中心查"根因**），无 v3 历史 API（仅 v2 有），无 `/screener-v3/history` 路由。评估**不**学 analysis-inbox 单页合并：选股表单重（NL 输入 / 14 大师多选 / mode / market / candidate_n / 成本预估）+ 结果页重（v1.2 KPI 6 列 + Top 5 圆桌）+ 频次低（1-2 次/天，对比 analysis 高频多并发）+ 单次耗时长（agent_rt 15-30 分钟）。方案 3 入口结构：(A) `/screener-v3` 主页加 `<RecentScreensCard>` 顶部 3 卡 + 右上角 [查看全部 →] 链接；(B) `/screener-v3/history` 独立路由 `<ScreenerHistoryList>` 复用 v1.18 R-fix-12G HistoryPage 工具栏/行/多选 + 加 mode/market 多选筛选 + [包含失败] toggle + [复制配置重跑] 按钮；(C) `/screener-v3?prefill=<task_id>` 主页 ScreenerForm 自动用历史 task params_json 预填（NL/market/candidate_n/gurus/mode/with_roundtable）+ banner 提示；(D) 提交后从 `/tasks/<task_id>` 改为 `/screener-v3?task=<task_id>`，新增 `<ScreenerRunningView>` 同页内 PipelineDAG + `<GuruParallelProgress>` 大师并发矩阵（订阅 unified-progress `guru_unit_done` 事件） + 完成 replaceState 到 `?result=<task_id>` + 失败留在运行页带返回。后端：(E) `TaskStore.list_screen_v3_history(user_id, modes, markets, limit, offset, include_failed)` 新方法返每行 summary（candidates_count / avg_score / votes / consensus_rate_pct / top3_tickers / llm_calls / cache_hit_pct / duration_sec）从已有 payload.candidates + payload.metrics 实时聚合，不需 schema 变更；(F) `get_screen_v3_history_one(task_id, user_id)` 单条端点供 prefill；(G) `/api/screen/v3/history` `/api/screen/v3/history/<task_id>` 新端点（多租户 v1.18 R-fix-12 边界仅返 created_by==g.user.id）。**Sidebar 不动**（不新增"选股记录"菜单项，保持 1 项 `[选股]`，"查看全部"作主页二级链接）。`<ResultsView>` v1.0+v1.2+v1.3 / `<ScreenerForm>` 既有字段 / 任务中心 / screen_v3 schema 全部不动。复用 v1.18 HistoryPage ~70% 代码 + unified-progress + shadcn 组件库；自写 ~700 LOC | — |
| 2026-05-02 | [screener-v3.md](screener-v3.md) | v1.3 | 选股主题污染修复 V3 联动（用户 2026-05-02 提，~3h；联动 [screener-v2.md](screener-v2.md) v1.2）：现状 `存储龙头股` 经 v3 ScreenerV3Pipeline → guru agents → roundtable 全链漏掉主题——guru 收不到 query 信息按"全市场龙头"打分，roundtable 辩论与 query 无关，pipeline `_get_candidates` 异常路径裸 mega-cap fallback 含 BRK-B/JPM/V。方案 v1.3（注入主题约束到 BaseGuruAgent / roundtable / pipeline 三处，**不动** 14 大师 prompt 内容、aggregator 字段、RoundtableResult dataclass）：(A) `BaseGuruAgent._build_theme_instruction(query, spec)` 渲染 query + filter_spec verbatim 进 SystemMessage，包含 theme_fit SubAnalysis 强制要求、显式 broad-market 黑名单（BRK-B/JPM/V/MA/UNH/WMT/PG）、required pure-play (MU/WDC/STX/SNDK)、cloud carve-out（"云存储"/"对象存储"/"S3"/"Azure Storage" → AMZN/MSFT/GOOGL）；`_llm_reason` 拼 instruction 入 SystemMessage；4 个具体 guru (buffett/graham/lynch/munger) 调用签名加 `nl_query`/`filter_spec` 透传；(B) `_enforce_theme_fit(signal, context)` 后处理：theme_fit<4 → total_score≤60 + bullish→neutral；theme_fit<2 → total_score≤45 + bullish→bearish；缺 theme_fit subanalysis 的 legacy 输出不动（return same instance），不会上调 signal；(C) `roundtable._build_debate_prompt(guru, ticker, sig, stance, query, spec)` 加 query/spec 入参 + 内嵌 3 个 theme-fit 问题（"是否直接符合用户查询主题"/"是否主题内的龙头"/"主题匹配"）；(D) `ScreenerV3Pipeline._get_candidates` 异常路径走 v2 `theme_fallback_universe`（依赖 v2 v1.2），off-theme 回 trimmed mega-cap（经 v2 `_clean_theme_tickers` 过滤 7 个 polluters）。验收：`tests/screener/v3/test_theme_aware_prompts.py` 12 case + `test_pipeline_storage_fallback.py` 2 case 全绿。**不开新 LLM call、不发明 v3 端点、复用 v2 theme_universe 模块**；自写 ~250 LOC | — |
| 2026-05-02 | [screener-v2.md](screener-v2.md) | v1.2 | 选股主题污染修复 V2 基础（用户 2026-05-02 提，~2h；联动 [screener-v3.md](screener-v3.md) v1.3）：用户报告 `存储龙头股` 返 BRK-B/JPM/V/MA/UNH/WMT/PG（broad-market 蓝筹），根因（线性）：(1) NLParser system prompt 没有"龙头=主题内"语义约束 + 没有 storage 子主题消歧 → LLM 把 `存储` 默认项目到 cloud storage 或干脆出 mega-cap padding；(2) UniverseFilter LLM 不可用时裸用 `_DEFAULT_US` → 主题查询直出 BRK-B；(3) v3 pipeline 异常路径同样裸 mega-cap。方案 v1.2 **三层防御**：(A) 新建 `screener/v2/theme_universe.py`：`detect_theme(query, intent_summary, themes)` 当前注册 1 主题 `memory_storage`（关键词正则匹配 存储/内存/DRAM/NAND/闪存/SSD/HDD/memory chip 等）；`theme_fallback_universe(theme, query)` 返 required pure-play (MU/WDC/STX/SNDK) + extended semis (MRVL/AVGO/INTC/AMD/NVDA) + cloud carve-out（仅 query 含 `云存储`/`对象存储`/`S3`/`Azure Storage` 时打开 AMZN/MSFT/GOOGL）；`filter_off_theme(tickers, theme, query)` 大写化 + 黑名单过滤；`broad_market_blacklist()` = (BRK-B/JPM/V/MA/UNH/WMT/PG)；(B) `nl_parser._SYSTEM_PROMPT` 追加 Rule 9（中文存储主题消歧 + 6 子词）+ Rule 10（"龙头=主题或行业内部" + 显式 forbidden examples + cloud carve-out）+ cloud 仅 explicit keyword 打开；(C) `UniverseFilter.filter_by_spec` 引入 `_STORAGE_EXCLUDED_US = {BRK-B/BRK.B/BRKB,JPM,V,MA,PG,WMT,UNH}` + `_clean_theme_tickers` 二次清洗：LLM 输出 / theme_fallback / heuristic-narrow 三路全过 — `_DEFAULT_US` 本身**不动**；LLM 不可用 → theme_fallback；off-theme 查询保持 `_heuristic_filter(_DEFAULT_US)` 路径但同样过 cleaner（行为对称）；返 `(tickers, source)`，source∈{"theme_fallback","llm","heuristic","default"}。验收：`test_nl_parser_theme_rules.py` 4 + `test_theme_universe.py` 8 + `test_universe_filter_storage.py` 5 + `test_universe_theme_fallback.py` 5 case 全绿；off-theme path 100% 不变；`tests/tasks/test_task_store.py` 同期补 47 行覆盖 advice 隔离/idempotency/orphan recovery 已有契约（不是新设计）。**不动 FilterSpec dataclass 字段、不开新 LLM endpoint**；自写 ~330 LOC | — |
| 2026-05-02 | [analysis-inbox.md](analysis-inbox.md) | v1.0 | AI 分析 + 分析记录合并（用户 2026-05-02 提，~9h）：现状 3 island 分散——`/analysis` 主页（表单 + 5 卡）/ `/analysis/<task_uuid>` 运行中视图 / `/history` 独立分页列表（v1.18 R-fix-12G） / `/tasks` 任务中心，用户提交后跳 `/analysis/<task_uuid>` 离开主页 + 想看历史得切 `/history` + 不知道运行中跑哪了被迫去任务中心。根因：**没有页面同时展示运行中 + 已完成全状态**。方案：1 主入口（`/analysis`）= 紧凑表单 + 时间线 Inbox（混合运行中 task + 已完成 analysis 按 created_at DESC 排序），详情页 `/analysis/<numeric-id>` 完整 8 tab 保持独立路由（深链 / 分享 / SEO 友好），任务中心 `/tasks` **保持不变**（跨类型 task 总览）。后端：(A) `TaskStore.list_tasks_by_user_and_type(user_id, task_type, statuses)` 新方法；(B) `/api/history?include_running=true` 合并 tasks（pending/running/failed/cancelled）+ analysis_history 排序，response 加 `running_total / completed_total`，运行中**全返不参与 limit**，`exclude_task_ids` 防 task_completed 与 DB 写入间的瞬时重复；(C) `/history` 路由 301 redirect 到 `/analysis` 保留 query string。前端：(D) `<AnalysisHomeInbox> + <AnalysisFormHeader> + <AnalysisInboxList>` 主框架；(E) `<RunningRow>` 运行中卡 inline PipelineDAG（v1.17 R-fix-11F 已修对的组件直接复用，桌面默认展开 / 移动默认折叠+进度条 / 按 575.98px 断点同 mobile-optimization）；(F) `<CompletedRow>` 抽取自 v1.18 HistoryPage 行渲染 + 展开摘要 + 操作按钮 + 多选对比（功能完整保留）；(G) 提交不跳页 → 列表顶部乐观插运行中卡 → 订阅 task_events 流（unified-progress per-user room） → task_completed 替换为 analysis 行 / task_failed 替换为失败卡带重试 / task_cancelled 同；(H) 旧 `/analysis/<task_uuid>` URL 前端 `replaceState` 到 `/analysis?task=<uuid>` + Inbox 滚动锚定；(I) Sidebar 删 "分析记录" 菜单项（桌面 line 36 + 移动 line 140），`/history` 路由仍可作书签 redirect。`<AnalysisDetailView>` 完整 8 tab 详情页 + analysis-rendering v1.0/v1.1 8 Card 组件 + PipelineDAG v1.17 / unified-progress v1.0 / 多租户隔离 v1.18 全部不动。复用 v1.18 HistoryPage ~80% 代码 + unified-progress catch-up + shadcn 组件库；自写 ~310 LOC | — |
| 2026-05-01 | [screener-v3.md](screener-v3.md) | v1.2 | 选股结果决策透明化（用户 2026-05-01 提，~5h）：现状信号列全 `-`、看多/看空 KPI 全 0、大师数都是 4（无信息量）、圆桌辩论看不到。Audit 根因：`pipeline._aggregate` 已经计算 bullish/bearish/total 但**没把 verdict 写回 candidate.signal**；前端 ResultsView 只展示 `result.roundtable.summary` 一段而非按 ticker 分卡；展开行只 slice(0,80) 字 reasoning；运行模式 / LLM call / cache 命中 / 大师列表全无显示；价格/PE 有 `/api/fundamentals` 缓存但没接入。方案 v1.2（保持 `RoundtableResult` dataclass `consensus/dissent/split/debate_snippets` 字段不变）：(A) 后端 `_aggregate` 末尾追加每 candidate `signal` (bullish/bearish/neutral/split) + `votes` (bullish/bearish/neutral/total 计数) + `consensus` (unanimous/majority/split chip) + `confidence_range` (min/max/avg) + `top_bull_argument` / `top_bear_argument` (highest-confidence guru reasoning 200 字)；(B) `/api/screen/v3/results/<id>` payload 加 `run_metadata` (mode/llm_calls/cache_hit_pct/duration_sec/gurus_used/candidates_count/roundtable_enabled)，从 pipeline 已输出的 metrics 字段重命名透传；(C) 前端 4 块视觉一次落地：① KPI 扩 6 列（候选/均分/看多/看空/中性/共识率）；② 运行模式 banner（mode chip + LLM call + cache 命中 + 耗时 + gurus_used Avatar 链）；③ Top 5 圆桌独立 grid 按 ticker 卡片化（边框色按 split 标黄/统一标绿，CONSENSUS / CONTESTED Badge，debate_snippets 按 🟢🔴⚖️ 分色 border-l-2 渲染）；④ 候选表扩列（`#/代码/综合分/信号/投票分布条/共识度/现价/PE/操作`，现价/PE 走 `/api/fundamentals` 30s LocalCache 懒加载）+ 展开当前行（不开新页）显示 4 张大师卡（signal Badge + confidence + reasoning 完整段最大 240 字 line-clamp-6 + tier chip + philosophy hover）+ KPI 底栏（200-SMA / PE / ROE / D/E）+ `[→ 跑 AI 分析]` `[→ 加观察列表]` 操作。强约束：不动 `_aggregate` 已有字段（仅追加），不动 `RoundtableResult` 字段名，不开独立投票详情页路由，不发明 fundamentals 端点（复用 v1.6 R-perf 缓存） | — |
| 2026-05-01 | [analysis-rendering.md](analysis-rendering.md) | v1.1 | News / Fundamentals 改混合数据源（用户 2026-05-01 提，~2h 增量）：现状 quick-info 三卡显示 `FINAL TRANSACTION PROPOSAL: **BUY** # 宏观经济与...` 是 LLM markdown 报告头，因为 `extractFundamentals` regex 抽不到数字 / `newsSnippet.slice(0,200)` 截断 LLM 报告。Audit 发现项目里**已有真实数据源**：`data_manager.get_fundamentals(ticker)` (yfinance `Ticker.info` 含 PE/PB/ROE/D/E/营收增长/利润率全套) + `data_manager.get_news(ticker)` (Polygon `list_ticker_news` 优先, yfinance fallback 返 [{title, url, published, source}])，且 `/api/fundamentals/<ticker>` `/api/news/<ticker>` 端点已就绪。方案：(A) 新建 `stock_trading_system/agents/rendering/data_sources.py` 把 yfinance .info → Valuation/Growth/Profitability/BalanceSheet schema 映射 + Polygon news → Headline 映射；缺字段保 None 不发明；(B) 修订 RenderingExtractor 构造时多传 `data_manager`，News tab 调 `fetch_news_headlines` 拉真头条后让 LLM 仅做 sentiment/impact 标注 + catalysts + summary，Fundamentals tab 调 `fetch_fundamentals_facts` 拉真数字后让 LLM 仅写 vs_industry / quality_score / summary；其它 6 tab 不变；(C) **Hard guard**：headlines 必须 title 命中真集合（防幻觉），valuation/growth/profitability/balance_sheet 字典以真 facts 覆盖（防 LLM 改数字）；(D) 前端 quick-info 三卡（最近新闻 / 基本面指标 / 多空辩论）改为直接拉 `/api/news/<ticker>` + `/api/fundamentals/<ticker>` 显示真实 3 条头条 + 6 个真指标 + 多空辩论用 `detail.rendering["Investment Debate"]` 的 bull/bear arg 数 + verdict + key_disagreement；删除 `extractFundamentals / extractDebateCount` regex + `newsSnippet.slice(0,200)` 截断三个脆弱函数；(E) `/api/fundamentals` `/api/news` 已有 LocalCache（v1.6 R-perf 30s TTL），quick-info 卡与 8-tab extractor 共用 cache 不双拉。schema 不变，数据填充策略变 | — |
| 2026-05-01 | [analysis-rendering.md](analysis-rendering.md) | v1.0 | AI 分析结构化渲染（用户 2026-05-01 提，~10h）：现状 8 tab 直接渲染 LLM markdown → 决策结论/KPI/三派辩论被埋长文里。方案分层：每 tab = 结构化卡片头 + Markdown 主体（折叠）。后端：新建 `stock_trading_system/agents/rendering/`（schemas.py 8 个 Pydantic：OverviewCard/MarketCard/SentimentCard/NewsCard/FundamentalsCard/DebateCard/RiskCard/DecisionCard + 共享 KeyMetric/Stance/Argument；extractor.py RenderingExtractor 用 quick_think_llm + with_structured_output 并发 8 个 LLM call），analyzer 在 pipeline_done 前调 extractor 写 `result.rendering`，worker 序列化为 `rendering_json`，`analysis_history` 加 `rendering_json TEXT` 列 + idempotent ALTER；TaskStore `_save_analysis_result` 同步写；`/api/history/<id>` DTO 加 `rendering: {[tab_key]: dict\|null}`，绝不暴露原 JSON 字符串。**严格保证 rendering_json 是共享研究字段**，不含个人 advice/仓位（v1.13 R-fix-7D 边界），技术面客观价位（支撑/阻力/SMA 止损位）允许保留。前端：8 个 Card 组件（OverviewCard/MarketCard/.../DecisionCard）+ 4 共享子组件（RatingBadge/KpiRow/StanceCard/ConfidenceMeter/MoodGauge）；Tab 渲染优先卡片头，markdown 主体走 `<details>` 折叠保留 react-markdown + rehype-sanitize；rendering 缺失或 tab key=null → 单 tab 降级到纯 markdown。8 calls 并发用 ThreadPoolExecutor，单 tab 超时 45s/失败 raise 不影响其它 tab；rendering 失败不阻断分析任务（best-effort）；老分析无 rendering_json 自动降级。验收：8 schema 单元 + extractor 失败隔离 + 详情 DTO 测试 + 前端快照 + npm build。复用 v1.13 R-fix-7B 元数据迁移模式 + paper-trade v1.3 F3 ExecutiveSummary 模式 + v1.13 R-fix-7F rehype-sanitize 通道 + shadcn 组件库；自写 ~1500 LOC | — |
| 2026-04-30 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.12 | v1.11 修了代码没修数据：实测 daily_snapshots 仅 3 行（2026-04-14/15/16/19），距今缺 11 天，`task_scheduler.py:105` take_snapshot 调用存在但调度器没真跑。新增 R-fix-6 共 4 子项（~3h）：(a) `migrations/backfill_daily_snapshots.py` 历史回填（按交易日重放 transactions + yfinance 收盘价 → upsert 幂等）；(b) 修 APScheduler 启动 + cron 美东 16:30 + 多用户迭代 + 启动期 log；(c) Dashboard "↻ 重新计算"按钮 + `/api/portfolio/snapshots/backfill` 异步 task；(d) Settings 页"调度器状态"卡 + `/api/scheduler/status` & `/api/scheduler/run-now`。验收：DB ≥ 14 行 snapshot，dashboard ALL 14 连续点 | — |
| 2026-04-30 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.13 | AI 分析模块 7 大产品&技术缺口（用户提，~10h）：(A) **TVChart 修初始 Skeleton 早 return**——`TVChart.tsx:108` `if (loading && data.length===0) return <Skeleton/>` 导致 chart 容器永不挂载；改为始终渲染容器 + overlay 三态（loading/empty/error）+ `onRetry`；详情页 K 线主路径 `/api/quote/history?days=90`、回退 `/api/chart/<ticker>?period=3mo`；(B) **analysis_history 元数据补齐**——schema 加 `created_by/provider/config_hash/task_id/duration_sec/bookmarked` 6 列 + idempotent ALTER TABLE，TaskStore `_save_analysis_result` INSERT 同步写，worker 注入 `provider/model/config_hash/duration_sec/created_by/task_id`，详情页 Header 显示创建者/Provider/耗时/创建时间；(C) **废 `/api/analyze` 旧路径**——删 daemon thread + 直写 history + 硬编码 `gemini.deep_think_model`，改 thin wrapper 转 TaskManager 返 `{task_id, status}`；(D) **拆分共享分析与个人 advice**——新建 `user_analysis_advice` 私有表（user_id+analysis_id+持仓 snapshot+action+entry/stop/take）；`analysis_history` 仅共享研究；`/api/history/<id>` LEFT JOIN advice WHERE user_id=current（别人的 advice 永远不返）；(E) **`/analysis` 产品闭环**——首页 5 条最近卡 + 深度 RadioGroup（quick/standard/deep）+ 详情 8 tab（"决策"独立）+ 操作按钮 5 个（再次分析/加入持仓追踪/导出 PDF/MD/分享/收藏 per-user）；新增 `GET /api/history/<id>/export?format=pdf\|md` + `POST /api/history/<id>/bookmark` + `POST /api/portfolio/track`；(F) **Markdown sanitize**——`react-markdown` 加 `rehype-sanitize` + 白名单 schema（table/code/pre），防 LLM 注入；(G) **修 `_record_agent_scores` 双重记录**——之前为拿 analysis_id 先 save 半成品行 + worker 主路径再 save 完整行 → 同次分析 2 行；改为 TaskManager 落库取得真 analysis_id 后调 `scorer.record_analysis`，确保唯一。剩余 ~13h | — |
| 2026-05-01 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.14 | R-fix-7 验收 4 处遗留（用户 2026-05-01 提，~2.5h）：(A) **worker/analyzer progress_cb 契约对齐**——`workers.py:57` 无条件传 `progress_cb=` 但 `FakeAnalyzer.analyze(self,ticker,date)` 不收 → `pytest tests/tasks/test_workers.py` 红；统一契约为"analyzer.analyze 必须接受可选 progress_cb=None"，FakeAnalyzer 加 kw + 至少 1 case 验证 events；(B) **`/api/history/<id>` advice_json 跨用户泄露关闭**——legacy fallback 加 `record.created_by == g.user.id` 守卫；非创建者也 strip action/confidence/position_pct/entry_low/entry_high/stop_loss/take_profit 反推字段；`_migrate_analysis_history` 把 advice_json 搬到 user_analysis_advice 成功后**清空共享行的 advice_json + 7 结构化字段**（事务内）；新增测试 `test_bob_does_not_see_alice_legacy_advice_json`；(C) **TaskStore advice 后门关闭**——`_save_analysis_result` 删除 `if result.get("advice")` 兼容分支，advice_json 硬编码 `""`、action/confidence/position_pct/entry/stop/take_profit 硬编码 `None`；新增测试 `test_save_analysis_result_strips_advice` + `test_worker_advice_payload_routes_to_user_advice`；(D) **depth 参数真实生效**——`analysis_history` schema 加 `depth TEXT` 列 + idempotent ALTER；worker 读 `params.depth` 归一化到 `{quick,standard,deep}` 写入 result 与 DB；最小行为差异化：quick 强制关 iteration、deep 沿用 config iteration、standard 默认；详情页 Header meta 行展示"分析深度"；前端 RadioGroup 文案降级为不绝对化（quick 跳过迭代 / deep 启用迭代如已配置）；新增测试 `test_worker_persists_depth` / `test_worker_default_depth_is_standard` / `test_worker_unknown_depth_falls_back_to_standard` / `test_detail_returns_depth`。剩余 ~10.5h | — |
| 2026-05-01 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.17 | AI 分析 4 链彻底闭环（用户 2026-05-01 提，~4.5h）：v1.14/v1.15 多项设计**未真实落地**本轮真改 + 加 2 项 NEW。(A) **worker / Analyzer `progress_cb` 契约统一**——`StockAnalyzer.analyze(ticker,date,progress_cb=None)` 必选 kw；worker 调用加 `TypeError` fallback 兼容旧适配器（fallback 到 `analyze(ticker,date)`）；`tests/tasks/test_workers.py FakeAnalyzer` 加 `progress_cb=None` 入参 + 验证事件转发 case；(B) **`/api/history/<id>` advice_json 跨用户守卫**——legacy fallback 前置 `is_creator = (record.created_by == g.user.id)` 守卫；非创建者额外 strip `action/confidence/position_pct/entry_low/entry_high/stop_loss/take_profit` 反推字段；`_migrate_analysis_history` 把 advice_json 搬到 user_analysis_advice 后清空共享行 advice_json + 7 结构化字段（事务内）；新增 `test_bob_does_not_see_alice_legacy_advice_json`；(C) **TaskStore advice 后门彻底关闭**——`_save_analysis_result` 删除 `if result.get("advice"): advice_raw = json.dumps(...)` 兼容分支；INSERT 时 `advice_json` 硬编码 `""`、7 结构化字段硬编码 `None`；CREATE TABLE 加 `depth TEXT` 同步；新增 `test_save_analysis_result_strips_advice` + `test_worker_advice_payload_routes_to_user_advice`；(D) **provider/model cache_key 错配修复（NEW）**——`StockAnalyzer._init_graph` 废弃 `cfg["llm"]["model"]`（配置里根本没这条）；新建 `_resolve_active_model()`：qwen→`qwen.deep_think_model \|\| qwen.model \|\| 'qwen-plus'`，gemini→`gemini.deep_think_model \|\| gemini.model \|\| 'gemini-2.5-flash'`；cache_key=`{provider}:{真实 model}`；同 provider 不同 model 切换创建新 graph，切回原配置复用；worker `_resolve_active_provider_model` + `_hash_llm_config` 用同路径；3 个 provider switch 测试；(E) **depth 真实端到端生效**——`analysis_history` schema 加 `depth TEXT` 列 + idempotent ALTER；worker 读 `params.depth` 归一化 `{quick,standard,deep}` 未知值回落 standard，写入 result 与 DB；analyzer 加 `set_depth()` 钩子（quick 强制关 iteration、deep 沿用 config、standard 默认）；详情页 Header meta 显示"分析深度"；前端 RadioGroup 文案降级为不绝对化；新增 4 个测试 `test_worker_persists_depth / _default_is_standard / _unknown_falls_back / _quick_disables_iteration` + `test_detail_returns_depth`；(F) **PipelineDAG 契约对齐（NEW）**——前端 STAGES 改为与后端 `PIPELINE_STEPS` 完全一致 7 节点 `market/social/news/fundamentals/debate/risk/decision`（之前是 9 个 `market_agent/sentiment_agent/.../trader` 完全错位）；事件按 `payload.type` 分支：`pipeline_start` 仅设 first→running 不 done，`step_start` 按 step 标 running，`step_done` 按 step id 匹配标 done + next pending→running，未知 step 忽略不前进；废除 `currentIdx++` 顺序 fallback（之前导致 pipeline_start 就跳完所有节点）；(G) **"加入持仓追踪"按钮拆分**——改名"加入观察列表"调 `/api/portfolio/track`（toast 明示"不会自动下单"）+ 新增"按此建议纸面交易"调 `/api/paper/track` 返 `{plan_id, num_orders, triggered}`，按 toast 显示"已生成 / 立即成交 N 单 / N 单待触发"。剩余 ~3h | — |
| 2026-05-01 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.16 | Dashboard / 持仓多租户契约修复（用户 2026-05-01 提，~5h）：(A) **PortfolioDatabase 默认 schema 与运行时对齐**——`_init_tables` 默认 CREATE positions/transactions/daily_snapshots/alerts 全部含 `user_id`；positions `UNIQUE(user_id,ticker)`、daily_snapshots `UNIQUE(user_id,date)`；老 DB 兜底用 `ALTER TABLE ADD COLUMN user_id` + 复合 UNIQUE INDEX `ux_positions_user_ticker / ux_snapshots_user_date`；NULL user_id 自动 backfill 到 first active user；fresh DB 直接调 add_position/sell_position/take_snapshot/add_alert 不再 OperationalError；(B) **`/api/search` 补 user_id 过滤**——positions/transactions/alerts 全加 `user_id=g.user.id`；transactions 不再用 notes 参与匹配（避免私人备注泄露）；analysis_history 共享研究保留；加 `@login_required`；(C) **AlertMonitor + `/api/dashboard.alerts_count`**——`list_alerts(user_id, scope='user'\|'all')` / `check_alerts(...)`；`/api/dashboard` 仅当前用户；后台 cron 显式 `scope='all'`；(D) **持仓交易校验 + 卖空守卫**——新建 `_validate_trade(data, require_existing, user_id)` helper：ticker 字母数字 / shares>0 / price>0 / 卖出必须先有持仓 + shares ≤ 持仓；校验失败 400 不写 transaction；`PortfolioManager.sell_position` 删除"无持仓也写 transaction"兜底，改 raise；(E) **交易记录字段契约统一**——`/api/portfolio/transactions` 返 `action` 大写 (BUY/SELL) + `timestamp` + `date` 别名兼容老前端；前端按大写上色绿/红；(F) **today_pnl 文案/数据修正**——`api_portfolio_summary` 不再让 today_pnl 复用 total_pnl；新增 `_compute_today_pnl(user_id, current_value)` 基于昨日 daily_snapshot 算真实日内变化，无昨日时返 null；前端 `today_pnl != null` 显示"今日 PnL"否则降级"总盈亏"；(G) **测试** `test_fresh_db_add_position_works / _sell / _take_snapshot / _add_alert` + `test_search_positions_isolated / _alerts_isolated / _transactions_notes_not_indexed` + `test_dashboard_alerts_count_only_self` + 校验 6 个用例 + 字段契约 3 个用例。剩余 ~3.5h | — |
| 2026-05-01 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.15 | AI 建议 → paper trade 执行链贯通（用户 2026-05-01 提，~6h）：(A) **SignalLoader 切到 `user_analysis_advice`**——`SignalLoader` 的 `__init__/load/get_one/backfill_all` 全加 `user_id` 入参；主源改读 `user_analysis_advice` 并转成 paper trade 期望字段（双键名 `suggested_position_pct↔position_pct` / `entry_price_low↔entry_low` / `entry_price_high↔entry_high`）；`analysis_history.advice_json` 仅当 `created_by==user_id` 才允许 legacy fallback，非创建者一律 advice 返 `{}`；(B) **`TaskManager._post_analysis_save` 加第 3 步驱动 paper trade**——`save_user_advice` 之后追加 `ensure_ticker_session(user_id) + process_analysis(...)`，传 analysis_id/ticker/date/signal/advice + trade_decision/risk/debate；尝试拿 router 当日价以触发 immediate order；失败仅 warning 不影响 task；(C) **`/api/paper/track` 升级**——`@login_required`，读 `pdb.get_user_advice(g.user.id, analysis_id)` 喂给 `process_analysis`；返 `{ok, session_id, plan_id, num_orders, triggered}`；旧 `manual_track` 行为改为 audit log；(D) **UI 拆分观察列表 vs 纸面追踪**——`AnalysisDetailView` 操作栏"加入持仓追踪"改名"加入观察列表"（仍 `/api/portfolio/track`）+ 新增"按此建议纸面交易"按钮调 `/api/paper/track`；toast 文案区分"已生成 plan / 立即成交 N 单 / N 单待触发"；(E) **收敛主链路 + 列表字段补全**——`PaperTradeStore.list_ticker_sessions` SQL 子查询返 `active_plan_count / pending_orders_count / triggered_orders_count / open_position_shares / last_eod_date / last_skip_reason`；`/api/paper/tickers?mode=forward\|replay` query 过滤；列表页 tab 切换前向追踪/历史回放；(F) **PaperTradeStore schema 自初始化补 v1.3 列**——`_SCHEMA_TRADING_PLANS` 加 `fingerprint/reconfirmed_count/reconfirmed_at/analysis_ids` 4 列；`_init_schema` 幂等 ALTER 老 DB + 创建 `ix_plans_session_ticker_fp` 索引；`migrations/paper_trade_v1_3.py` 保留作历史路径但核心功能不再依赖；(G) **测试**——10 个新增/扩展用例：`test_signal_loader_reads_user_advice / _legacy_fallback_only_for_creator / _normalizes_dual_keys` + `test_paper_track_returns_plan_id / _uses_user_advice_not_shared / _bob_cannot_use_alice_advice / _immediate_execution` + `test_post_analysis_save_drives_paper_trade / _paper_trade_failure_is_swallowed` + `test_fresh_db_save_plan_works_without_migration / _existing_db_idempotent_migration`。剩余 ~4.5h | — |
| 2026-04-27 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.11 | §6.1 R-fix-1~5 全部真修：(1) **R-fix-1** 净值曲线后端：`PortfolioDatabase.get_snapshots(days=None)` 不过滤、`days=N` 改用 `WHERE date >= today - N` 并 `ORDER BY date ASC`；`PortfolioManager.get_history` 透传 None；`/api/dashboard?history_days=all\|<int>` 解析；DashboardPage 默认 `?history_days=all` 让 ChipRow 客户端切片完整序列。功能验证：14 天 snapshot（2026-04-14 → 2026-04-27）all 全显，7d 仅 8 行。(2) **R-fix-2** Dashboard 持仓概览每行加 `+$X.XX` 绝对值列（取 `holding.pnl`，颜色 >0 绿/<0 红/=0 灰），后端 `get_holdings()` 已返回 `pnl` 字段。(3) **R-fix-3** `/api/screen/v3/results/<task_or_result_id>`：先按 task UUID 反查、依 `result_ref` 解析 `screen_results_v2:N` 或 `task_results_generic:N` 两种存储，整数 fallback 兜底；新增 `_result_ref_to_int` / `_params_dict` helpers。三种 ID 形态全部 200，bogus 返 404。(4) **R-fix-4** PaperTradePage `useState(mainTab)` 提到所有 conditional return 之上 — 这是真正的 ErrorBoundary 触发根因（违反 React rules of hooks）；额外加 `[...orders].sort` 不可变、`pct_target_total ?? 0`、`STATUS_ICONS[o.status] ?? <Clock4>`、`start_capital ?? 0` 兜底。(5) **R-fix-5** `/api/quote/history?ticker=&days=` 新建：days→period 映射（5d/1mo/3mo/6mo/1y/2y/5y），返 `{ticker, days, bars: [{date,open,high,low,close,volume}]}`，错误返 200 + 空 bars 不阻塞 UI；AnalysisPage K线主路径切到新端点，`/api/chart/<ticker>` 作 fallback。验收：`tsc --noEmit` 净、`npm run build` ✓、4 项 server-side R-fix live smoke pass、原 P0 81 tests 仍 pass | — |
| 2026-04-26 | [product-architecture-optimization-instructions.md](product-architecture-optimization-instructions.md) | v1.2 | §7 P0 全部 8 项落地：(1) `set_llm_provider` 改用 `_reset_config_dependent_singletons` 并清空 `_analyzer`；(2) `SettingsPage.tsx` 重写为 dotted-path schema（gemini.* / qwen.* / alerts.email.* / alerts.telegram.*），删除旧聚合 provider/model/api_keys/alerts；移除 OpenAI/Anthropic/DeepSeek 字段；(3) 任务 scope 前后端契约统一为 `mine\|shared_research\|all`，未知 scope 在 web 层降级到 mine、在 store 层用不可能谓词兜底，TasksPage 增加"共享研究" tab；(4) `GET /api/tasks/<id>` 接入 `_check_task_ownership`，新增 `_sanitize_shared_task` 按 type 白名单脱敏 params_json；(5) screen_v2/screen_v3/paper_trade/paper_backfill 路由全部显式传 `created_by=g.user.id`；(6) `TaskManager._emit` 直接 emit 到注入的 socketio 并按 `user:{owner}` room 分发，event_emitter 拆分 `persist_event` / `emit_event`；(7) 新建 `tests/conftest.py`：临时 `STOCK_CONFIG_DIR` + 临时 portfolio.db + admin/alice/bob 三 fixture client，`test_llm_provider_api.py` / `test_task_api.py` 改写为登录后业务测试 + 跨用户隔离 case；(8) 前端 npm build 修：新建 `vite-env.d.ts` 声明 css module，TVChart 改 v5 `chart.addSeries(CandlestickSeries/HistogramSeries)`，删 PipelineDAG 死代码，AnalysisDetail 加 `task_id`，PaperTradePage Badge `secondary→muted`，ScreenerV3Page Guru 类型补全 principles/tier。验收：`pytest tests/auth tests/web/test_llm_provider_api.py tests/tasks/test_task_api.py tests/tasks/test_task_manager.py` 81 passed；`npm run build` ✓ | — |
| 2026-04-26 | [product-architecture-optimization-instructions.md](product-architecture-optimization-instructions.md) | v1.1 | 验收回归补充指令：合并本轮代码验证结果，新增 §7 P0 修复清单（LLM 切换 `_invalidate_singletons` 运行时错误、Settings dotted path schema、任务 scope 泄漏、任务详情权限、screen_v2/v3 created_by、TaskManager 事件、鉴权测试夹具、前端 build）+ P1 产品/架构建议 + 可复制给实现会话的执行指令 | — |
| 2026-04-21 | [ui-react-island.md](ui-react-island.md) | v1.0 | UI React Island 迁移方案：新建 `web/frontend/`（Vite 8 + React 19 + TS + Tailwind v4）+ `vite_helpers.py` 读 manifest + `layout.html` 共享 Jinja 壳 + 4 个 island entry（screener-v3 / tasks / paper-trade / dashboard）+ 共享 `lib/api.ts`（CSRF/fetch）+ `lib/socket.ts`（SocketIO + catch-up）+ `components/ui/` 14 个 shadcn 风格组件 + `components/shared/ProgressStream` 复用 unified-progress envelope + manualChunks（react-vendor/radix/ui 三共享 chunk）+ Railway Nixpacks 加 nodejs-18 + 7 简单页保留 Jinja + 7 Phase 实施计划（~28h）| — |
| 2026-04-21 | [ui-react-island.md](ui-react-island.md) | v2.0 | 完整迁移：新增剩余 11 页页面规格（Portfolio / History / Alerts / Reports / Backtest / Paper list / Analysis 列表+详情 / Settings / Login / Register / Reset）+ 新增共享组件（`<AppShell>`、`<DataTable>`（@tanstack/react-table）、`<Form>`（react-hook-form+zod）、`<EChartsPanel>`、`<AuthCard>`、`<SettingsTabs>`、`<FilterBar>`）+ Phase 8-19 共 ~47h 实施计划 + Phase 18 废弃旧 index.html / app.js / Bootstrap（减 5000+ LOC）+ v2.0 新依赖清单（@tanstack/react-table / react-hook-form / zod / echarts / react-markdown）| — |
| 2026-04-24 | [ui-react-island-pages.md](ui-react-island-pages.md) | v1.0 | 剩余 11 页完整施工图（独立文档）：每页 10 子节（路由 / 当前 Jinja 诊断 / 桌面+移动 mockup / TS 数据模型 / 页面状态机 / 组件树 / 后端 API 契约 / 交互清单 / 空态loading error 边界 / 移动端+a11y）+ 7 共享组件详规（AppShell / DataTable / Form / EChartsPanel / AuthCard / SettingsTabs / FilterBar）+ 页面矩阵总览 + 10 个新增后端端点汇总 + 共享 types.ts 索引 + v2.0 复用审计（自写 ~3600 LOC，库替代 ~2500 LOC）| — |
| 2026-04-24 | [ui-migration-validation.md](ui-migration-validation.md) | v1.0 | 架构级迁移完整验证计划：6 层验证（L0 冒烟 / L1 基础 / L2 功能回归 / L3 跨模块集成 20 场景 / L4 数据完整性 snapshot+invariant / L5 对抗性）+ 新增 `stock_trading_system/validation/` 目录（snapshot / compare / invariants / cross_user_access / sign_off 脚本）+ 迁移前数据快照机制 + 业务不变量 SQL 检查（10 条） + 执行流程（pre-flight / 上线日 / post-deploy）+ 签字凭证生成 + 回滚预案 + 性能基线对比 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.0 | 迁移回归修复方案：合并 11 页功能回归（59 项：8 CRITICAL / 20 HIGH / 23 MEDIUM / 1 LOW，含 Portfolio 卖出消失、Paper-trade 权益图消失、Analysis 7-tab 报告全失、Dashboard stat 溢出 等）+ 移动端适配 15 项（与 mobile-optimization.md 断点不一致）+ R-1~R-7 七 Phase 实施 ~42h + 横切组件升级（Stat 强制 --fs-stat clamp / ChartPanel 共享 / form-row-mobile 套接 / tabs-scrollable / 表格→卡片）+ 严格"只补不改"约束 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.1 | 补充修订：(1) paper-trade 列表页 PT-P1-2 升级 P0（实测 `/paper-trade` 路由不存在，整个入口缺失，新建 island/paper-trade-list/） (2) 新增 §4.12 菜单重组方案：6 大组（概览/分析/选股/持仓/纸面交易/系统）+ 11 叶子；含桌面 Sidebar 分组组件 + MobileTabbar 5+更多 sheet 设计；总工时 ~42h → ~49h；CRITICAL 8 → 9 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.2 | 补充修订：Tasks 4 项升级 P0 CRITICAL（T-P0-1 历史无分页/无限滚动 + T-P0-2 类型 chip-row 8 类 + T-P0-3 scope tab 我的/全部 + T-P0-4 9 类 task → 落地页 URL 映射表 `lib/tasks.ts::getTaskResultUrl(task)` + T-P0-5 详情操作齐全）；新增 R-3b Phase（Tasks 改造 ~4h）；总工时 ~49h → ~53h；CRITICAL 9 → 14 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.3 | 补充实测发现的 4 个 P0：(1) PT-P0-3 paper-trade ticker 详情空白 bug（pathname 末尾斜杠 → ticker 解析为空）；(2) T-P0-6 任务中心空白 bug（后端 `{items}` vs 前端期望 `{tasks}` schema 不匹配）；(3) SE-P0-1 设置缺 GEMINI_API_KEY + QWEN_API_KEY 字段；(4) MS-P0-1~4 整个 Model-Switch UI 缺失（后端 100% 已就绪，前端 0 组件）—— 新增 §4.13 LLMSwitcher 详细规格（4 状态：active/缺 key/env 锁定/loading），挂 NavTopbar；R-1 含 LLMSwitcher（+3h）+ R-3b 含 schema 修复（+1h）+ 新增 R-5b（Settings keys 1h）；CRITICAL 14 → 22；总工时 ~53h → ~57h | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.4 | 实施进度审计：R-1~R-7 commits（6e583b4..5370863）落地后实测 22 P0 中 18 ✅ DONE / 6 ❌ MISSING（MS-P0-1~4 LLMSwitcher 全套 + SE-P0-1 Gemini/Qwen key + A-P0-3 Pipeline DAG）+ 1 ⚠ PARTIAL（T-P0-6 schema）。新增 §0 实施进度状态表 + §6.1 R-1.x 收尾批次合并指令（~5h：LLMSwitcher 3h + Settings keys 10min + Pipeline DAG 2h + Tasks schema 验证 5min）；完成 R-1.x 后 P0 闸门绿 → 可 sign-off | `6e583b4..5370863` (R-1~R-7) |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.4.1 | 修订 T-P0-6 状态：实测前后端均已对齐（app.py:1713 双字段 + TasksPage.tsx:70 fallback 已含 items），由 PARTIAL → DONE。DONE 19，余 6 项（仍是 LLMSwitcher 4 + Settings keys 1 + Pipeline DAG 1）。R-3b.1 标 NO-OP，移出工作清单 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.5 | 实测发现 R-1~R-7 仍有 6 项 P0 + 1 错位需修：(1) HE-P0-1 History 跳转 analysis 详情 URL `/analysis/analysis_history:33` 类损坏（int vs string id 路由不匹配 → 404）；(2) SV3-P0-1 Screener V3 结果页**完全未建**（仅表单，无 result rendering）；(3) A-P0-3a Pipeline DAG 位置错（仅表单视图有，详情页 161-285 无）；(4) A-P0-4 K线应用 TradingView widget 替代 ECharts candlestick；(5) A-P0-5 缺新闻+基本面 quick-info 侧卡；(6) B-P0-1 Backtest 结果详情页未建（仅表单，跳 /tasks）；(7) D-P0-5 dashboard /backtest-v2 死链。新增 6 个 R-1.x 子任务（~7h），R-1.x 总剩余 5h → 12h；总 P0 缺失 6 → 12 项 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.6 | 性能 PERF-P0-1：实测 Dashboard / Portfolio 加载 ~3s 慢，根因后端 `get_pnl()` / `get_allocation()` 都内部调 `get_holdings()` → 单次 HTTP request 拉 2 次全量实时价格。新增 R-perf（~1.5h）：Layer A `PortfolioManager.get_holdings()` 加 Flask `g` request-scoped memoize（一次 request 只拉一次价，~30min，砍半）+ Layer B `data_manager.get_price()` 复用 LocalCache 加 30s TTL（30 秒内复访 < 200ms，~1h）。总剩余工作 12h → 13.5h。固化 doc-first 工作流到长期记忆 | — |
| 2026-04-25 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.7 | 实测两个 P0：(1) PT-P0-4 paper-trade ticker 详情完全空白（PaperTradePage.tsx:68 pathname.split 仍老代码，v1.3 PT-P0-3 audit 误判 DONE，需真修 regex/filter(Boolean) + 加 React ErrorBoundary）；(2) A-P0-7 AI 分析提交后跳 /tasks/<id>，用户看不到 Pipeline DAG 实时 + 7-tab 流入，需改跳 /analysis/<task_id>，配合 R-5.1 显示 DAG + 占位 tabs，完成后切 7-tab。新增 R-3.1（30min）+ R-5.4（1h）；总剩余 13.5h → 15h | — |
| 2026-04-26 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.8 | 功能补强 2 项：(1) D-FEAT-1 仪表盘净值曲线自动回溯（后端 get_history(days=None) 全量，前端 range switcher 全部/1Y/6M/3M/1M/7D + dataZoom，从最早 daily_snapshot 起）；(2) P-FEAT-1 持仓表加"盈亏 $"绝对值列（后端 get_holdings.pnl 已就绪，前端补显示，dashboard 概览 + 移动 m-card 同步），与"盈亏 %" 并列。共 ~2h；剩余 15h → 17h | — |
| 2026-04-26 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.9 | A-P0-8：实测 AI 分析提交后页面完全空白。运行中态完整布局（合并强化 v1.7 A-P0-7 + R-5.1/2/3）：跳 /analysis/<task_id> 立即显示 K线 + 三列侧卡（新闻/基本面/多空比，独立拉 API） + Pipeline DAG 实时（订阅 agent_stage_done） + 7-tab 占位 skeleton（agent 完成后逐个填 markdown），完成后 history.replaceState 切 /analysis/<analysis_id>。~3h（含在 R-5.x 工作量）；剩余 17h → 20h | — |
| 2026-04-26 | [ui-react-island-regression.md](ui-react-island-regression.md) | v1.10 | 生产环境实测仍 5 项未真修（之前轮次号称 DONE 但漏改）：(1) D-FEAT-1 后端 `get_history(days=30)` 硬编码，前端 ChipRow 无效；(2) P-FEAT-1 Dashboard 持仓 top3 漏加绝对值列；(3) SV3-P0-1 前端 task_id (UUID) 拉 `/api/screen/v3/results/<id>` 路由期望整数；(4) PT-P0-4 PaperTradeContent 内部抛错触发 ErrorBoundary，需 null check；(5) R-5.3 TVChart 已挂但 klineData 空，需 `/api/quote/history` 端点。新增 R-fix-1~5（~3h）；剩余 20h → 23h | — |
| 2026-04-20 | [unified-progress.md](unified-progress.md) | v1.0 | 统一实时进度系统（后端 + 前端）：新建 `task_events` 表 + `emit_event()` 统一入口替代散落的 `socketio.emit` + Per-user SocketIO room 隔离 + `GET /api/tasks/events?since=<seq>` 断线续传 + `GET /api/tasks/running` reconnect 同步 + 前端 `ProgressStream` 组件（3 布局 compact/detail/inline-badge × 5 态 connecting/streaming/stalled/disconnected/done）+ 5 页面统一挂载（任务中心/分析/screener-v3/batch/backtest）+ 断线 banner UX + 全局连接指示灯 + 7 天 events 保留 + 11 种 task 事件标准 envelope | — |
| 2026-04-19 | [paper-trade.md](paper-trade.md) | v1.3 | UX / 数据 surface 5 处修正：F1 Plan 内容指纹 dedup（fingerprint + reconfirmed_count + analysis_ids）+ F2 底部 "AI 最终决策" 改渲染 analysis_history.trade_decision 全文 + F3 新增 analysis_history.executive_summary 列并用 `with_structured_output(ExecutiveSummary)` 抽取（禁写字面量 "regex 解析"）+ F4 日度图表 ECharts 双 grid + drawdown markArea + visualMap + F5 "时间轴 / 策略历史" 合并为 "执行记录" 单 tab 内嵌双视图切换（按 Plan / 按 Event） | — |
| 2026-04-19 | [screener-v3.md](screener-v3.md) | v1.0 | 智能选股 V3：新建 `stock_trading_system/screener/v3/` + 14 大师 agent（12 位 virattt clean-room 重写 + Marks/Dalio 自建）+ 并发 Semaphore(10) + `(ticker,guru,date)` 缓存 + 成本预估动态校准 + Round-table 辩论（Top 5）+ 流式 WebSocket + 预选配置面板 + 经典阈值模式兼容保留（v2 gurus 不动）+ `BaseGuru` 接口保留 aggregator/regime 零改动 | `042a98d` (P0), `8c3ceea` (P1), `40bbad4` (P2+3), `5e0ca93` (P4), `141f8fa` (P5), `cca0ed1` (P6) |
| 2026-04-19 | [screener-v3.md](screener-v3.md) | v1.1 | 复用审计修订（依据 [engineering-principles.md](../engineering-principles.md)）：§4.1 改用 LangChain `ChatOpenAI.with_structured_output(GuruSignal)` 替代自写 JSON 解析；§4.3 改用 `tenacity` 替代自写重试循环；§4.9 复用 TradingAgents `bull_researcher` / `bear_researcher` / `conservative_debator` / `reflection` 作辩论图基底；新增 §12 复用清单；自写 LOC ~31% 下降 | — |
| 2026-04-19 | [engineering-principles.md](../engineering-principles.md) | v1.0 | 新建跨文档原则：复用优先（L0 项目内 → L1 依赖库 → L2 vendor → L3 clean-room → L4 自写）+ 应用规则 + 4 份近期设计审计矩阵 + 对 screener-v3 产生 3 处 P0 修订 | — |
| 2026-04-19 | [multi-tenant.md](multi-tenant.md) | v1.0 | 多租户：新建 `stock_trading_system/auth/` 模块（password/session/invite/repository/decorators/bootstrap）+ 3 张新表（users/invite_codes/user_settings）+ `analysis_bookmarks` 共享收藏 + 6 类私有表 + paper 子表 user_id FK 迁移 + Flask session 认证 + admin 首启自动迁移脚本 `migrations/to_multi_tenant.py` + model-switch router 签名加 user_id + 前端 login/register 两页 | `af6965c` (P0), `01354f3` (P1), `1ac99a5` (P2), `f69b526` (P3), `e1db42b` (P4), `5d32293` (P5) |
| 2026-04-19 | [mobile-optimization.md](mobile-optimization.md) | v1.0 | 移动端统一优化（11 页 × 3 断点）：设计 tokens（断点/字号 clamp/触摸 44px）+ 7 个通用组件（form-row-mobile / num-responsive / table-to-cards / tabs-scrollable / collapse-row / btn-group-wrap / chip-row）+ 11 页逐项清单。含 screener v1.2 移动端专项 | `e198acd` (P0), `1bfd290` (P1), `2380d09` (P2) |
| 2026-04-18 | [model-switch.md](model-switch.md) | v1.0 | 全局模型切换：新建 `stock_trading_system/llm/` 模块（router + client + constants），analyzer graph 按 provider 缓存，screener V2 call site 改用 LLMTextClient 抽象，Nav 下拉 + `/api/settings/llm-provider` GET/POST | `f682855` (P1), `dffbab4` (P2), `a549b90` (P3), `e21f6ac` (P4) |
| 2026-04-18 | [self-iterating-agents.md](self-iterating-agents.md) | v3.0 | 自我迭代能力模块（包裹 TradingAgents）：最大化复用版。Agent Scorer(唯一核心新模块) + Darwinian 权重(采用 atlas-elenchus 常量) + Meta Agent(采用 atlas MUTATOR_SYSTEM_PROMPT) + A/B 验证复用 paper trade sessions。仅 2 张新表 | `e083788` |
| 2026-04-18 | [batch-analyze-holdings.md](batch-analyze-holdings.md) | v1.0 | 一键持仓分析方案：batch_analysis worker + 逐只推送 + 跳过近期 + 进度面板 | — |
| 2026-04-12 | [ui-ux-redesign.md](ui-ux-redesign.md) | v1.0 | 初版 UI/UX 重设计方案：9 页面线框图、移动端适配、组件规范（8 大类） | — |
| 2026-04-12 | [technical-design.md](technical-design.md) | v1.0 | 初版技术方案：前端改造 + 后端 API 补全 + 回测引擎 + PRD 追踪矩阵 | — |
| 2026-04-15 | [architecture-upgrade.md](architecture-upgrade.md) | v1.0 | 架构升级方案：Qwen 主导数据层 + TradingView 图表 + 异步任务系统 | — |
| 2026-04-15 | [architecture-upgrade.md](architecture-upgrade.md) | v1.1 | 补充：三层数据策略、TaskManager 设计、缓存策略、配置扩展 | — |
| 2026-04-15 | [screener-v2.md](screener-v2.md) | v1.0 | 智能选股 V2：8 Agent 并行打分 + 8 Guru 哲学 + Regime 自适应权重 | — |
| 2026-04-16 | [screener-v2.md](screener-v2.md) | v1.1 | 修订：NL 驱动优先，strategy chip 退化为辅助提示 | — |
| 2026-04-16 | [paper-trade.md](paper-trade.md) | v1.0 | 纸面交易方案：AI 信号回放 + 前向追踪 + 权益曲线 | — |
| 2026-04-16 | [paper-trade.md](paper-trade.md) | v1.1 | 修订：独立菜单 + 可选留痕 + 个股绑定 analysis_tracked 表 | — |
| 2026-04-16 | [paper-trade.md](paper-trade.md) | v1.2 | 修订：全量自动追踪（所有非 ERROR 分析自动进入默认 session） | — |
| 2026-04-15 | — | — | Phase A-E 架构升级实施完成（commit `034efe0`） | `034efe0` |

## 决策摘要

| 决策 | 选择 | 原因 |
|------|------|------|
| K 线图方案 | TradingView Widget（展示）+ ECharts（净值/回测） | TV 零数据成本，专业体验 |
| 回测数据源 | yfinance + LocalCache，不走 Qwen | 精度 + 可复现性 |
| 异步任务 | ThreadPoolExecutor + SQLite，非 Celery | 单人项目，零运维 |
| 数据主源 | Qwen（语义/判断/单点查询），Local（精确结构化） | LLM 做语义，专业 API 做数据 |
| 选股 V2 NL 优先 | 自然语言输入为主，strategy 为辅 | 用户反馈 |
| 纸面交易追踪 | 全量自动，非 opt-in | 100% 覆盖率，AI 效果可量化 |
