[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m stock_trading_system.main web"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
