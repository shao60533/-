[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn -k gthread -w 1 --threads 4 --timeout 120 --bind 0.0.0.0:$PORT \"stock_trading_system.web.app:create_app()\""
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
